@model KadirliApp.Application.Features.Transport.Commands.CreateIntercityRouteCommand
@{
    ViewData["Title"] = "Yeni Şehirlerarası Hat";
    var departurePoints = ViewBag.DeparturePoints as IReadOnlyList<KadirliApp.Application.Features.Lookups.DeparturePointAdminDto>
                          ?? new List<KadirliApp.Application.Features.Lookups.DeparturePointAdminDto>();
}

<div class="max-w-3xl mx-auto">
    <div class="mb-6 flex justify-between items-center">
        <div>
            <h2 class="text-2xl font-bold text-gray-800">Yeni Şehirlerarası Hat</h2>
            <p class="text-sm text-gray-500 mt-1">Hattı oluşturduktan sonra kalkış saatlerini ekleyeceksiniz.</p>
        </div>
        <a asp-action="Intercity" class="text-indigo-600 hover:text-indigo-800 text-sm font-medium transition-colors">
            <i class="fas fa-arrow-left mr-1"></i> Listeye Dön
        </a>
    </div>

    <div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden">
        <form asp-action="IntercityCreate" method="post" class="p-8">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="md:col-span-2">
                    <label asp-for="Destination" class="block text-sm font-semibold text-gray-700 mb-2">Varış Yeri</label>
                    <input asp-for="Destination" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all shadow-sm outline-none" placeholder="Örn: Adana" required />
                    <span asp-validation-for="Destination" class="text-red-500 text-xs mt-1 block"></span>
                </div>

                <div>
                    <label asp-for="Company" class="block text-sm font-semibold text-gray-700 mb-2">Firma</label>
                    <input asp-for="Company" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all shadow-sm outline-none" placeholder="Örn: Kadirli Turizm" />
                    <span asp-validation-for="Company" class="text-red-500 text-xs mt-1 block"></span>
                </div>

                <div>
                    <label asp-for="Price" class="block text-sm font-semibold text-gray-700 mb-2">Ücret (₺)</label>
                    <input asp-for="Price" type="number" step="0.01" min="0" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all shadow-sm outline-none" placeholder="Örn: 150" />
                    <span asp-validation-for="Price" class="text-red-500 text-xs mt-1 block"></span>
                </div>

                <div>
                    <label asp-for="DurationMinutes" class="block text-sm font-semibold text-gray-700 mb-2">Yolculuk Süresi (Dakika)</label>
                    <input asp-for="DurationMinutes" type="number" min="1" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all shadow-sm outline-none" placeholder="Örn: 90" />
                    <span asp-validation-for="DurationMinutes" class="text-red-500 text-xs mt-1 block"></span>
                </div>

                @* ---- Faz 12.5: araç tipi + kalkış noktası ---- *@
                <div>
                    <label asp-for="VehicleType" class="block text-sm font-semibold text-gray-700 mb-2">Araç Tipi</label>
                    <select asp-for="VehicleType" class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all shadow-sm outline-none">
                        <option value="@KadirliApp.Domain.Enums.TransportVehicleTypes.Bus">Otobüs</option>
                        <option value="@KadirliApp.Domain.Enums.TransportVehicleTypes.Minibus">Minibüs</option>
                    </select>
                    <p class="text-xs text-gray-500 mt-1">Mobilde otobüs ve minibüs ayrı listelenir.</p>
                </div>

                <div>
                    <label asp-for="DeparturePointId" class="block text-sm font-semibold text-gray-700 mb-2">Kalkış Noktası</label>
                    <partial name="_DeparturePointSelect" model="@(new KadirliApp.Web.Models.DeparturePointSelectViewModel {
                        Points = departurePoints,
                        Selected = Model.DeparturePointId
                    })" />
                    <p class="text-xs text-gray-500 mt-1">
                        Sözlükten seçilir (<a asp-controller="LookupsAdmin" asp-action="Index" asp-route-open="departure-points" class="text-indigo-600 hover:underline">Sözlükler</a>).
                        Koordinatı olan nokta mobilde "Yol tarifi" butonu üretir.
                    </p>
                </div>
            </div>

            <div class="mt-8 pt-6 border-t border-gray-100 flex justify-end">
                <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-3 px-8 rounded-xl shadow-md hover:shadow-lg transition-all transform hover:-translate-y-0.5">
                    <i class="fas fa-save mr-2"></i> Kaydet ve Saatlere Geç
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
