<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Faz 11.16 — Ağ izni. Flutter şablonu bunu YALNIZ debug/profile
         manifestlerine koyar; release'e bugün `firebase_messaging`in kendi
         manifestinden birleşerek giriyor. Yani uygulamanın can damarı olan izin
         bir eklentinin iç detayına bağlı: push kaldırılsa ya da eklenti manifestini
         daraltsa release build ağa hiç çıkamaz ve HER ekran "İnternet bağlantısı
         kurulamadı" der — debug'da fark edilmez, çünkü orada izin zaten var.
         Bu yüzden açıkça bildiriyoruz. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Faz 11.13 — Android 13 (API 33) ve üstünde bildirim göstermek için
         çalışma zamanı izni şart. İzin, kullanıcı giriş yaptığında isteniyor
         (açılışta değil): boşluğa sorulan izin reddediliyor. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Faz 12.23 — YEDEKLEME KAPALI, iki yönden birden.
         `allowBackup="false"` API 30 ve altını + API 31+'te bulut yedeklemesini
         kapatır; `dataExtractionRules` ise API 31+'te CİHAZDAN CİHAZA AKTARIMI
         kapatır (Android belgesi: bazı üreticilerde `allowBackup` D2D'yi
         kapatMAZ). Yalnız biri yazılsaydı koruma yarım kalır ve bunu hiçbir şey
         söylemezdi. Gerekçe + neyin feda edildiği: res/xml/data_extraction_rules.xml
         ve `mobile/lib/core/preferences/app_preferences.dart`. -->
    <application
        android:label="Kadirli"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
