# Active Context (Sistem Durumu ve Teknik Kararlar)

> Son güncelleme: 3 Ağustos 2026 — **Faz 11.15 TAMAMLANDI: Cilalama, durumlar ve erişilebilirlik. Golden test altyapısı ayakta.** Kod `mobile/test/golden/` + `core/paging/paged_list_footer.dart` + `core/network/connectivity_status.dart` + `core/widgets/state_views.dart`. **KARARLAR:** (1) 🔑 **golden kapsamı bileşen, ekran DEĞİL** — tam ekran golden her metin değişiminde kırılır ve insan "güncelle geç" alışkanlığı edinir, testin değeri sıfırlanır; kapsam 8 ortak bileşen + 7 liste kartı = **32 referans PNG**, her biri **360 dp × (1.0 ve 1.4 ölçek) × açık/koyu**; (2) 🔑 **golden'lar `@Tags(['golden'])` ile ayrıldı** — ubuntu CI `--exclude-tags golden`, **yeni `macos-latest` işi** `--tags golden` (yazı tipi rasterleştirmesi platforma bağlı); ⚠️ `--update-goldens` CI'da **bilinçli olarak yok**, üretirse hatalı düzen "yeni doğru" diye kaydedilir; (3) karşılaştırıcı **%0.5 toleranslı** ama **boyut değişimini hiç tolere etmez**; piksel karşılaştırması `flutter_test`in kendi `GoldenFileComparator.compareLists`'iyle yapılıyor → yalnız eşik için `image` paketi **eklenmedi**; (4) 🔑 **çevrimdışı sinyali bağlantı paketiyle değil GERÇEK İSTEKLERDEN** türetiliyor (`ConnectivityInterceptor`): kullanıcı için soru "Wi-Fi'a bağlı mıyım" değil **"sunucuya ulaşılıyor mu"**; 404/500 çevrimdışı **sayılmaz**; (5) düzeltmeler **çağrı yerinde değil ortak bileşenin içinde** → yeni ekran unutamaz. 🐛 **ÜÇ GERÇEK HATA:** (1) 🔴 **`EmptyView`/`ErrorView` kaydırılamıyordu → pull-to-refresh boş ve hata durumunda SESSİZCE ÖLÜYDÜ** (11.6'da iki ekranda tek tek çözülmüş, kalan **12 liste ekranında** açıkmış) — üstelik tam da kullanıcının en çok ihtiyaç duyduğu iki anda; (2) 🔴 **`OfflineBanner` 11.1'den beri yazılıydı ama uygulamada HİÇ GÖRÜNMEMİŞTİ** (`AppScaffold(offline:)` sabit `false`, hiçbir ekran değer geçmiyordu — yalnız stil kılavuzunda duruyordu); (3) **eczane ekranında liste altbilgisi HİÇ YOKTU** → 2. sayfa patlarsa liste sessizce eksik kalıyordu; ayrıca 10 ekran sayfa hatasının **sebebini** göstermiyordu (boş "Devamını yükle" düğmesi). 🐛🔑 **GOLDEN + ERİŞİLEBİLİRLİK TESTİNİN İLK GÜNDE YAKALADIĞI HATA — dar sütun taşmasının YEDİNCİ tekrarı, ilk kez EL EMEĞİYLE DEĞİL MEKANİK olarak:** `FilterChoiceChip` 1.4 ölçekte uzun etiketle **90 px taşıyordu** ve dokunma hedefi **36 dp** idi → `Flexible`+ellipsis + hap görsel olarak ince kalırken çevresi **gerçekten** dokunulabilir (Material'ın `MaterialTapTargetSize.padded` davranışı; `InkWell` `excludeFromSemantics` + dıştaki 48 dp `GestureDetector` — kozmetik semantik boyutu DEĞİL). ⭐PLANDIŞI: **`PagedListFooter`** (11 birebir kopya tekilleştirildi) · **`ScrollableStateBody`** · **`ConnectivityStatus`+`ConnectivityInterceptor`** · **rozet "pop"** (plandaki "sayaç pop" hiç yapılmamıştı; `TweenSequence` ile tek atomik geçiş, yalnız ARTIŞTA, hareket azaltılmışken hiç) · **`turkish_ui_test.dart`** (hata sözlüğü eksiksizliği **kaynağı tarayarak** — elle liste çürümez) · **`reduced_motion_test.dart`** · `dart_test.yaml` · CI macOS golden işi · README golden/erişilebilirlik bölümleri · `ARCHITECTURE.md` §8 güncellendi. **Tuzaklar:** golden'da `pumpAndSettle` **kullanılamaz** (spinner sonsuz animasyon) ama tek kare de yetmez — **açılış sayfa geçişi** bitmeden çekilen golden yarı saydam ve kaymış çıkar → `pump()` + `pump(1s)`; `LocalFileComparator` taban dizinini **dosya** URI'sinin klasöründen hesaplar (klasör URI'si verilirse golden'lar bir üst klasöre yazılır) ve `getTestUri` yol çözmez → `basedir.resolveUri`; `AppScaffold` artık `ProviderScope` istiyor (mevcut widget testi güncellendi); `waitUntil` **widget testinde kullanılamaz** (gerçek zamanı bekler, süit kilitlenir) → kareleri elle ilerlet; chip 48 dp'ye çıkınca şikayet formunda gönder butonu ekran dışına taştı (`ListView` tembel → testte sürükleme). **Doğrulama:** `flutter analyze` **0**, `flutter test` **669/669** (613 → +56), `dotnet test` **161/161** (backend'e **hiç dokunulmadı**). **Kuralı bilerek boz ölçütü uygulandı:** `ScrollableStateBody` kaldırılınca 2 test kırmızı · `AdCard` görsel boyutu 104→96 yapılınca golden **%19.67 fark** ile kırmızı · sözlüğe karşılıksız kod eklenince Türkçe testi kırmızı. **Canlı (Android Pixel_9 + çalışan API):** ortak altbilgi "Toplam 3 duyuru" · **API durdurulup pull-to-refresh → "İnternet bağlantısı yok" şeridi çıktı, okunan içerik silinmedi; API geri gelince şerit kayboldu** · **boş durumda pull-to-refresh göstergesi çalıştı** (düzeltmeden önce ölüydü). ⚠️ **iOS:** yeni kodla **derlendi ve açıldı** (iPhone 17, açık tema doğru, çökme yok) ama simülatör **sürülerek** doğrulanamadı: **`mobile/tool/ios_sim.sh` koordinat eşlemesi iPhone 17'de kayıyor** (giriş ölçeği ~0.2755, doğrusu ekran-kutusu/ekran-görüntüsü ~0.2303) → iki denemeden sonra bırakıldı, **11.16'da düzeltilmeli**. **⏭️ SIRADAKİ: Faz 11.15b — Emniyet ağı 2. tur (panel + Redis önbellek + moderasyon durum makineleri).** Devralınanlar: **`PagedListFooter`** (yeni sayfalı liste kendi altbilgisini YAZMAMALI) · **`ScrollableStateBody`** (boş/hata görünümleri kendiliğinden kaydırılabilir — çağrı yerinde sarmalamayın, iç içe kaydırma olur) · **`connectivityStatusProvider`** (`offline:` yalnız test/stil kılavuzu için) · **`test/golden/`** (yeni ortak bileşen/liste kartında oraya senaryo düşün, **uzun Türkçe metinle**) · **`accessibility_test.dart`**.
>
> Önceki: 2 Ağustos 2026 (4. oturum) — **Faz 11.13 TAMAMLANDI: Bildirimler + FCM Push. Push uçtan uca CANLI ÇALIŞIYOR.** Kod `mobile/lib/features/notifications/` + **`core/push/`**. **Bildirim merkezi:** sayfalı liste + sonsuz kaydırma, **gün gruplaması** (Bugün/Dün/tam tarih), okunmamış vurgusu (dolu ikon zemini + kalın başlık + nokta), tür rozeti (`relatedType`→Türkçe etiket+ikon), **"Okunmamışlar" filtresi** (uçtaki `?unreadOnly=`), **"Tümünü okundu yap"** (AppBar ikonu), pull-to-refresh, misafirde `SignInPrompt`. **FCM:** `firebase_core`+`firebase_messaging`, Android google-services eklentisi + `POST_NOTIFICATIONS`, izin/token/token-yenileme, ön plan + arka plan + kapalı-uygulama handler'ları. **Deep-link:** `data.relatedType`+`data.relatedId` → `notificationRouteFor` → mevcut detay rotaları. **KARARLAR:** (1) 🔑 **`PushMessaging` arayüzü + `NoopPushMessaging`** — Firebase yapılandırması depoda yok; `Firebase.initializeApp()` patlarsa uygulama **çökmüyor, push'suz açılıyor** (backend `Fcm:Provider=None` kararının istemci aynası). Gerçek sağlayıcı `main.dart`'ta override ediliyor (`sharedPreferencesProvider` deseni) → **widget testleri Firebase kanalı olmadan koşuyor**; (2) 🔑 **11.3'ün sözü tutuldu**: yalnız `deviceFcmTokenProvider` gerçeklendi, `registerAfterLogin` çağrı yeri hiç değişmedi; provider artık **önce izin, sonra token** istiyor; (3) liste `PagedFeedController` üstünde, filtre **sunucuda**; (4) ⚠️ okunan satır "yalnız okunmamışlar" görünümünde **gözünün önünde kaybolmuyor**; (5) okundu iyimser, 404'te geri alınmıyor; (6) ⚠️ sınıf adı **`AppNotification`** (Flutter'ın kendi `Notification`'ı `material.dart`'ta); (7) **uydurma rotaya gidilmez** — tanınmayan tür ya da **GUID olmayan** kimlik → gezinme iptal, okundu yine işaretlenir (yol enjeksiyonu da böyle eleniyor); (8) rozet sunucu-otoriter (mutasyondan sonra invalidate). ⭐PLANDIŞI: 🔑 **ön plan push'u için uygulama içi şerit** (uygulama açıkken FCM sistem bildirimi göstermiyor → kullanıcı habersiz kalırdı; `SnackBar` + **"Görüntüle"** aynı deep-link'i çalıştırıyor, rozet ve liste kendiliğinden tazeleniyor) · 🔑 **bildirim izni şeridi** (izin kapalıyken push düşmüyor, kullanıcı bunu bilmeli) · gün gruplaması saf mantığı · `NotificationKind` (tanınmayan tür genel zile düşer) · AppBar'da "Bildirim tercihleri" kısayolu · **35 yeni test (toplam 613)**. 🐛 **CANLI TESTİN YAKALADIĞI GERÇEK HATA:** **Android'de izin hiç sorulmamışken de `getNotificationSettings()` `denied` döner** (`notDetermined` pratikte yalnız iOS'ta) → "İzin ver" düğmesi yalnız `notDetermined`'da çizildiği için Android kullanıcısı şeridi görüyor ama **hiçbir şey yapamıyordu (çıkmaz sokak)**. Düğme artık iki durumda da var; istendiği hâlde reddedilirse metin telefon ayarlarına yönlendiriyor. 🐛 **TESTİN YAKALADIĞI TAŞMA (bu tuzağın ALTINCI tekrarı):** filtre şeridi `Row`'u 360 dp'ye **113 px taşıyordu** → "Tümünü okundu yap" AppBar ikonuna taşındı. 🐛 **iOS DERLEMESİ KIRILDI, düzeltildi:** `firebase_core` **en az iOS 15** istiyor, proje 13.0 idi → `IPHONEOS_DEPLOYMENT_TARGET` **15.0**'a çekildi (pbxproj 3 yer + `Podfile`). ⚠️ **Yayın etkisi:** iOS 13-14 cihazlar artık desteklenmiyor (bilinçli kabul). **Tuzaklar:** `flutter analyze` **`build/ios/SourcePackages/`** altındaki eklenti kopyalarını tarıyordu (firebase_messaging'in kendi mock'ları farklı mockito sürümüne göre) → **85 sahte hata**, `analysis_options.yaml`'a **`exclude: build/**`**; `find.bySemanticsLabel` semantik ağaç kapalıyken 0 döner → okunmamış noktası `ValueKey`; sabit `Future.delayed` flaky → `waitUntil`; arka plan handler'ı **üst düzey + `@pragma('vm:entry-point')`**. **DOĞRULAMA (canlı, uçtan uca, Android Pixel_9 + gerçek FCM):** panelden duyuru → **8 bildirim satırı** → Hangfire işi **FCM'e gerçekten iletti** (`fcm_sent=t`, `fcm_sent_at` dolu, `fcm_error` boş) → **ön planda uygulama içi şerit** + liste kendiliğinden tazelendi + **rozet 1 oldu** → "Görüntüle" → duyuru detayı → DB'de `is_read=true`; **ikinci duyuru uygulama ARKA PLANDAYKEN** → **gerçek sistem bildirimi** ("Kadirli • now") → dokununca **duyuru detayı açıldı** + okundu. Token kaydı `onTokenRefresh` boru hattıyla **kendiliğinden** yapıldı. `flutter analyze` **0**, `flutter test` **613/613**, `dotnet test` **161/161**. ❌ **iOS push 11.16'ya DEVREDİLDİ (açıkça not edildi, sessizce atlanmadı):** APNs `.p8` **ücretli Apple Developer üyeliği** + **gerçek iPhone** gerektiriyor (simülatörde güvenilir test edilemez). iOS'ta uygulama derleniyor ve bildirim **listesi/okundu/deep-link çalışıyor**; eksik olan yalnız cihaza push **düşmesi**. **⏭️ SIRADAKİ: Faz 11.15 — Cilalama, durumlar ve erişilebilirlik** → sonra **11.15b (yeni)** → 11.16 yayın. 🆕 **PLANA İKİ EKLEME (2 Ağu, 4. oturum sonunda; kullanıcı "sürdürülebilirlik/test sayısı" tartışması üzerine istedi — SONRAKİ oturumlarda yapılacak):** (1) **11.15 içine 🔑 GOLDEN TEST (görsel regresyon) altyapısı** — dar sütunda `Row` taşması bu projede **altı kez** tekrarladı (11.7/11.8/11.9/11.10/11.11/11.13) ve her seferinde **el emeğiyle** yakalandı; golden test bunu mekanikleştirir. Kapsam **ortak bileşenler + modül liste kartları** (tam ekran golden YOK — her metin değişiminde kırılır), matris **360×800 + 1.4 yazı ölçeği × açık/koyu** (taşmaların çıktığı tam koşullar), ⚠️ golden testte **Nunito açıkça yüklenmeli** yoksa makineler arası fark olur, CI yalnız karşılaştırır (golden üretmez). Ayrıca **erişilebilirlik iddiaları testle** (`meetsGuideline` kontrast + dokunma hedefi + etiket). (2) 🆕 **`11.15b — Emniyet ağı 2. tur`** (11.16'dan hemen önce): **panel (`KadirliApp.Web`) 20 controller neredeyse test edilmemiş — en büyük çıplak alan** · **Redis önbellek geçersizleştirme** (sessiz hata alanı: yanlış anahtar temizlenirse mobilde eski veri kalır, kimse hata almaz) · **vefat/etkinlik/kampanya/işletme moderasyon durum makineleri** · handler birim testleri · `ArchiveDeathsJob`+`PublishScheduledAnnouncementsJob`. ⚠️ **HEDEF SAYI DEĞİL:** "1100-3200 test case olmalı" türü ölçü kolayca şişirilir (`[Theory]+InlineData` her satır ayrı sayılır, döngüyle yazılan on doğrulama tek test görünür) → ölçüt 11.14'ünkiyle aynı: **kuralı bilerek boz, test kırmızıya dönüyor mu?** Sayı (161 → ~350-400) bunun sonucu. Bugünkü oran: backend ~40k satır / **161 test** (1/250), mobil ~43k satır / **613 test** (1/70) — backend hâlâ ince taraf. Devralınanlar: **`PushMessaging` arayüzü** (yeni push senaryosunda Firebase'e değil buna bak) · **`PushCoordinator.openNotification`** (liste+push tek yol) · **`notificationRouteFor`** (sunucu yeni `relatedType` üretirse buraya eklenmeli, yoksa sessizce yok sayılır — `ARCHITECTURE.md` §7/18) · ön plan şeridi (`scaffoldMessengerKey` app.dart'ta) · ⚠️ `exclude: build/**` · ⚠️ CI Android derlemesi `google-services.json` yer tutucusu yazıyor.
>
> Önceki: 2 Ağustos 2026 (3. oturum) — **Faz 11.14 TAMAMLANDI: Devir Teslim — mimari haritası + backend emniyet ağı + CI.** ⚠️ **Plan sırası dışında**: kullanıcı 11.13'ten (FCM) önce bunu istedi; 11.13 hâlâ açık. **Teslim edilenler:** (1) 🔑 **`ARCHITECTURE.md` (kök, 10 bölüm)** — sistem şeması + katman kuralı (*yanlış yön DERLENMEZ*, proje referans grafiğiyle), klasör haritası, **20 satırlık modül envanteri** (modül → `Features/` klasörü + public uçlar + panel controller + izin adı + mobil `features/` klasörü + rotalar) + Hangfire tablosu, **§4 "Modül EKLE" 18 adımlı reçete**, **§5 "Modül DEĞİŞTİR"** (additive vs kırıcı; alan silmenin üç adımlı sürüm planı), **§6 "Modül KALDIR"** (mobil önce → uç → panel → application → izin → **veri: soft-delete'li satırlar için tabloyu düşürme, `pg_dump` yedeği** → doküman), **§7 GÖRÜNMEZ SÖZLEŞMELER (15 tablo maddesi + 5 kod-dışı madde)**, **§8 test haritası** (hangi katmanda ne + bilinen tuzaklar), §9 çalıştırma/yapılandırma, §10 değişmez kurallar. (2) **Backend testleri 94 → 161 (+67)**: `AdSubmissionRulesTests` **40 birim testi** (içerik kuralları + dinamik kategori alanları + görsel sahipliği; sınıf `internal` → csproj'a **`InternalsVisibleTo`**), `InvisibleContractsTests` **12** (§7'nin her satırı), `EndpointAuthorizationSweepTests` **5** (**yapısal** — `EndpointDataSource`'tan gerçek uç tablosu; "her `/v1/admin/*` yetki ister", "anonim yazma uçları TAM OLARAK şu 9", "`/v1/users/me*` korumalı", "her admin controller ortak tabandan türer" → **yeni uç kendiliğinden kapsanır**), `ModuleVisibilitySweepTests` **5** (mevcut testler çoğunlukla DETAY ucunu deniyordu; bunlar **liste** seviyesinde: rehber/mekan pasif, etkinlik pending+rejected+soft-delete, vefat pending, kampanya süresi dolmuş). (3) 🔑 **`ArchitectureDocTests` 5 test — doküman çürüme önlemi**: `ARCHITECTURE.md` ↔ gerçek klasörler, **iki yönlü** (eksik modül + fazla modül) + admin controller sayısı + vaat edilen bölümler; **çürümeyi gerçekten yakaladığı doğrulandı** (tabloda `Transport/`→`Ulasim/` yapılınca iki test kırmızı). (4) **CI**: `.github/workflows/mobile.yml` **yeni** (Flutter 3.44.2 pin + `analyze` + `test` + debug APK); mevcut `dotnet.yml` zaten build+test koşuyordu → denetlendi, `services:`+migration adımlarının **Testcontainers yüzünden gereksiz** olduğu not düşüldü (çalışan hattı doğrulamadan bozmamak için bırakıldı). (5) **`CLAUDE.md` (kök)** — 30 saniyelik giriş + "hangi dokümanı ne zaman" tablosu + 8 değişmez kural. 🐛 **TESTİN YAKALADIĞI GERÇEK BACKEND HATASI:** `AdSubmissionRules` sayısal alanları `NumberStyles.Number` ile okuyordu; o stil **`AllowThousands` içerir** ve .NET grup boyutunu denetlemez → Türkçe **`"2020,5"` doğrulamadan GEÇİYOR ve `20205` okunuyordu** (10 kat sapma, sessiz); `"1,000"` da geçiyordu. Düzeltme **`AllowLeadingSign | AllowDecimalPoint`** (binlik ayracı yok, ondalık yalnız nokta) + açık Türkçe mesaj. Risk denetlendi: `ad_property_values`'ta **0 sayısal değer** vardı, panelin property arayüzü yok, mobil zaten virgülü noktaya çeviriyor → mevcut veri/akış etkilenmedi. **Canlı:** `"150,5"`/`"150,000"` → **400**, `"150.5"` → **201**; Android'de kullanıcı **"150,5" yazdı → DB'ye "150.5" yazıldı**. 🐛 **MOBİLDE İKİ SAATE BAĞIMLI TEST (düzeltildi):** `transport_screen_test` sabit `07:00/10:30/14:00/17:30` fixture'ı yüzünden **akşamdan sonra**, `deaths_screen_test` `timeUntilFuneral()`'a `now` verilmediği için **23:59'dan sonra** kırılıyordu; oturum 21:45'te koştuğu için ikisi de yakalandı. Ulaşım fixture'ı saatleri artık **şimdiye göre** üretiyor (gece yarısı taşmasını kolluyor), vefat testi zamanı **sabitliyor**. 📌 11.10'da "fixture TARİHİ `DateTime.now()` olmasın" öğrenilmişti; bu kez sorun **saatte**ydi — aynı sınıfın üçüncü tekrarı. ⭐PLANDIŞI: 🔑 **görünmez sözleşmelerin liste değil TESTLE kilitlenmesi** (liste yazmak korumaz) · 🔑 **yetki denetiminin elle liste yerine YAPISAL yapılması** ("anonim yazma uçları tam olarak şu 9" iddiası **ilk denemede tuttu** = bugün kaçak yok) · `InternalsVisibleTo` · çürüme testinin iki yönlü olması + "tablo okunabildi mi" kontrolü. **Tuzaklar:** test DB'si yalnız `DbSeeder` lookup verisiyle geliyor (`MockDataSeeder` yalnız `Program.cs`'te) → yeni entegrasyon testi **kendi verisini kurmalı** (marker + `IAsyncLifetime` + `ExecuteDeleteAsync`); `Assembly.GetTypes()` **derleyici üretimi durum makinelerini** de döndürüyor → `Name.EndsWith("Controller")`; dosya yükleme alanı **`cdnUrl`**; `POST /v1/complaints` **200** (201 değil); `UpdateMyAdCommand` alanları **`newImageFileIds`/`removeImageIds`**; `Business.BusinessName` (+`IsVerified`, `IsActive` yok); markdown regex'i yalnız **numaralı tablo satırlarına** bakmalı. **Doğrulama:** `dotnet test` **161/161**, `flutter analyze` **0**, `flutter test` **578/578**, canlı API + Android Pixel_9 + iPhone 17. Geçici veri (1 ilan + 4 property) ve `uploads/` artefaktları temizlendi → `ads` oturum öncesi hâlinde (5 satır). **⏭️ SIRADAKİ: Faz 11.13 — Bildirimler + FCM Push** (Firebase service-account bağlı, Android'de uçtan uca doğrulanabilir; APNs `.p8` yoksa iOS push 11.16'ya devreder). Devralınanlar: ⚠️ **yeni `Features/` ya da mobil `features/` klasörü açarsanız `ARCHITECTURE.md` tablosuna satır eklemeden `dotnet test` yeşile dönmez** · `POST /v1/notifications/fcm-token` yetki süpürmesinin **anonim listesinde DEĞİL**, orada kalmalı · push `data` alanlarının sözleşmesi §7'ye + `InvisibleContractsTests`'e yazılmalı.
>
> Önceki: 2 Ağustos 2026 (2. oturum) — **Faz 11.12 TAMAMLANDI: Ulaşım + Şikayet/İstek. Hub ızgarasındaki 12 modülün TAMAMI artık gerçek** (`kAppModules` içinde `ready=false` kalmadı). Kod `mobile/lib/features/transport/` + `features/complaints/`. **Ulaşım** (`/ulasim`): iki sekme — **Şehirlerarası** (arama + kart: hedef/firma/süre/ücret + ⭐**"Sıradaki 14:00 · 1 sa 16 dk sonra"**; kart açılınca tüm kalkış saatleri hap olarak — geçenler üstü çizili+soluk, sıradaki dolu vurgulu — + "Saatleri paylaş") ve **Şehir içi** (hat numarası rozeti + servis saatleri + ⭐**"Şu an çalışıyor · 20 dk arayla · yaklaşık 12:50"**; kart açılınca **durak zaman çizelgesi** `+7 dk`/`+21 dk`). **Şikayet/İstek**: liste `/sikayet` (durum rozeti + tür + **"Yetkili yanıtı" kartı** + sonuçlanma tarihi; misafirde Giriş daveti ama FAB açık) + form `/sikayet-bildir` (6 tür chip'i + konu + mesaj + türe göre değişen ipucu). **KARARLAR:** (1) 🔑 **ulaşımda detay ekranı/rotası YOK** — sunucuda `{id}` ucu yok, saatler ve duraklar liste gövdesinde geliyor; listeyi tarayıp id bulan sahte detay rotası derin bağlantıda ve 2. sayfada kırılırdı → **kart yerinde açılıyor**, aynı anda tek kart; (2) ⚠️ **uçlar sayfalı** (planın "sayfasız olabilir" uyarısının aksine) → `PagedFeedController`; (3) ⚠️ arama parametresi **`searchTerm`** (11.11 taksi tuzağı); (4) ⚠️ **saatler tarihsiz "duvar saati"** — şehirlerarası `"07:00"`, şehir içi `TimeSpan`→**`"06:30:00"`**; hesap **Kadirli gün içi dakikası** üzerinden, saat dilimi kaydırması yok (11.7/11.10/11.11 dersinin dördüncüsü); (5) şehir içi sıradaki kalkış **"yaklaşık"** ve ekranda öyle yazıyor, sefer listesi yoksa **saat uydurulmaz**; (6) **şikayet anonimde de gönderilebilir** (uç `[AllowAnonymous]`) ama anonim kayıt `user_id` NULL → "Bildirimlerim"de görünmez; uyarı **gönderimden ÖNCE** verilir, başarı diyaloğu da anonimde takip vaat etmez; (7) rota `protectedPrefixes`'e **yazılmadı** (misafir listeyi göremez ama gönderebilir); (8) ⚠️ **şikayet türü sunucuda serbest metin** (doğrulayıcı/sözlük ucu yok) — mobil 6 tür tanımlar (`content`/`app` mevcut veriyle uyumlu), **tanınmayan değer ham hâliyle yazılır**; (9) "Bildirimlerim"de **durum filtresi yok** (uç desteklemiyor, istemcide süzmek sayfalamayı bozardı); (10) form `/sikayet`in **kardeşi**. ⭐PLANDIŞI: 🔑 **`departure_times.dart` "sıradaki kalkış" saf mantığı** (17 test; bugünkü seferler bittiyse **yarının ilk seferine** düşer, 30 dk kala kart vurgulanır; şehir içi servis durumu + gece yarısını aşan pencere) · **30 sn'de tazelenen geri sayım** · **hat/saat paylaşımı** · **durak zaman çizelgesi** · 🔑 **ilan detayında "Bu ilanı şikayet et"** (eski metin kullanıcıyı "Ayarlar → Şikayet"e yolluyordu; form artık tür+modül+ilan kimliği+**ilan adıyla** açılıyor, ham kimlik gösterilmez) · 🔑 **panelde şikayet türü rozeti** (`Type` panelde hiç gösterilmiyordu → mobilde seçilen tür yöneticiye ulaşmıyordu; tek additive view değişikliği) · türe göre değişen mesaj ipucu · `LookupDropdown` uçsuz da kullanılabiliyor (`AsyncValue.data(kAppModules)`) · **48 yeni test (toplam 578)**. 🐛 **İKİ GERÇEK HATA:** (1) 🔴 **`AppScaffold(onRefresh:)` pull-to-refresh kısa listelerde SESSİZCE ÖLÜYDU** — canlıda yakalandı, **11.6'dan beri her liste ekranını ilgilendiriyor**: içerik ekrana sığdığında Android'in `ClampingScrollPhysics`i taşmaya izin vermiyor, jest `RefreshIndicator`a ulaşmıyordu. ⚠️ Tetikleyici dar: `ListView` **kendi `controller`ını aldığında** `primary=false` olur ve Flutter'ın otomatik `AlwaysScrollableScrollPhysics` takviyesi devreye girmez — bu projedeki **her sonsuz kaydırmalı liste** `loadMore` için controller veriyor. Düzeltme `AppScaffold`ta `ScrollConfiguration` ile; ⚠️ **ilk denemem çok genişti** (`AlwaysScrollableScrollPhysics()` tek başına platform fiziğini tümden düşürüyor → fling/sınır davranışı bozuluyor, `ads_screen` ve `home_screen` testleri kırıldı) → **`applyTo` ile mevcut fiziğin üstüne**. Regresyon testi düzeltme kaldırılınca gerçekten kırmızı oluyor. (2) **Başarı diyaloğu açıkken gönder butonu arkada dönmeye devam ediyordu** (`AppButton.loading` sonsuz animasyon) → gösterge diyalog açılmadan önce kapanıyor; **11.10 kampanya kod modalı hatasının birebir tekrarı**. **Tuzaklar:** `FilterChoiceChip.onTap` nullable değil → gövdede erken dönüş; misafir formunda başa uyarı şeridi eklenince gönder butonu ekran dışında kalıyor + `ListView` tembel → testte elle sürükleme; `adb shell input text` **boşlukta kesiyor** → `%s`; POST gövdesi testte `String` değil `Map`. **Doğrulama (canlı, Android Pixel_9 + iPhone 17 + çalışan API + admin panel):** şehirlerarası "Sıradaki 14:00 · 1 sa 16 dk sonra" (saat 12:44), açılan kartta 07:00/10:30 üstü çizili + 14:00 vurgulu · şehir içi "Şu an çalışıyor · 20 dk arayla · yaklaşık 12:50" + 4 duraklı çizelge · mobilden gönderilen şikayet **DB'de `type='complaint'` + `user_id` bağlı + `pending`**, liste kendiliğinden tazelendi ("az önce") · **panelden "Çözüldü" + yönetici notu** → mobilde pull-to-refresh ile **"Yetkili yanıtı"** kartı + "Sonuçlandırıldı: 2 Ağustos 2026, 13:00" · panelde tür rozetleri (Şikayet / İçerik şikayeti / Uygulama hatası) · ilan detayından "Bu ilanı şikayet et" → form "İçerik şikayeti" + "İlgili içerik: Sahibinden Temiz Fiat Egea" ile açıldı · iOS'ta ulaşım + şikayet açık temada doğru, geri sayım 40→39 dk tıkladı. `flutter analyze` **0**, `flutter test` **578/578**, `dotnet test` **90/90**, iOS derlemesi geçti. Geçici veri (1 şikayet) ve `uploads/` artefaktları (2 dosya) temizlendi → `complaints` oturum öncesi hâlinde (2 satır). 🆕 **PLANA YENİ ALT-FAZ EKLENDİ (2 Ağustos 2026, aynı oturum): `11.14 — Devir Teslim` (mimari haritası + backend emniyet ağı + CI)**, 11.13 ile cila fazı arasına; cila 11.15'e, yayın 11.16'ya kaydı. **Gerekçe:** proje sahibinin ailesinden gelen ve haklı olan soru — *"yarın biz modül ekler/değiştirir/kaldırırsak yapının bozulmayacağını, neyin nerede olduğunu nereden bileceğiz?"*. Yapı bu soruyu büyük ölçüde zaten cevaplıyor (20 backend modülünün tamamı `Features/X/{Commands,Queries,Dtos}`, mobil modüllerin tamamı `features/x/{application,data,presentation}`, **katman ihlali derlenmiyor** [proje referanslarıyla zorlanmış], modül kaydı tek listede + "ölü buton yok" testi, mobilde 578 test, ortak çekirdekler [11.12 pull-to-refresh hatası **tek düzeltmeyle 11 ekranı** onardı]); **eksik olan yapı değil, ona dışarıdan girişi sağlayan iki şey:** (a) kronolojik günlük değil **harita** niteliğinde `ARCHITECTURE.md` (modül envanteri + ekle/değiştir/kaldır reçeteleri + **görünmez sözleşmeler listesi**), (b) **backend iş kuralı testleri** — bugün 41k satır C#'a **90 test** (çoğu uç seviyesi entegrasyon), mobilde 43k satıra 578 test; yani bir handler değişse testler yakalamaz. Ayrıca CI yok + `CLAUDE.md` yok. Konum bilinçli: daha erken yapılırsa modüller değişmeye devam ettiği için doküman çürür, daha geç yapılırsa yayın telaşına karışır. **⏭️ SIRADAKİ: Faz 11.13 — Bildirimler tamamlama + FCM Push.** ✅ **FIREBASE SERVICE-ACCOUNT BAĞLANDI (2 Ağu, aynı gün):** kullanıcı indirdi → **`secrets/firebase-service-account.json`** (klasör `.gitignore`'da + `chmod 600`; **`secrets/README.md`** commit edilir ve nasıl edinileceğini/sızarsa ne yapılacağını anlatır), `appsettings.Development.json`'da `Fcm:Provider="Firebase"` + `ServiceAccountKeyPath="../secrets/firebase-service-account.json"` (yol `KadirliApp.Api/`'ye göre çözülür); API açılışında **`FCM push sağlayıcısı hazır`** logu doğrulandı. `mobile/.../google-services.json` + `GoogleService-Info.plist` de artık `.gitignore`'da (önce yalnız "commit etmemeye dikkat ediyorduk" — kırılgandı). 🐛 **BAĞLARKEN ÇIKAN GERÇEK HATA:** `FcmPushService` **10.11'den beri bozuktu ama hiç çalıştırılmamıştı** (`Fcm:Provider` hep `"None"` → sınıf hiç kurulmuyordu). FirebaseAdmin **.NET** SDK'sında `FirebaseApp.GetInstance(name)` uygulama yoksa `ArgumentException` **fırlatmaz, `null` döndürür** (Java SDK'sı fırlatır, kod ona göre yazılmıştı) → `catch` çalışmıyor, `Create` çağrılmıyor, `GetMessaging(null)` patlıyordu; anahtar bağlanır bağlanmaz **Hangfire işi dakikada bir hata verdi.** Düzeltme `GetInstance(...) ?? Create(...)` + kurulumun tamamına `try/catch` (bozuk anahtar da çökertmemeli). **4 yeni unit test**, düzeltme geri alınınca 2'si kırmızı → hatayı gerçekten yakalıyor; `dotnet test` **90 → 94**. 📌 **Ders:** bayrakla kapatılmış kod yolu **hiç test edilmiyor demektir**, bayrak açıldığı gün ilk kez çalışır — 11.14'te `ARCHITECTURE.md`'ye görünmez sözleşme olarak yazılacak. ⚠️ **Kalan tek Firebase eksiği: APNs .p8** (iOS push; Apple Developer üyeliği ücretli + gerçek cihaz şart → yoksa iOS push 11.16'ya devreder). ⚠️ **Eski denetim notu (artık geçersiz):** `google-services.json` + `GoogleService-Info.plist` **var** (proje `kadirliapp`, `app.kadirli`, commit edilmedi); **eksik (1) service-account JSON** (backend push gönderimi; `Fcm:Provider="None"`, ücretsiz, Firebase Console → Hizmet hesapları; yoksa `FcmPushService` no-op'a düşer, çökmez) ve **(2) APNs .p8 anahtarı** (iOS push; **Apple Developer üyeliği ücretli** + gerçek cihaz şart → yoksa iOS push 11.16'ya devreder). Bildirim merkezi + token boru hattı + deep-link **Firebase'siz de yazılabilir**, oturum bloklanmaz. Devralınanlar: **detay alt-rotaları deep-link hedefi olarak hazır**, `AppRoutes.complaintForContent` (sorgu parametreli rota üretimi), **`kAppModules` 12/12 `ready`** (deep-link eşlemesi tek listeden), `PagedFeedController`, ⚠️ Firebase config kullanıcıda hazır ama commit edilmedi, ⚠️ `deviceFcmTokenProvider` hâlâ null stub.
>
> Önceki: 2 Ağustos 2026 — **Faz 11.11 TAMAMLANDI: Vefat + Taksi + Mekanlar.** Kod `mobile/lib/features/deaths/` + `features/taxis/` + `features/places/` (+ 🔑 `core/widgets/lookup_dropdown.dart`, `features/lookups/data/models/named_lookup.dart`). **Vefat** (`/vefat` + `/:id` + `/vefat-bildir`): aramalı liste, üstte **"Bugün cenaze namazı: X, 13:30"** hatırlatması (ek istek yok, yüklenmiş listeden türer), bugünkü ilanda ince şerit + yeşil saat; detayda cenaze namazı kartı + **bugünkü cenazede geri sayım**, cami/defin/taziye satırları, yol tarifi, paylaş, **"Merhuma Allah'tan rahmet…"** dileği; **Vefat bildir** `[A]` = ad + tarih/saat seçici (Türkçe, 24 saat) + cami/mezarlık/mahalle dropdown + taziye adresi + opsiyonel foto → `pending`. **Taksi** (`/taksi` + `/:id`): ad **ve plaka** araması, **listede doğrudan "Ara"**, plaka rozeti; çağrı = `ensureSignedIn` → `POST /drivers/{id}/call` → dönen telefonla çevirici. **Mekanlar** (`/mekanlar` + `/:id`): arama + **kategori chip şeridi**, kartta kategori/uzaklık/ücret; detayda kapak, yol tarifi, adres/uzaklık/saat/giriş/mevsim, "Nasıl gidilir?", **olanaklar var/yok**. **KARARLAR:** (1) 🔑 **backend'e tek additive ekleme `GET /v1/places/categories`** (`GetPlaceCategoriesQuery`, lookup cache 15 dk) — `PlaceResponseDto` yalnız `categoryId` taşıyor, public kategori ucu yoktu, mobil ne ad yazabiliyor ne chip çizebiliyordu; 3 entegrasyon testi (**literal segment `{id}` ile çakışmıyor** dahil); (2) **vefat ekranı uygulamanın en sade ekranı** — filtre/rozet/sayaç yok, tek vurgu **bugünkü cenaze**; (3) **taslak kaydı YOK** (11.9 deseninden bilinçli sapma: vefat bildirimi acil ve tek seferlik, "taslağı sakla" yersiz); (4) ⚠️ **`funeralDate` saat dilimi KAYDIRILMAZ** (11.7 `dutyDate` / 11.10 `eventDate` dersinin üçüncüsü), saat ayrı `funeralTime` (`TimeSpan` → `"13:30:00"`); (5) **geri sayım yalnız bugünkü cenazede**; (6) **taziye konumu yoksa cami/mezarlığın lookup koordinatına düşülür**, ikisi de yoksa buton **hiç çizilmez** (gövde yalnız *ad* taşıyor); (7) 🔑 **taksi "Ara" dayanıklılığı** — uç 5xx/ağ hatası verirse çağrı kaydı tutulamaz ama kullanıcı taksiye ihtiyaç duyuyor → listedeki telefonla arama yine denenir ve **sebebi yazılır** (sessiz başarısızlık yok); (8) ⚠️ **taksi arama parametresi `searchTerm`** (diğer modüllerde `search`) — yanlış ad sessizce yok sayılırdı; (9) ⚠️ **`places.amenities` `jsonb` kolonu ama DTO'da `string`** → yanıtta **JSON içeren METİN** geliyor (`"{\"WC\":true}"`), nesne değil; **anahtarda olmayan olanak "belirtilmemiş"** demek, "yok" değil; (10) **mekan sıralaması sunucuda ada göre sabit** → sıralama chip'i yok (sayfalar arası tutarsız olurdu), `0,0` koordinatı "girilmemiş"; (11) **"Vefat bildir" / "Ara" anonimde router'la Giriş'e ATMAZ** → `ensureSignedIn` daveti, anonimde uca istek yok. ⭐PLANDIŞI: 🔑 **`LookupDropdown<T>`** (yükleniyor/hata/**boş liste** durumlu sözlük açılır listesi; 11.5'teki mahalle alanı vefat formunda üç kez tekrarlanınca ortak bileşene çıkarıldı) · **`DeathSubmissionService`** (foto yükle → kayıt yaz, ekransız test edilebilir) + `TimeOfDayValue` · **`RecentTaxiCallsStore` "Son aradıklarınız"** (cihazda, en fazla 3; ⚠️ **telefon SAKLANMAZ** — arama yine uçtan, numara taze + sayaç işler; ad/plaka anlık görüntüsü saklanır ki bölüm yüklenmiş sayfaya bağımlı olmasın) · `todaysFuneralsProvider` · **zorunlu alan boşsa formu başa kaydırma** · üç modülde paylaşım · **54 yeni test (toplam 527)** + **3 dotnet testi (toplam 90)**. 🐛 **TESTİN YAKALADIĞI GERÇEK HATA:** `LookupDropdown` **ve `AppTextField`in etiket `Row`'u** uzun etiketle (`"Cenaze namazının kılınacağı cami"`) `RenderFlex` taşırıyordu → `Flexible`+ellipsis; **aynı sınıf hatanın beşinci tekrarı** (11.7/11.8/11.9/11.10) ama ilk kez **ortak bileşende**, yani tüm formları ilgilendiriyordu. 🐛 **11.10 TESTLERİNDE GİZLİ, YALNIZ GECE PATLAYAN HATA (bu oturumda ortaya çıktı, düzeltildi):** `event_card_test`/`events_screen_test` fixture'ları `eventDate`'i **`DateTime.now().toUtc()`** ile üretiyordu; alan "Türkiye günü, 00:00 UTC" konvansiyonunda ve model onu kaydırmıyor → saat **00:00-03:00 arasında** UTC günü bir geri kalıyor, **5 test yalnız geceleri** kırmızı oluyordu (oturum 01:00'de başladığı için görüldü). Fixture'lar `AppDate.nowInTurkey`e çevrildi; "detay açılır" testi de ekran dışı satırı arıyordu (gün adı uzayınca kart büyüyor) → iddia kaydırmaya bağlandı. **Tuzaklar:** `scrollUntilVisible` kabuk dalları ayakta olduğu için "Too many elements" veriyor → `dragUntilVisible(..., find.byType(ListView).last, ...)`; **5xx `apiRetry` için geçici hata** → "liste gelmezse" testlerinde **kalıcı** hata (404) kullanılmalı, yoksa "pending timer"; kategori şeridi 360 dp'ye sığmıyor → testte şeridi elle kaydır; `AppMoney.plain(12.5)` "12,50" verir (mesafede tek hane doğal → ayrı biçimleyici). **Doğrulama (canlı, Android Pixel_9 + iPhone 17 + çalışan API, geçici veriyle):** vefat listesinde bugünkü ilan şeritli + "Bugün, 13:30", **pending ilan hiçbir yerde görünmedi** ("Toplam 2 ilan"), detayda "Namaza 9 sa 3 dk var." · **mobilden vefat bildirimi gönderildi → DB'de `pending`, `funeral_date` 2026-08-03 (gün kaymadı), `funeral_time` 13:00, cami bağlı** · taksi "Ara" → **`taxi_calls` satırı + `total_calls` 0→1**, çevirici `+90 533 123 00 01` ile açıldı, dönünce **"Son aradıklarınız" belirdi ve uygulama yeniden açılınca korundu** · mekanlarda kategori filtresi 3→2, "Merkeze 12,5 km", olanaklar **Otopark ✓ / WC ✓ / Wi-Fi ✗** (jsonb metin doğru çözümlendi) · iOS'ta üç modül de açık temada doğru. `flutter analyze` **0**, `flutter test` **527/527**, `dotnet test` **90/90**, iOS derlemesi geçti. Geçici veri (4 vefat ilanı + 1 taksi çağrısı + sayaçlar + `Ala Cami` amenities/saat) ve `uploads/` artefaktları temizlendi → tablolar oturum öncesi hâlinde (2 arşivli vefat / 0 taxi_calls / 0 total_calls). **⏭️ SIRADAKİ: Faz 11.12 — Ulaşım + Şikayet/İstek.** Devralınanlar: `LookupDropdown`, form deseni (`_PickerField` + istemci denetimi + başarı diyaloğu + `addPostFrameCallback` ile kapatma), `PagedFeedController`, `ensureSignedIn`, `AppDate.clockLabel` (11.2'den, henüz hiç kullanılmadı — ulaşım saatleri için yazılmıştı).
> Önceki: 1 Ağustos 2026 (2. oturum) — **Faz 11.10 TAMAMLANDI: Etkinlikler + Kampanyalar.** Kod `mobile/lib/features/events/` + `features/campaigns/` (+ 🔑 `core/widgets/month_calendar.dart`, `core/widgets/filter_chip.dart`). **Etkinlikler** (`/etkinlikler` + `/:id`): iki sekme — **Liste** (arama + Yaklaşan/Geçmiş/**Ücretsiz** şeridi + kategori chip'leri + sonsuz kaydırma) ve **Takvim** (aylık ızgara → güne dokununca o günün saat sıralı listesi); kart **takvim yaprağı** + saat/mekan + "Bugün/Yarın/N gün sonra" + Ücretsiz/fiyat; detayda tarih-saat kartı + geri sayım, mekan + **Yol tarifi**, bilet, açıklama, düzenleyen, paylaş. **Kampanyalar** (`/kampanyalar` + `/:id`): aramalı liste (işletme adı + başlık), indirim/kod/**"Son gün!"** rozetleri; detayda **"İndirim kodunu göster"** → `view-code` → **kod modalı** (büyük kod + **kopyala** + ilk görüntüleme anı + koşullar); kodsuz kampanyada buton yerine açıklayıcı şerit. **KARARLAR:** (1) 🔑 **backend'e tek küçük additive ekleme: `GET /v1/events?sort=date_asc`** (varsayılan `date_desc` korundu, bilinmeyen değer varsayılana düşer) — uç `EventDate`'i **azalan** sıralıyordu, "yaklaşan" listesinde ilk sayfa **en uzak tarihli** etkinlikleri getiriyordu ve bu istemcide çözülemezdi (sıralama sayfalar arası); 4 entegrasyon testi eklendi; (2) liste varsayılanı **Yaklaşan**, ayrım **gün bazında** (`startDate=bugün`/`endDate=dün`) → bugün akşamki etkinlik sabah hâlâ "yaklaşan"; (3) ⚠️ **`eventDate` saat dilimi KAYDIRILMAZ** (11.7 `dutyDate` dersi; saat ayrı `eventTime` alanı); (4) kampanyada **kategori/durum filtresi yok** (uç `?categoryId=` **işletme** kategorisine bakıyor ama public lookup ucu yok; public uç zaten yalnız yürürlükteki kampanyaları döndürüyor → süresi dolan detay **404**); (5) ⚠️ **`discountCode` liste gövdesinde geliyor ama gösterilmiyor** — kod `view-code` ile açılır (sayaç = "kaç FARKLI kullanıcı gördü", ikinci istek aynı kaydı döner → modal "ilk görüntüleme" anını yazar); (6) **kod paylaşım metnine konmaz**; (7) "Kodu göster" anonimde router'la Giriş'e ATMAZ → `ensureSignedIn` daveti, anonimde uca istek yok; (8) aciliyet/geri sayım rozetleri **yalnız bir hafta içinde**, süre **Kadirli gününe** göre. ⭐PLANDIŞI: 🔑 **`MonthCalendar` + `MonthSwitcher` ortak takvime çıkarıldı** (`DutyCalendar` artık ince sarmalayıcı, eczane testleri hiç değişmedi) · 🔑 **`FilterChoiceChip` ortak filtre chip'i** (ilan `_FilterChip` + rehber `_CategoryChip` kopyaları silindi; rehberin acil vurgusu `accent` ile korundu; ikon **opsiyonel**) · **"Ücretsiz" filtresi** (`?isFree=`) · **kod modalında panoya kopyala** (`Clipboard`, yeni paket yok) · paylaş (etkinlik+kampanya) · **51 yeni test (toplam 473)** · `waitUntil` zaman aşımı 5→15 sn. 🐛 **ÜÇ GERÇEK HATA:** (1) 🔴 **`PagedFeedController` atılmış provider'ın `ref`'ine dokunuyordu** — `build()`'deki `Future.microtask(loadFirstPage)` ve `await` sonrası `state` yazımı *"Cannot use the Ref after it has been disposed"* üretiyordu; **altı sayfalı listeyi birden** ilgilendiriyordu, süit büyüyünce flaky test olarak ortaya çıktı → mikro-görev ve her `await` sonrası **`ref.mounted`** kontrolü; (2) **kod modalı açıkken buton arkada dönmeye devam ediyordu** (`await showModalBottomSheet` boyunca `loading:true`) → gösterge modal açılmadan önce kapanıyor; (3) **kampanya rozeti 1.4 yazı ölçeğinde 49 px taştı** (`Wrap` çocuğuna sınırlı genişlik verir) → `Flexible`+`ellipsis`; **aynı sınıf hatanın dördüncü tekrarı** (11.7/11.8/11.9). **Tuzaklar:** üç filtre chip'i 360 dp'ye sığmıyordu ("Ücretsiz" ekran dışında kalıyordu) → o şeritte `dense` + **ikonsuz** chip; canlıda **"% %25 indirim"** okunuyordu → ikon `local_offer`; widget testinde `initializeDateFormatting('tr_TR')` şart; `AppMoney.plain(12.5)` → "12,50". **Doğrulama (canlı, Android Pixel_9 + iPhone 17 + çalışan API, geçici veriyle):** yaklaşan liste **en yakın tarih başta** (1/3/10/26 Ağu), "Ücretsiz" 150 ₺'lik etkinliği eledi (4→3), "Geçmiş" iki geçmiş etkinliği soluk tarihle azalan sırada verdi, **pending etkinlik hiçbir yerde görünmedi**; takvimde 1/3/10/26 işaretli + bugün çerçeveli, 10'a dokununca "10 Ağustos Pazartesi · 20:30" → detay; kampanyada "Son 1 gün" rozeti, kod modalı açıldı ve **DB'de `code_view_count` 0→1 + `campaign_code_views` satırı**, **ikinci açış sayacı artırmadı**, kod panoya kopyalandı, kodsuz kampanyada buton yerine şerit; iOS'ta açık temada liste+takvim+kampanya doğru. `flutter analyze` **0**, `flutter test` **473/473** (4 kez üst üste), `dotnet test` **87/87**. Geçici veri (5 etkinlik + 2 kampanya + 1 kod görüntüleme) temizlendi → tablolar oturum öncesi hâlinde (3 etkinlik / 2 kampanya / 0 kod görüntüleme). **⏭️ SIRADAKİ: Faz 11.11 — Vefat + Taksi + Mekanlar.** Devralınanlar: `ContactActions` (taksi+mekan), `FilterChoiceChip`, `MonthCalendar`, `ref.mounted` korumalı `PagedFeedController`, `AdDraftStore` deseni (vefat bildirimi), `AdSubmissionService` (vefat fotoğrafı), `ensureSignedIn`, detay alt-rota deseni.
> Önceki: 1 Ağustos 2026 — **Faz 11.9 TAMAMLANDI: İlanlar Bölüm 2 — ilan verme + İlanlarım + Favorilerim. Mobil artık İÇERİK ÜRETİYOR** (11.6-11.8 yalnız okuyordu). Kod `mobile/lib/features/ads/` (+ `application/ad_submission_service.dart`, `data/ad_draft_store.dart`, `presentation/{ad_form_screen,my_ads_screen,favorites_screen}.dart`, `presentation/widgets/{ad_property_field,ad_image_picker,my_ad_card}.dart`, `core/utils/app_money.dart → plain`). **İlan ver** (`/ilan-ver`, kabuğun DIŞINDA tam ekran): **üç adımlı sihirbaz** Kategori → Bilgiler → Fotoğraflar & gönder; kategori kök→alt drill-in + "X (genel)"; **kategoriye özel dinamik alanlar** sunucudan gelen tanımdan üretiliyor (Text/Number/Select/MultiSelect/Boolean); çoklu görsel + **kapak seçimi**; özet kartı + moderasyon bilgisi. **İlanlarım** (`/profil/ilanlarim`, Profil sekmesinin İÇİNDE): statü şeridi, **red gerekçesi kırmızı kartta**, **performans sayaçları**, süre uyarısı, Düzenle/Sil/Uzat. **Favorilerim** (`/profil/favorilerim`): `isAvailable=false` soluk + metinli rozet, favoriden çıkarma **geri alınabilir**. **KARARLAR:** (1) 🔑 **üç adımlı sihirbaz** — kategoriye özel alanlar ancak kategori bilindikten sonra çekilebiliyor, tek sayfada form ortadan büyüyüp yazılanları kaydırırdı; düzenlemede kategori kilitli → 1. adım hiç çizilmez; (2) ⚠️ **düzenleme rotası `/ilan-ver`in ALT ROTASI DEĞİL, kardeşi (`/ilan-duzenle/:id`)** — go_router iç içe rotada **üst ekranı da kurar** (11.7 tuzağı) → arkada "yeni ilan" formu açılıp kategori isteği atıyor ve **taslak diyaloğunu düzenleme ekranının üstüne fırlatıyordu**; test "pending timer" ile yakaladı; (3) **ilan verme kabuğun dışında** (odaklanmış görev), **me-scoped listeler sekmenin içinde** (geri profile döner); (4) **"İlan ver" anonimde router'la Giriş'e ATMAZ** → `ensureSignedIn` daveti (rota yine `protectedPrefixes`'te); (5) 🔑 **kapak = ilk fotoğraf** (sunucu da böyle yapıyor, tek kural); (6) ⚠️ **düzenlemede kapak/sıra değişimi "yeniden bağlama" ile** — `UpdateMyAdCommand` yalnız ekle/sil biliyor ve yeni görselleri sona+`isCover=false` yazıyor, kapağı ancak "hiç kapak kalmadıysa" en düşük sıradakine veriyor → sıra değiştiyse mevcut görseller **fileId'leriyle** silinip yeni sırada ekleniyor; **sıra değişmediyse bu yola girilmez**; (7) **zorunlu alan denetimi istemcide de** (`AdSubmissionRules` aynası), sunucu yine reddederse mesaj ilgili alanın altına; (8) **boolean başlangıçta seçili değil** ("Hayır" ≠ "cevaplanmadı"), **seçeneksiz select hiç çizilmez**; (9) **uzatma yalnız approved/expired + hak varken açık**, kalan hak butonda (`Uzat (2)`); (10) **sil/uzat listeyi yerinde günceller**; (11) **yayında olmayan favorinin detayı açılmaz** (public uç 404 verirdi), listeden de düşürülmez; (12) ⚠️ **düzenleme ekranı `GET /v1/ads/{id}` çağırdığı için `view_count` 1 artar** — kabul edildi, görsel id'leri + property değerleri yalnız detayda var. ⭐PLANDIŞI: **taslak kaydı** (`AdDraftStore`; çıkışta "Taslağı sakla/Vazgeç/Devam et", açılışta **sorularak** geri yükleme, **görseller taslağa yazılmaz**, 7 günden eski taslak teklif edilmez) · **ilan performans kartı** (`MyAdDto`'nun zaten döndürdüğü 4 sayaç) · **süre uyarısı + uzat CTA** · **Profil satırında ilan sayısı rozeti** (ek istek yok, denetleyiciden türer) · **`AdSubmissionService`** (yükleme + sıra/kapak kararları ekransız, test edilebilir; **yükleme ilerlemesi**) · **`FavoriteAdsController.setFavorite(value:)`** · `AppMoney.plain` · `AdStatus` enum · **68 yeni test (toplam 432)**. 🐛 **ÜÇ GERÇEK HATA:** (1) **favori listesinde "çıkar" POST atıyordu** — ekran `toggle` çağırıyordu, `toggle` favori kimlik kümesine bakıyor, küme `ref.read` ile o an kurulduğundan boştu → yön ters; **durumu bilen ekran yönü kendi söylemeli** (`setFavorite`); (2) **`_FavoriteTile` meta satırı `RenderFlex` taşması** (dar sütun + `Wrap` sınırlı genişlik) → `Flexible`+`ellipsis`; ⚠️ **aynı sınıf hata 11.7 `PharmacyTile` ve 11.8 `AdCard`'da da çıkmıştı** — dar sütunda `Row` içindeki çıplak `Text` bu projenin tekrar eden tuzağı; (3) **zorunlu özellik boşken form en üste kaydırıyordu**, hata aşağıda kalıyordu → yukarı kaydırma yalnız üstteki alanlarda hata varsa. **Tuzaklar:** form uzun + `ListView` tembel → widget testinde yüzey `tester.view.physicalSize` ile yükseltildi; `MultipartFile.fromFile` gerçek dosya ister → servis testinde `Directory.systemTemp`; `find.text` **`EditableText`'i de eşler** (ipucu = değer ise iki eşleşme) → controller doğrudan okunmalı; sabit ipucu her sayısal alana uymuyor ("Kilometre: Örn. 2018") → sayısal property'de ipucu yok. **Doğrulama (canlı, Android Pixel_9 + iPhone 17 + çalışan API + admin panel):** Otomobil kategorisinde **3 fotoğraflı, 5 özellikli** ilan verildi → DB'de `pending` + `ad_images` 3 satır (kapak = kullanıcının seçtiği) + `ad_property_values` 5 satır, telefon `+905321110002` E.164; **panelde "Bekliyor"** göründü; düzenleme fiyatı 525.000→495.000 yaptı, kapağı taşıdı, bir fotoğrafı sildi → DB'de 2 görsel + yeni kapak + `pending`; **panelden reddedildi** → mobilde "Yayınlanmama gerekçesi" kırmızı kartta; uzatma → DB'de `+30 gün` / `extension_count=1`, buton `Uzat (2)`; silme → soft-delete + listeden düştü; favori ekle/çıkar/**geri al** DB'de doğrulandı; taslak sakla→geri yükle çalıştı; iOS'ta form/İlanlarım/boş durum açık temada doğru. `flutter analyze` **0**, `flutter test` **432/432**, `dotnet test` **83/83**. Geçici veri temizlendi (ilan + 3 görsel + 5 property + 2 favori + 1 uzatma + `uploads/`'taki 3 dosya silindi, uzatılan ilanın süresi geri alındı → tablolar oturum öncesi hâlinde). Backend'e **hiç dokunulmadı**. **⏭️ SIRADAKİ: Faz 11.10 — Etkinlikler + Kampanyalar.** Devralınanlar: `PagedFeedController`, `MonthSwitcher`/`DutyCalendar` (etkinlik takvimi), `AdDraftStore` deseni (11.11 vefat bildirimi), `AdSubmissionService` (görsel yükleme), `AdStatus` deseni (metinli durum rozeti), `ContactActions`.
> Önceki: 31 Temmuz 2026 (6. oturum) — **Faz 11.8 TAMAMLANDI: İlanlar Bölüm 1 (liste + detay). Uygulamanın en büyük modülü açıldı.** Kod `mobile/lib/features/ads/` (+ `core/utils/app_money.dart`, `core/utils/debouncer.dart`). **Liste** (`/ilanlar` — sekmenin kendisi): gecikmeli arama + **iki katmanlı kategori şeridi** + 4 sıralama chip'i + **fiyat aralığı** + sonsuz kaydırma + pull-to-refresh + "Toplam N ilan"; kart = kapak görseli/başlık/fiyat/göreli tarih/görüntülenme/**favori kalbi**. **Detay** (`/ilanlar/:id`): galeri (nokta göstergesi + "2/3" + **tam ekran görüntüleyici**), kategori/başlık/fiyat/tarih/görüntülenme, açıklama, **kategoriye özel özellikler tablosu**, satıcı kartı, yayın süresi + güvenlik uyarısı, üstte favori + paylaş, **altta sabit Ara / WhatsApp çubuğu**. **KARARLAR:** (1) **kartta mahalle yok** (plandan bilinçli sapma — `AdResponseDto` mahalle taşımıyor, `Ad` varlığında alan **hiç yok**; boş satır yerine tarih + görüntülenme); (2) 🔑 **kategori tek şerit, drill-in** — ⚠️ **sunucu filtresi TAM EŞLEŞME**, `?categoryId=<Araçlar>` "Otomobil"deki ilanları döndürmez, bu yüzden kök seçilince şerit içine iniyor (`[Tümü][Araçlar][Otomobil]…`), hangi chip vurguluysa filtre odur; `subCategoryCount=0` olan kökte ikinci istek atılmaz; (3) **favori durumu ayrı küme** — `AdDetailDto`'da `isFavorited` YOK, favori uçları yalnız "değişiklik oldu mu" döner → `GET /v1/users/me/favorites` bir kez okunup bellekte tutulur (`FavoriteAdsController`), liste+detay aynı kümeye bakar, kalp **iyimser** dolar, hata olursa geri alınır, **anonimde uca istek gitmez**; (4) **detay sekmenin İÇİNDE** (`/ilanlar/:id` = dalın alt rotası → alt sekme çubuğu kalır, geri liste konumunu korur; diğer modüllerin detayı kabuğun dışında); (5) **`view_count` yanıtı artıştan ÖNCEKİ değer** (her detay çağrısı sunucuda sayacı artırıyor) → +1 eklenmedi, provider gereksiz invalidate edilmiyor; (6) **sayaç uçları beklenmez, hatası yutulur** (arama gecikmesin); (7) **ters fiyat aralığı sessizce takas edilir**; (8) kategoriler patlarsa şerit hiç çizilmez (11.6/11.7 kararı). ⭐PLANDIŞI: **fiyat aralığı filtresi** (uç + panel destekliyordu, mobilde eksikti) · **`AppMoney`** (kuruş yalnız varsa; **fiyatsız ilan "0 ₺" DEĞİL "Fiyat belirtilmemiş"**; `parse` Türkçe binlik noktasını anlar: yalnız nokta + her gruptan sonra tam 3 hane → binlik) · **`Debouncer`** (arama sunucuda başlık+açıklamada koşuyor; canlıda **7 harf → 1 istek**) · **tam ekran galeri + `InteractiveViewer` yakınlaştırma** (yeni paket yok) · WhatsApp'a hazır mesaj · paylaş · **hub ızgarası düzeltmesi** (modül rotası bir **sekme** ise `push` değil `go` — yoksa kabuk üstüne kabuk yığılıyordu) · **61 yeni test (toplam 304)**. 🐛 **CANLI TESTİN YAKALADIĞI ÜÇ GERÇEK HATA:** (1) **"Filtreleri temizle" arama kutusunu temizlemiyordu** (filtre sıfırlanıyor, kutuda eski metin kalıyor) → `ref.listen` ile senkron; ⚠️ **aynı hata 11.7 Rehber ekranında da vardı**, o da düzeltildi, ikisi de regresyon testinde; (2) **tam ekran görüntüleyici tam ekran değildi** — dal Navigator'ına push edildiği için alt sekme çubuğu altta kalıyordu → `Navigator.of(context, rootNavigator: true)`; (3) **görüntüleyicide fotoğraflar arası geçilemiyordu** — `InteractiveViewer` her yatay sürüklemeyi yutuyor → `panEnabled` yalnız yakınlaştırılmışken (+ `NeverScrollableScrollPhysics`), ayrıca AppBar başlığı siyah zeminde koyu çıkıyordu (`foregroundColor` `titleTextStyle`'ı ezmiyor). **Tuzaklar:** yatay `ListView` **tembel** → ekran dışı chip hiç kurulmuyor (erişilebilirlik + test) → şeritler `SingleChildScrollView + Row`; **`pumpAndSettle` `Timer`'ı ilerletmez** → debounce testinde süre elle pump; **`AppNetworkImage` shimmer'ı sonsuz animasyon** → görselli ekranda `pumpAndSettle` kilitleniyor, sabit kare pump; `find.bySemanticsLabel` semantik ağaç kapalıyken 0 döner → ikon finder. **Doğrulama (canlı, Android Pixel_9 + iPhone 17 simülatörü + çalışan API, 25 geçici ilan + 3 görselli ilan):** liste/sıralama/arama/kategori drill-in (Araçlar→Otomobil) · fiyat aralığı (ters girilen 300.000–100.100.000 takas edildi) · sonsuz kaydırma 2. sayfa + "Toplam 29 ilan" · detayda DB'de **`view_count` 41→42** · favori → **`ad_favorites` satırı oluştu** + kalp listede de doldu · **Ara → çevirici açıldı + `phone_click_count` 0→1** · **WhatsApp → wa.me tarayıcıda açıldı + `whatsapp_click_count` 0→1** · boş durum + "Filtreleri temizle" · iOS'ta açık temada liste/detay/tam ekran galeri (kaydırma 1/3→2/3). `flutter analyze` **0**, `flutter test` **304/304**, `dotnet test` **83/83**. 🧪 **AYNI OTURUMDA TEST GÖZDEN GEÇİRMESİ (304 → 364, +60):** beş boşluk kapatıldı — (1) **`core/paging/paged_feed_test.dart`**: çekirdeğin **yarış durumu** (`_requestId`) hiç test edilmiyordu, oysa dört modül buna dayanıyor (geç dönen eski filtre/tazeleme/`loadMore` yanıtı listeyi ezmemeli); (2) **`core/utils/app_links_test.dart`**: dış uygulama URL'leri hiç doğrulanmıyordu — **hatalı numara normalleştirmesi yanlış kişinin WhatsApp sohbetini açar**; `url_launcher` platform kanalı sahtelenip URL yakalanıyor; (3) **`core/widgets/contact_actions_test.dart`**: "veri yoksa buton çizilmez" kuralının kendisi (11.11 buna dayanacak); (4) **`features/ads/ad_card_test.dart`**: uzun başlık / 7 haneli fiyat / **1.4 yazı ölçeği** / dar ekran; (5) **`ads_feed_controller_test.dart`** + yazı ölçeği sınırı + favori kümesinin çıkışta sıfırlanması. 🐛 **Gözden geçirmenin yakaladığı gerçek hata:** `AdCard` sağ sütunu **sabit `SizedBox(height:104)`** idi → 1.4 ölçekte **15 px dikey**, tarih+görüntülenme tek `Row`'da olduğu için dar ekranda **8.6 px yatay** `RenderFlex` taşması; 104 artık yalnız alt sınır (`ConstrainedBox(minHeight:)`), meta satırı **`Wrap`**. Canlıda uzun başlıklı ilanla doğrulandı. ⚠️ **Öğrenilen test kuralı:** provider testlerinde **sabit `Future.delayed` flaky** (tek dosyada yetiyor, tüm süit paralel koşarken yetmiyor) → `test/helpers/pump_app.dart`'a **`waitUntil(condition)`** eklendi; negatif iddialar sınırlı bekleme kullanır. Tam süit 3 kez üst üste koşturuldu. Geçici veri (25 ilan + 1 görselli ilan + 3 dosya + favori) ve `uploads/` görselleri temizlendi → tablolar oturum öncesi hâlinde (5 ilan, tüm sayaçlar 0, 0 görsel/favori/dosya). Backend'e **hiç dokunulmadı**. **⏭️ SIRADAKİ: Faz 11.9 — İlanlar Bölüm 2 (ilan verme + benim ilanlarım + favorilerim).** Devralınanlar: `AdsRepository`, kategori provider'ları, **`FavoriteAd` modeli tam** (Favorilerim ekranı doğrudan kullanacak), `AppMoney.parse`, `features/files/` çoklu yükleme, `AdGallery`, `ensureSignedIn` (⚠️ "İlan ver" ekranı `AppRoutes.protectedPrefixes`'e yazılmalı).
> Önceki: 31 Temmuz 2026 (5. oturum) — **Faz 11.7 TAMAMLANDI: Nöbetçi Eczane + Şehir Rehberi.** Kod `mobile/lib/features/pharmacies/` + `features/guide/` (+ 🔑 `core/paging/paged_feed.dart`, `core/widgets/contact_actions.dart`). **Eczane** (`/eczaneler` + `/:id`): iki sekme — Nöbetçi (bugünün nöbetçisi öne çıkan kart + **aylık nöbet takvimi ızgarası**: ay ileri-geri, nöbetli gün işaretli, bugün çerçeveli, güne dokununca o günün nöbetçisi) ve Eczaneler (aramalı sayfalı liste); detayda eczacı/adres/telefon/saatler + **"bu ayki nöbet günleri"** + paylaş. **Rehber** (`/rehber` + `/:id`): tek ekran — arama + kategori chip'leri (**birlikte** uygulanır) + sonsuz kaydırma + kartta **tek dokunuşla arama düğmesi**; detayda tüm iletişim satırları + `ContactActions` + paylaş. **KARARLAR:** (1) 🔑 **ortak sayfalama çekirdeği çıkarıldı** — 11.6'nın `AnnouncementFeedState` deseni `PagedFeedState<T,F>` + `PagedFeedController<T,F>` oldu (sayfalama/yarış/mükerrer eleme/filtre/2. sayfa hatası tek yerde); duyurular taşındı ve **22 testin hiçbiri değişmedi**, eczane + rehber listesi bedavaya geldi, **11.8 ilanlar da bunu kullanacak**; (2) **rehber tek ekran** (plandan bilinçli sapma: 6 kategori için ayrı ekran fazladan dokunuş; kategori+arama birlikte çalışır) ve **listede doğrudan arama düğmesi** (rehberin işi "numarayı bul, ara"; telefonsuz kayıtta düğme yok); (3) **takvim ızgara, liste değil** (bakarak cevaplanan soru), **nöbetsiz gün dokunulabilir değil**, hafta Pazartesi'den başlar; (4) ⚠️ **`dutyDate` saat dilimi KAYDIRILMAZ** — sunucu zaten "Türkiye günü, 00:00 UTC" yazıyor (`TurkeyClock`), `dayKey` ham UTC'den üretilir; (5) **nöbet saatleri metin** (`"19:00"`), gece yarısını aşan nöbet tespit edilip **"ertesi gün"** notu yazılır (01:00'de eczane arayan için kritik); (6) **detaydaki "bu ayki nöbet günleri" EK UÇ İSTEMEZ** (takvimin aylık listesi süzülüyor, aynı önbellek; takvim patlarsa bölüm sessizce gizlenir); (7) kategori listesi patlarsa filtre şeridi hiç çizilmez (11.6 kararının aynısı). ⭐PLANDIŞI: **`ContactActions` + `InfoRow`** (Ara/Yol tarifi/Web/E-posta tek bileşende; veri yoksa buton çizilmez, dış uygulama açılamazsa şerit mesajı; koordinat yoksa **adres metniyle** harita araması — **11.11 taksi+mekanlar doğrudan kullanacak**) · `DutyCalendar`+`MonthSwitcher` (11.10 etkinlik takvimine hazır zemin) · `PharmacyTile`/`GuideItemTile` · kategori slug'ı → Material ikon (yeni paket yok; sunucunun icon/color alanları seed'de boş) · Acil Numaralar chip'i vurgulu · **33 yeni test (toplam 244)**. 🐛 **TESTİN YAKALADIĞI GERÇEK HATA:** `PharmacyTile` çalışma saati satırı `Expanded` değildi → uzun metinle (**"Nöbet ertesi gün 09:00'a kadar sürer"**) `RenderFlex overflowed by 222 pixels`; canlıda kısa metinle fark edilmezdi. **Tuzaklar:** varsayılan widget-test yüzeyi **800x600** → takvim "off-screen" sayılıp `tap` reddediliyordu, çözüm `tester.view.physicalSize` ile gerçek telefon yüzeyi (kaydırma jimnastiği bitti); yatay chip şeridi tembel → testte dokunulacak kategori `displayOrder` ile başa alınmalı; **iç içe rota (`/eczaneler/:id`) ALT ekranı da kurar** → liste istekleri arkada başlar, `pumpAndSettle` sonrası bir kare daha gerekir (yoksa "pending timer"). **Doğrulama (canlı, Android Pixel_9 + çalışan API, geçici nöbet verisiyle):** bugünün nöbetçisi doğru ("Şifa Eczanesi · 19:00 - 09:00") ve hub şeridine düştü · takvimde 3/25/28/31 işaretli, 31 (bugün) çerçeveli, 25'e dokununca "25 Temmuz Cumartesi → Merkez Eczanesi + *Nöbet ertesi gün 09:00'a kadar sürer*" · eczane detayında "Bu ayki nöbet günleri" 3 Tem + 25 Tem · rehberde 6 kategori + kayıtlar, "hastane" araması 1 kayıt, detayda Ara/Yol tarifi. `flutter analyze` **0**, `flutter test` **244/244**, `dotnet test` **83/83**, **iOS simülatör derlemesi geçti**. Geçici nöbet kayıtları (`source='gecici-test'`) ve `uploads/` artefaktları temizlendi; backend'e **hiç dokunulmadı**. 🔧 **DÜZELTME — iOS simülatörü ARTIK SÜRÜLEBİLİYOR** (aynı oturumda kullanıcının itirazı üzerine teşhis edildi, commit `8b949bd`, araç `mobile/tool/ios_sim.sh`): önceki oturumlarda "erişilebilirlik izni yok" diye not düşülmüştü — **yanlış teşhis, izin zaten vardı.** Gerçek iki sebep: (1) **Simulator penceresi odakta değilken AX ağacından kayboluyor** (`-1719 Geçersiz dizin`); gerçek izin hatası **`-1743`**, daha önce görülen `-25204` ise `kAXErrorCannotComplete` — ikisi de izinle ilgisiz → her komuttan önce `activate` + kısa bekleme; (2) **koordinat eşlemesinde pencere kutusu KULLANILAMAZ** (başlık çubuğu + kenar boşluğu içeriyor) → doğru referans `first UI element of window 1 whose role is "AXGroup"` (cihaz ekranı), screenshot piksel boyutuyla oranlanır. Komutlar: `check` / `shot` / `tap <x> <y>` (**screenshot koordinatı birebir**) / `text` / `key return|escape|delete`. iPhone 17'de 11.7 ekranlarıyla doğrulandı: sekme değiştirme, arama kutusuna "savrun" yazma → liste süzüldü, detay, geri butonu. **Kaydırma da eklendi** (`swipe`): System Events sürükleme üretemediği için **`cliclick`** kuruldu (brew, ~160 KB tek binary; `pyobjc/Quartz` ~30-40 MB + venv gerektirdiğinden seçilmedi) — ara adımlarla sürüklüyor, tek sıçrayışlı sürükleme bazı listelerde hiç hareket ettirmiyor; canlı doğrulandı. ⚠️ Kalan tek sınır: etikete göre dokunma (`entire contents` boş dönüyor). Belge `mobile/README.md` "Canlı doğrulama" (Android `adb` komutları da orada). **⏭️ SIRADAKİ: Faz 11.8 — İlanlar Bölüm 1 (liste + detay).** Devralınanlar: `PagedFeedController` (liste+filtre+sonsuz kaydırma hazır), `ContactActions`, `AppShare`, detay alt-rota deseni, `ready` bayrağıyla modül kaydı.
> Önceki: 31 Temmuz 2026 (4. oturum) — **Faz 11.6 TAMAMLANDI: Duyurular + Elektrik Kesintileri. Mobilde ilk GERÇEK içerik modülleri açıldı** (11.1-11.5 altyapı/hesap işleriydi). Kod `mobile/lib/features/announcements/` + `features/power_outages/` (+ `core/utils/app_share.dart`). **Duyurular** (`/duyurular`): sayfalı liste + **sonsuz kaydırma** + tür filtresi chip'leri (`/types`) + pull-to-refresh + liste sonunda "Toplam N duyuru"; detay (`/duyurular/:id`): yüklendiğinde `POST /{id}/view`, dış bağlantıda `POST /{id}/click` sonra tarayıcı, öncelik şeridi/tür/tarih/gövde/konum kartı/kaynak/paylaş. **Kesintiler** (`/kesintiler` + `/:id`): Güncel/Geçmiş sekmesi (sayı rozetli) · **Şu an sürüyor / Planlanan** başlıkları · **30 sn'de bir tazelenen geri sayım** ("Bitmesine 1 sa 18 dk" / "4 sa 58 dk sonra başlıyor") · **"Sadece \<mahallem\>" filtresi** · detayda başlangıç/bitiş/süre/sebep + paylaş. **KARARLAR:** (1) **sayfalı liste `AsyncValue` değil kendi durumu** (`AnnouncementFeedState`) — sonsuz kaydırmada üç yükleme hâli var ve **ikinci sayfa hatası okunan içeriği silmemeli** (liste kalır, altta "Devamını yükle"); yarış durumu `_requestId` ile, sayfalar arası mükerrer kayıt id'yle eleniyor; (2) **tür filtresi aynı denetleyicide** (liste sıfırlayan işlem), aynı türe tekrar dokunmak filtreyi kaldırır; (3) 🔑 **hub kendi kesinti isteğini ATMIYOR** — `relevantOutagesProvider` artık `allPowerOutagesProvider`'dan türüyor (uç zaten tek istekte tüm kayıtları döndürüyor) → hub'dan modüle girmek ikinci ağ isteği doğurmuyor, `refreshHome` kaynağı tazeliyor; (4) **kesinti gruplaması/filtresi tamamen istemcide** (`PowerOutageGroups.from`; uç sayfalamıyor+tarih filtrelemiyor, 10.x'te bilinçli donduruldu); (5) ⭐ **mahalle filtresi ŞEHİR GENELİNİ elemez** (mahallesi boş kayıt herkesi ilgilendirir) ve gizlenen sayı ekranda yazılır → kullanıcı listeyi boş görüp "veri yok" sanmaz; eşleşme **ad üzerinden** (uç mahalle id'si döndürmüyor), harf/boşluk duyarsız; (6) **tür renginin sınırlı kullanımı** — sunucunun hex'i yalnız seçili OLMAYAN chip'in ikon tonunda, seçili chip tamamen tema birincil rengi (yönetici açık renk seçse de okunabilirlik+koyu tema bozulmaz); FontAwesome sınıfı Material ikonuna eşleniyor, bilinmeyen ad nötr ikona düşüyor (yeni paket YOK); (7) **görüntülenme yalnız başarılı yüklemeden sonra** sayılır (silinmiş duyuru istatistiği kirletmesin), tıklama sayacı bağlantıyı bekletmez; (8) **tür listesi patlarsa filtre şeridi hiç çizilmez** ("işlevsiz buton yok"). ⭐PLANDIŞI: **`share_plus` + `core/utils/app_share.dart`** (Kadirli'de bilgi WhatsApp gruplarından yayılıyor → duyuru/kesinti tek dokunuşla paylaşılır; iPad popover çapası `originOf`; **11.8+ ilan/etkinlik/vefat paylaşımı bunu kullanacak**) · **`AppDate.range` + `AppDate.duration`** (⚠️ gün ayrımı **Kadirli saatine** göre — UTC'de farklı günde olan iki an aynı güne düşebiliyor, teste bağlandı; süre **yukarı yuvarlanır**, "0 dakika kaldı" yazan sayaç bozuk görünür) · **detay rotaları ALT ROTA** (`/duyurular/:id`, `/kesintiler/:id`) + `AppRoutes.announcementDetail/powerOutageDetail` üreteçleri → **11.13 push deep-link hedefi hazır** · hub vitrini artık doğrudan detaya gidiyor · `PowerOutageTile`/`AnnouncementTypeFilter` · liste ekranlarında boş/hata durumları da **kaydırılabilir** (yoksa pull-to-refresh ölür) · **51 yeni test (toplam 211)**. **Doğrulama (canlı, Android Pixel_9 + çalışan API, geçici veriyle):** 27 duyuru iki sayfada sonsuz kaydırmayla yüklendi ("Toplam 27 duyuru") · tür filtresi `?typeId=` gönderdi · detay açılınca DB'de **`view_count` 0→1**, "Bağlantıyı aç" ile **`click_count` 0→1** · kesintide Güncel (3)/Geçmiş (2), süren kırmızı geri sayımlı · mahalle filtresi açılınca yalnız şehir geneli kaldı + "4 kesinti mahalle filtresi yüzünden gizli." · paylaşım sayfası Kadirli saatiyle doğru metni verdi. `flutter analyze` **0**, `flutter test` **211/211**, `dotnet test` **83/83**, **iOS simülatör derlemesi geçti** (yeni `share_plus` pod'u sorunsuz, hub iOS'ta göründü; ⚠️ simülatörde programatik dokunma hâlâ yok → etkileşim doğrulaması Android'de). **Tuzaklar:** `app_modules_test`/`home_screen_test` "yakında" ekranını doğruluyordu → modül `ready=true` olunca kırıldı (**iyi işaret**: kayıt↔ekran eşleşmesi gerçekten denetleniyor; test artık `ready` bayrağına göre iki ayrı iddia kuruyor); kart rozeti bölüm başlığıyla birebir aynıydı → "Şu an sürüyor" rozeti "Sürüyor"a kısaldı. Geçici veri (24 duyuru + 3 kesinti + announcement_views satırı) ve curl denemelerimin bıraktığı sayaçlar temizlendi → tablolar oturum öncesi hâlinde (3 duyuru / 2 kesinti / sayaçlar 0). Backend'e **hiç dokunulmadı**. **⏭️ SIRADAKİ: Faz 11.7 — Nöbetçi Eczane + Şehir Rehberi.** Devralınanlar: `AppShare`, `AppDate.range/duration`, sonsuz kaydırma deseni (`AnnouncementFeedState`), `ready` bayrağıyla modül kaydı, detay alt-rota deseni.
> Önceki: 31 Temmuz 2026 (3. oturum) — **DENETİM DÜZELTME OTURUMU 2/2 BİTTİ → DENETİM LİSTESİ TAMAMEN KAPANDI.** (commit'ler `a5d296c` + `017fe5d`; 1/2 aynı gün `c598b9c`). **(1) 🔴 Panelde çifte hata/başarı balonu — CANLIDA DOĞRULANDI:** ASP.NET Core'da **TempData key'leri HARF DUYARSIZ** (`TempDataDictionary` `OrdinalIgnoreCase`); `_Layout` `TempData["ok"]/["error"]`, 24 view ayrıca `["Success"]/["Error"]` basıyordu → aynı kayıt **iki kez** ekrana geliyordu. Kanıt: geçersiz şikayet statüsü POST'undan sonra "Geçersiz durum." metni 2 kez basılıyordu, düzeltmeden sonra 1. Balonların **TEK yeri `_Layout`** (ikonlu, `is string` desenli); 24 view'dan **43 yerel blok** kaldırıldı (brace eşleştirmeli silme; hiçbir view TempData'yı başka amaçla kullanmıyordu — tarandı); controller key'leri standartlaştı (`SuccessMessage`→`Success` ×3, `ErrorMessage`→`Error` ×3, `ok`→`Success` ×1; kalan yalnız **Error 90 / Success 65**); 155 atamanın tamamı string üretiyor → `is string` mesajı yutmuyor. **(2) Kalan sayfalamalar:** **`UsersAdmin`** — denetimin KAÇIRDIĞI madde; `Index` filtresiz `.ToListAsync()` ile TÜM users tablosunu çekiyordu (en hızlı büyüyecek tablo) ve sayfalama yoktu; **`GetUsersQuery` arama+sayfalamayı zaten sağlıyordu** (Admin API kullanıyor), panel ondan sapmıştı (Faz 9.4 inline-drift) → artık query kullanılıyor, `UserListItemDto`'ya panelin gösterdiği `BanReason`/`BannedAt`/`ProfilePhotoUrl` eklendi (admin-only, additive); ⭐ **arama kutusu ÖLÜYDÜ** (input'un `name`'i, formun `action`'ı yoktu → "Ara" hiçbir şey yapmıyordu) artık `?search=`'e bağlı. **`AnnouncementsAdmin`** 10.8'den kalma `Limit=200` tek-sayfa okumasından sayfalı okumaya döndü. **`PowerOutagesAdmin`** sayfalama **BİLİNÇLİ olarak bellekte** — `GetPowerOutagesQuery` tarih filtresiz TÜM kayıtları döner ve **mobil (Faz 11.4) süren/planlı ayrımını istemcide bu tam listeye bakarak yapar**; sorguyu `PagedResult`'a çevirmek public kontratı KIRARDI, panelin sorunu 1000 satırı ekrana basmaktı. **(3) `AdsAdmin` filtre çubuğu:** sorgu kategori/min-max fiyat/arama/sıralamayı baştan destekliyordu, panelde girecek alan yoktu → form eklendi (+`LoadCategoriesAsync` + `ViewBag.Query` ile değerler forma geri yazılır, koşullu "Temizle"); **`Status` filtresi bilinçli EKLENMEDİ** (`QueryAdDto`'da yok, kontrat donduruldu). ⚠️ **RAZOR TUZAĞI: `<option selected="@(...)">` ifadesi KESİN bool olmalı** — kategori listesi `dynamic` üzerinden gezilirse Razor koşullu öznitelik silmesini yapamaz, `selected="True"` basar ve **TÜM seçenekler işaretli** görünür; somut `AdCategoryAdminDto` + yerel `bool` değişken. **Doğrulama (canlı, admin oturumu, geçici veriyle):** çift balon 2→1 ve başarı balonu 1; Users/Announcements/PowerOutages sayfa1=20 sayfa2=5 ortak 0; `UsersAdmin?search=` filtreliyor ve sayfa 2 linki search'ü koruyor; users toplamı 33 (35 satır − 2 soft-silinmiş → global filtre doğru); 25 panel sayfası 200; Ads filtreleri tek tek doğru (search/minPrice/maxPrice/price_asc/price_desc/categoryId), `selected` içeren option TAM 1, "Temizle" yalnız filtre varken; **filtre+sayfalama birlikte** sayfa 2 linki hem `categoryId` hem `sort` koruyor. `dotnet test` **83/83**. Geçici veri (25 kullanıcı + 25 kesinti + 25 duyuru + 25 ilan + ban) ve `uploads/` artefaktları temizlendi, tablolar orijinal sayılara döndü (ads 5 / users 10 / announcements 3 / power_outages 2). **⏭️ SIRADAKİ: Faz 11.6 — Duyurular + Elektrik Kesintileri.** Mobil kodunda bu iki oturumda HİÇBİR değişiklik yapılmadı. **Denetimden ERTELENENLER (B):** Docker'da API/Web servisi + `uploads` volume (⚠️ **Dockerfile bile yok** → 11.15 yayın), SMS/FCM gerçek sağlayıcı bağlama (dış hesap gerekir → 11.13/11.15). **REDDEDİLEN (C):** "19+ modül için unit test yok" — gerekçe aşağıda (1/2 kaydında).
> Önceki: 31 Temmuz 2026 (2. oturum) — **DENETİM DÜZELTME OTURUMU 1/2 (Faz 11.6'ya ARA VERİLDİ, kod yazımı Faz 11'de değil backend/panelde).** Kullanıcı projeyi Gemini CLI'ye denetletti; bulgular tek tek kodda doğrulanıp **A (yap) / B (ertele) / C (reddet)** diye ayrıldı, kota yarım olduğu için 2 oturuma bölündü. **BU OTURUMDA YAPILAN (commit `c598b9c`):** (1) 🔴 **Panel sayfalama boşluğu — denetimin bulduğundan GENİŞ:** 8 Index view'i `PagedResult`'a bağlıyken sayfalama UI'ı **hiç yoktu** (Complaints/Pharmacies/Transport/Campaigns/Guide/Taxi/Places/Events) → **20. kayıttan sonrasına panelden erişilemiyordu**; yeni ortak **`Views/Shared/_Pagination.cshtml` + `Models/PaginationViewModel`** eklendi, bağlantılar **mevcut query string'i KORUYARAK** yalnız `page`'i değiştirir (tüm Index'ler sayfayı ya `[FromQuery] int page` ya DTO'nun `Page` alanıyla alır; query string bağlama harf duyarsız → tek ad yeter); mevcut 4 blok da partial'a taşındı — **AdsAdmin `href="?page=N"` hardcoded'dı** (filtre 2. sayfada kaybolurdu), **DeathsAdmin yalnız `Search` koruyordu** (Date/Status kaybolurdu), Businesses/Staff doğruydu (tutarlılık için). `From()` null + `PageSize=0`'a karşı korumalı (`TotalPages` sıfıra bölerdi), numaralar ≤7 pencerelenir. ⚠️ **RAZOR TUZAĞI: döngü değişkeni `page` OLAMAZ** — satır içi `@page` sayfa direktifi sanılır, dosya Razor Page'e çevrilir ve `Context`/`Model` kaybolur (6 derleme hatası bundandı). (2) 🔴 **Hangfire dashboard korumasızdı (10.14/(2) KAPANDI):** yeni `HangfireDashboardAuthorizationFilter` — admin/super_admin rolü geçer; değilse **Basic auth** (`Hangfire:Dashboard:Username/Password`, sabit süreli karşılaştırma); **kimlik bilgisi yapılandırılmamışsa yalnız gerçekten yerel istek** (geliştirme davranışı korundu). ⚠️ Hangfire'ın "yalnız yerel" varsayılanı **reverse-proxy arkasında çöker** (her istek localhost'tan gelir) — prod'da anahtarlar DOLDURULMALI. (3) **Test borcu:** `UnitTest1.cs` (boş şablon) silindi; `VerifyOtpCommandHandlerTests` yarımdı (assertion yerine yorum satırı) → **8 teste tamamlandı**; plandışı **`Unit/TestAsyncQueryable.cs`** (mock'lanan `IRepository.Query()` üzerinde EF async LINQ uzantıları çalışsın diye bellek-içi async sağlayıcı — ⚠️ `IgnoreQueryFilters()` burada **no-op**, soft-delete filtresi Moq `SetupSequence` ile modellenir) + `HangfireDashboardAuthorizationFilterTests` **12 test**. **`dotnet test` 65 → 83/83.** **Canlı doğrulama:** panelde 25 geçici şikayetle `?status=pending` 26 kayıt → sayfa 1'de 20 + "Sonraki" **filtreyi koruyor**, sayfa 2'de 6 kayıt, iki sayfada ortak kayıt yok; dokunulan 12 Index 200; hangfire kimlik bilgisi yapılandırılınca **yerel istek dahi 401**+`WWW-Authenticate`, yanlış parola 401, doğru kimlik 200. Geçici veri + `uploads/` artefaktları temizlendi (şikayetler orijinal 1 pending + 1 in_progress hâline döndü). **⏭️ DENETİM OTURUMU 2/2 (kota sıfırlanınca):** (a) **TempData çift mesaj** — `_Layout` `TempData["ok"]/["error"]` basıyor, **25 view** ayrıca `TempData["Success"]/["Error"]` basıyor; controller'larda 5 farklı key (`Error`×87, `Success`×61, `SuccessMessage`×3, `ErrorMessage`×3, `ok`×1) → key standardizasyonu + mesaj YALNIZ `_Layout`'ta (⚠️ **uygulamadan önce canlı panelde çift balonun gerçekten çıktığı doğrulanacak** — iddia "TempData key'leri harf duyarsız"a dayanıyor, bu oturumda doğrulanmadı); (b) **UsersAdmin/PowerOutages/Announcements sayfalaması** — ⚠️ denetim `UsersAdmin`'i **kaçırmış**: `Index` tüm users tablosunu filtresiz `ToListAsync()` ile çekiyor (en hızlı büyüyecek tablo; PowerOutages'tan ciddi), Announcements `Limit=200`; bunlar yeni query parametresi ister; (c) opsiyonel AdsAdmin filtre UI'ı (backend'de kategori/min-max fiyat/arama/sort var, panelde kutucuk yok). **ERTELENDİ (B):** Docker'da API/Web servisi + `uploads` volume (⚠️ **Dockerfile bile yok** → 11.15 yayın), SMS/FCM gerçek sağlayıcı bağlama (dış hesap gerekir → 11.13/11.15). **REDDEDİLDİ (C):** "19+ modül için unit test yok" — handler'ların çoğu ince CRUD; mock'layıp unit test yazmak mock'un doğruluğunu test eder, davranışı değil. Projenin **entegrasyon-ağırlıklı** stratejisi (gerçek DB + gerçek pipeline) bu mimaride daha güçlü güvence; 3-4 oturum maliyetine karşılık yeni bug bulma ihtimali düşük. **Faz 11.6 (Duyurular + Elektrik Kesintileri) hâlâ sıradaki geliştirme fazı; mobil kodunda bu oturumda HİÇBİR değişiklik yapılmadı.**
> Önceki: 31 Temmuz 2026 — **Faz 11.5 TAMAMLANDI: Ayarlar/Kontrol + Profil. Kullanıcı artık kendi hesabını yönetiyor.** Kod `mobile/lib/features/profile/` (+ `features/files/`, `features/settings/`, `features/auth/data/models/notification_preferences.dart`, `core/widgets/user_avatar.dart`, `core/config/app_info.dart`). **Ayarlar (kontrol merkezi):** Hesap özeti + "Profili düzenle" · **6 bildirim anahtarı** · tema · Hakkında (**sürüm `package_info_plus`**) + Şikayet kısayolu · Hesap işlemleri (Çıkış + **Hesabı sil**) · Geliştirici. Misafirde bildirim/hesap bölümleri hiç çizilmiyor. **Profil düzenleme** (`/profil/duzenle`, `PATCH /v1/users/me`): kullanıcı adı/mahalle/yaş/fotoğraf; **yalnız değişen alan gönderilir**, değişiklik yoksa istek atılmaz; fotoğraf `image_picker` → `POST /v1/files/upload` → id → PATCH (**yükleme kaydederken yapılır** → vazgeçilirse yetim dosya yok). **KARARLAR:** (1) **30 gün kısıtı istemcide önden gösterilir** — `CurrentUser.canChangeUsername/DaysLeft` (sabit 30, sunucu sabitiyle aynı; kalan süre yukarı yuvarlanır), alan kilitlenir + "X gün sonra…"; sunucu yine de reddederse `USERNAME_CHANGE_LIMIT`/`NEIGHBORHOOD_CHANGE_LIMIT` **koduna** göre mesaj ilgili alanın altına düşer; (2) **bildirim tercihlerinin tek kaynağı oturum kullanıcısı** — controller kendi kopyasını tutmaz, `AuthController.applyProfile` ile `CurrentUser`'ı günceller; anahtar **iyimser** değişir, hata olursa geri alınır + sebep gösterilir, yanıttaki tüm tercihler senkronlanır; (3) **PATCH yanıtı = güncel profil** → ek `GET /v1/users/me` yok; (4) **hesap silme diyalog değil ayrı ekran** (mağaza şartı: bulunabilir + ne olacağı yazılı), yönetici hesabında buton baştan kapalı, silmeden sonra `logout` ucu çağrılmaz (hesap pasif; refresh token gövdede iptal edilir). ⭐PLANDIŞI: **`UserAvatar`/`UserIdentityRow`** (baş harfte Türkçe `i`→`İ`) · **`features/files/` ortak yükleme repository'si** (11.9 ilan görselleri + 11.11 vefat fotoğrafı bunu kullanacak) · **`package_info_plus` + `core/config/app_info.dart`** · `SectionHeader.subtitle` · **`AppRoutes.protectedPrefixes` ilk kez dolu** (`/profil/duzenle`, `/ayarlar/hesabi-sil`; ⚠️ `startsWith` → `/profil` sekmesi takılmaz) · **40 yeni test (toplam 160)**. 🐛 **CANLI TESTTE YAKALANAN GERÇEK HATA:** `context.push` ile açılan ekran **router redirect'inin ÜSTÜNDE kalıyor** → (1) kaydettikten sonra ekran kapanmıyordu, (2) **hesap silinince sonsuz spinner** (kullanıcı kilitleniyordu), (3) ⚠️ **11.3'ten beri var olan hata: kayıtlı kullanıcı giriş yapınca boşalmış kod ekranında sıkışıyordu** (yeni kullanıcıda fark edilmemiş). Desen: durum değişiminden **bir kare sonra** `addPostFrameCallback` içinde `context.pop()`; oturum kapandıysa `context.go(AppRoutes.home)` (karar yine router'da). Üçü de regresyon testine bağlandı — eski testler "ekran üstte kaldı mı" diye sormadığı için göremiyordu. Diğer tuzaklar: `DropdownButtonFormField.helperText` varsayılan 1 satır (kesiliyordu); mahalle adına " Mahallesi" eklemek yanlış ("Yenimahalle Mahallesi") → ad + 📍 ikon; `MapEntry` yapısal eşitlik taşımaz. **Doğrulama (canlı, Pixel_9 + çalışan API):** mahalle değişti→DB'de `neighborhood_last_changed_at` doldu→alan anında kilitlendi; bildirim anahtarı DB'ye yazıldı; **uçak modunda** anahtar geri alındı + çevrimdışı şeridi; profil fotoğrafı yüklendi (`/uploads/…`) ve kaldırıldı; **hesap silme uçtan uca** (uygulamadan yeni hesap açıldı → silindi → DB'de `del…` anonimleştirme + `is_active=false` → Giriş ekranı → **aynı numarayla yeniden kayıt olundu**). `flutter analyze` 0, `flutter test` **160/160**, `dotnet test` **65/65**, **iOS simülatör derlemesi geçti** (yeni pod sorunsuz). Test kalıntıları temizlendi (yüklenen dosya + files satırı silindi, aysedmr'ın mahallesi/tercihleri geri alındı). **⏭️ SIRADAKİ: Faz 11.6 — Duyurular + Elektrik Kesintileri.**
> Önceki: 30 Temmuz 2026 (2. oturum) — **Faz 11.4 TAMAMLANDI: uygulama kabuğu + Ana Sayfa (Hub). Mobil artık GEZİLEBİLİR bir uygulama.** Kod `mobile/lib/core/router/app_shell.dart` + `core/navigation/app_modules.dart` + `features/home/` + `features/{settings,profile,notifications,announcements,pharmacies,power_outages,ads,common}/`. **Kabuk:** `StatefulShellRoute.indexedStack` 4 sekme (Ana Sayfa / İlanlar / Bildirim[rozet] / Profil), her dalın kendi Navigator'ı → durum+kaydırma korunur, aynı sekmeye tekrar dokunmak kökene döner; **modül ekranları kabuğun DIŞINDA** (hub→modül "içeri girmek", geri hub'a döner). **Hub:** saate göre selamlama (Günaydın/İyi günler/İyi akşamlar/İyi geceler + ad, Kadirli saati) + sağ üst ⚙️→Ayarlar · **acil şerit** "ŞU AN KADİRLİ" (nöbetçi eczane `on-duty` + kesinti `power-outages`; iki satır **bağımsız** yükleniyor/boş/dolu/hata, dokununca modüle gider, hatada satır tekrar dener) · **12 modüllük ızgara** · son 3 duyuru + "Tümü" · pull-to-refresh **4 kaynağı** (3 hub isteği + rozet) birden tazeler. **KARARLAR:** (1) **sekmeler `protectedPrefixes`'e YAZILMADI** (plandan bilinçli sapma) — misafiri router'la giriş ekranına atmak kabuğu kapatıyor ve "misafir gezinme birinci sınıf" kararıyla çelişiyordu; Bildirim/Profil sekmesi **içinde** `SignInPrompt` daveti gösteriyor, mekanizma 11.9 "ilan ver" gibi sekme dışı korumalı ekranlar için duruyor; (2) **modül kaydı tek liste** `kAppModules` (id/etiket/ikon/rota/**hangi faz**/özet/**bağlanacağı uçlar**) → ızgara+rotalar+"yakında" ekranları buradan üretilir, "işlevsiz buton yok" şartı **test edilebilir** oldu; (3) **hub üç ayrı provider** (tek "hub isteği" değil — biri patlarsa diğerleri görünür), `autoDispose` yok → sekme geçişi yeniden istek atmaz; (4) **kesinti filtresi istemcide** (`GET /v1/power-outages` tarih filtresiz TÜM kayıtları döner → `PowerOutage.isActive/isUpcoming/isRelevant`, süren=danger/planlı=vurgulu/yok=sakin); (5) **rozet anonimde istek atmaz** (uç `[A]`), `unreadCount` sayfalı gövdenin İÇİNDEN okunur (10.10 kontrat özelliği), `limit=1`. ⭐PLANDIŞI: **`ModulePlaceholderScreen`** (12 modülün gerçek "yakında" ekranı + debug'da uç listesi → ölü buton yok, yaşayan yapılacaklar panosu) · **`SettingsScreen` iskeleti** `/ayarlar` (⚙️ çalışsın diye; Ana Sayfa'nın geçici Hesap kartı + tema + dev kısayolları buraya taşındı, 11.5 üstüne inşa edecek) · **`ProfileScreen` iskeleti** · **`apiRetry`** (⚠️ **Riverpod 3 hatalı provider'ları sınırsız otomatik yeniden deniyor** → 404/401/400'de boşuna istek + testlerde sönmeyen timer; artık yalnız bağlantı/timeout/5xx/429'da ≤2 tekrar, 429'da `Retry-After`) · **`AppNetworkImage`** (URL mutlaklaştırma + skeleton + nötr fallback, 11.6+ standardı) · **`AnnouncementTile`** (öncelik şeridi + **metinli** Acil/Önemli etiketi — renk tek başına yetmez) · **`SignInPrompt`** · `homeStubs()` test yardımcısı · **32 yeni test**. 🐛 **DÜZELTİLEN GERÇEK HATA:** `SkeletonCardList` başka bir kaydırılabilir alanın içinde "unbounded height" ile patlıyordu (hub'ın duyuru vitrini **yükleniyor** durumuna girdiği an ekran çökerdi) → `shrinkWrap`+`padding` parametreleri. Diğer tuzaklar: `ListView` tembel kurulumu yüzünden vitrin provider'ı hiç tetiklenmiyordu (HomeScreen artık onu da izliyor); Riverpod 3'te `valueOrNull` yok → `.value`; canlı kontrolde Kampanya ikonu İlanlar'la aynı görünüyordu → bilet ikonu. **Doğrulama (canlı, Android Pixel_9 + çalışan API):** şeritte gerçek nöbetçi eczane + süren kesinti kırmızı; nöbet/kesinti yokken sakin metin; 12 kart kendi ekranını açıyor; sekme geçişi yeniden istek atmıyor; giriş sonrası selamlama kişiselleşiyor (`aysedmr`); DB'ye eklenen 2 okunmamış bildirim rozette turuncu göründü; misafir Bildirim/Profil sekmesinde davet gördü; ⚙️→Ayarlar→Koyu tema anında değişti. `flutter analyze` 0, `flutter test` **120/120**. 🍎 **iOS İLK KEZ DENENDİ** (kullanıcı Xcode+simülatör kurdu; `xcode-select` etkin; CocoaPods'u **`brew install cocoapods`** ile ben kurdum — sudo gerekmedi): `pod install` geçti, Xcode derlemesi + iPhone 17 simülatörü sonucu Progress.md 11.4 iOS notunda. **⏭️ SIRADAKİ: Faz 11.5 — Ayarlar/Kontrol + Profil (`users/me`).** Devralınanlar: `SettingsScreen`/`ProfileScreen` iskeletleri (bölümler yerinde, içerik doldurulacak), `AppNetworkImage` (profil fotoğrafı), `apiRetry`, `homeStubs()`, `kAppModules`.
> Önceki: 30 Temmuz 2026 — **Faz 11.3 TAMAMLANDI: kimlik doğrulama akışı uçtan uca çalışıyor. Mobil artık GERÇEK oturum açıyor.** Kod `mobile/lib/features/auth/` (+ `features/lookups/`, `features/notifications/data/fcm_token_service.dart`). **Akış:** Splash (`AuthController.bootstrap`) → token yoksa Giriş / varsa `GET /v1/users/me` ile doğrulanıp Ana Sayfa; Giriş (ulusal 10 hane + sabit `+90` ön ek + `532 111 00 01` maskesi) → `POST /v1/auth/login` → Kod ekranı (6 hane, **6. hanede otomatik doğrulama**, `retryAfter` geri sayımı, dev modda kod otomatik dolu) → `verify-otp` → kayıtlıysa token'lar `SecureTokenStore`'a + `users/me` → Ana Sayfa; yeniyse `tempToken` **yalnız bellekte** + Kayıt ekranı (kullanıcı adı + mahalle dropdown `GET /v1/neighborhoods` + opsiyonel yaş) → `register` → oturum. **`AuthState` sealed class** (unknown/anonymous/registering/authenticated) — freezed YOK (JSON'a çevrilmeyen durum makinesi için `sealed` yeterli; freezed yalnız kontrat modellerinde). **Yönlendirme tek yerde:** ekranlar `context.go` çağırmaz, `routerProvider._redirect` karar verir; `sessionExpiredProvider` (11.2) buraya bağlandı → yenileme reddedilince anonime düşer + "Oturumunuzun süresi doldu" bilgisi Giriş'te görünür. **KARARLAR:** (1) **misafir gezinme birinci sınıf** — Giriş kapı değil davet; "misafir devam" demeyen anonim kullanıcı Ana Sayfa'da tutulmaz (ilk açılış + çıkış sonrası aynı kural; çıkışta misafir tercihi sıfırlanır → plandaki "çıkış → login" kriteri); (2) **telefon tek yerde normalize** (`core/utils/phone.dart` — sunucu OTP'yi ham telefon string'iyle anahtarlıyor → her zaman E.164; 10 hane doluyken yeni hane yok sayılır); (3) **profil önbelleği** (`shared_preferences: auth.cachedUser`) → çevrimdışı açılışta oturum düşmez, selamlama ilk karede dolu (401/403'te silinir, bağlantı hatasında dokunulmaz); (4) `CurrentUser` **kısmi** model (11.5 genişletecek). ⭐PLANDIŞI: **`AppTextField`** (⚠️ Material `prefix` yalnız odaklıyken görünür → ülke kodu `prefixIcon` yuvasına taşındı) + **`InfoBanner`** + **`ensureSignedIn`/`showLoginRequiredSheet`** (korumalı aksiyonların tek kapısı, 11.4+ deseni) + **`features/lookups/`** + **`test/helpers/pump_app.dart`** (gerçek router+interceptor zinciri; ⚠️ splash spinner'ı yüzünden `pumpAndSettle` kilitlenir → `settleApp()`) + Ana Sayfa geçici "Hesap" kartı + **FCM stub** (`deviceFcmTokenProvider` bugün null → uca istek yok; 11.13'te yalnız bu provider override edilecek). 🐛 **BACKEND'E TEK BİLİNÇLİ DOKUNUŞ (yalnız loglama):** hatalı OTP istemciye doğru `400 INVALID_OTP` dönerken Serilog/Seq'e `500 ERR` düşüyordu — `Program.cs`'te `ExceptionMiddleware` Serilog'dan dışta kayıtlıydı; iki satır yer değişti (Serilog en dışta) → artık `400 INF`, kontrat/gövde değişmedi. 📌 **`API_CONTRACT.md` §4 DÜZELTİLDİ:** login yanıtı `{message, expiresIn, retryAfter, otp}` (doküman yanlış olarak `expiresInSeconds/retryAfterSeconds/devOtp` diyordu). **Doğrulama (canlı, Android emülatörü + çalışan API):** yeni kullanıcı kaydı (kullanıcı kendi eliyle: `+905555555555`/`atahanblcr`, DB'de doğrulandı) · kayıtlı kullanıcı `aysedmr` direkt giriş · force-stop sonrası oturum korundu · çıkış → Giriş + DB'de `fcm_token` NULL · hatalı kod mesajı · misafir devam · "giriş gerekli" daveti. `flutter analyze` 0, `flutter test` **88/88** (41 yeni), `dotnet test` **65/65**. **⏭️ SIRADAKİ: Faz 11.4 — alt sekme kabuğu + Ana Sayfa (Hub).** Devralınanlar: `currentUserProvider`, `ensureSignedIn`, `AppRoutes.protectedPrefixes` (sekme yolları yazılınca redirect korur), `AppTextField`/`InfoBanner`, `features/lookups/` deseni.
> Önceki: 29 Temmuz 2026 (2. oturum) — **Faz 11.2 TAMAMLANDI: mobil ağ katmanı + kontrat modelleri. Mobil artık GERÇEK API'den veri okuyor.** Kod `mobile/lib/core/network/` + `core/utils/`. **Yapı:** `DioClient.create` İKİ Dio üretir — ana istemci `[AuthInterceptor, EnvelopeInterceptor, (dev log)]`, yardımcı istemci `[EnvelopeInterceptor]` (yenileme + 401 sonrası yeniden gönderim orada koşar → döngü yok); sıralama kritik (auth ham 401'i önce görmeli). **`EnvelopeInterceptor`:** `{success,data,meta}` açar, `data`'yı döndürür, `meta`'yı `response.extra`'ya koyar; `success:false` HTTP 200 olsa bile `ApiException` (⚠️ **announcements 200-quirk'i burada normalleşir**); zarfsız `/health` dokunulmadan geçer. **`ApiException` tek hata tipi** (`code/message/traceId/statusCode/retryAfter` + `isNotFound`/`isConflict`/`isConnectionProblem`…), 429'da `Retry-After` okunur; `api_error_codes.dart` 14 sunucu + 4 istemci kodu (`NETWORK_ERROR`/`TIMEOUT`/`CANCELLED`/`UNEXPECTED_RESPONSE`), `error_messages.dart` Türkçe sözlük (**öncelik: sunucu mesajı > sözlük > genel**). **`AuthInterceptor`:** Bearer ekler, 401→`/v1/auth/refresh`→**dönen yeni refresh saklanır (rotasyon)**→istek BİR KEZ tekrarlanır; **eşzamanlı 401'ler tek refresh'e düşer** (tek-uçuşlu kilit); refresh reddedilirse token'lar silinir + `sessionExpiredProvider` sinyali (yönlendirmeyi 11.3 yapar); auth uçları yenileme denemez. **`TokenStore`** arayüz + `SecureTokenStore` (secure_storage, bellek önbellekli) + `InMemoryTokenStore` (test); tempToken saklanmaz. **Modeller:** freezed `PagedResult<T>` (+`hasNextPage`) & `ApiMeta` — **build_runner ilk kez çalıştı**. **`ApiClient`:** ham `get/post/put/patch/delete` + tipli `getObject/getList/getPaged`; şekil tutmazsa sessiz null değil `UNEXPECTED_RESPONSE`. **Utils:** `AppImage.url` (göreli→origin, mutlağa dokunmaz), `AppDate` (**KARAR: `timezone` paketi yerine sabit UTC+3** — TR'de yaz saati yok; cihaz saat diliminden bağımsız + testler deterministik), `AppLinks` (call/whatsapp/map/web/email). **KARAR: çevrimdışıyken oturum DÜŞÜRÜLMEZ** (refresh bağlantı hatası alırsa token silinmez). ⭐PLANDIŞI: **`/gelistirici/ag` Ağ tanılama ekranı** (debug; açılışta 7 gerçek uç + süre/traceId — 11.2'nin canlı kanıtı ve sonraki fazlarda ilk bakılacak yer), kompakt `NetworkLogInterceptor` (Dio'nunki gövdeyi basıp konsolu boğuyordu), **teknik sunucu mesajı filtresi** (canlıda `GET /v1/ads/{yok}` → İngilizce `Entity "Ad" (guid) was not found.`; backend dondurulduğu için istemci bu kalıbı eleyip Türkçe sözlük mesajını gösteriyor — 📌 backend'e not: `NotFoundException` Türkçeleşirse filtre gereksizleşir), testlerde sahte `HttpClientAdapter` (ek paket yok, tüm zincir gerçekte olduğu gibi koşuyor; `DioClient.create`'e `adapter` parametresi). **Doğrulama:** Android emülatörü (Pixel_9) + çalışan API — **7/7 uç beklendiği gibi**; `flutter analyze` 0 sorun, `flutter test` **47/47** (36 yeni). **⏭️ SIRADAKİ: Faz 11.3 — kimlik doğrulama akışı** (splash/telefon→OTP→register; 401-refresh zaten otomatik, elle yazılmayacak; `sessionExpiredProvider`'ı go_router redirect'ine bağla; `tempToken` bellekte taşınacak).
> Önceki: 29 Temmuz 2026 (1. oturum) — **Faz 11.1 TAMAMLANDI: Flutter proje iskeleti + tasarım sistemi. MOBİL KOD YAZIMI BAŞLADI.** Kod **`mobile/`** dizininde (Flutter 3.44.2 / Dart 3.12.2), paket kimliği **`app.kadirli`** (Android applicationId = iOS bundle id; ⚠️ mağazadan sonra değişmez), uygulama adı "Kadirli". **Kurulan:** riverpod 3 (`Notifier` API) + go_router 17 + dio 5 + freezed/json_serializable + secure_storage + shared_preferences + cached_network_image + image_picker + intl + url_launcher + flutter_localizations; **Nunito** 400/600/700 `assets/fonts/`'ta (OFL lisansıyla; Türkçe diakritikler cihazda doğrulandı). **Tema:** `core/theme/` — `AppColors` (MOBILE_UX_PLAN §1 hex'leri) + **`AppPalette` ThemeExtension** (accent/success/info/warning/danger/border/muted/skeleton; `Theme.of(context).palette`), elle yazılmış açık+koyu `ColorScheme` (fromSeed tonu kaydırıyordu), `AppTypography` (Display28/H1 22/H2 18/Body16/sm14/caption13/label12), `AppSpacing`+`AppRadius`+`AppDurations`+`AppA11y.minTapSize=48`, tema modu Riverpod `Notifier`+`shared_preferences` (Açık/Koyu/Sistem, kalıcı). **Ortak bileşenler** (`core/widgets/widgets.dart` barrel): `AppButton`(primary/accent/ghost/danger × normal/small, loading, expand, basılı 0.98 ölçek, reduced-motion saygılı) · `AppCard`(+accentStripe, `SectionHeader`) · `AppScaffold`(AppBar+offline şerit+pull-to-refresh) · `SkeletonBox`/`SkeletonCardList`(shimmer) · `LoadingView`/`EmptyView`/`ErrorView`(traceId)/`OfflineBanner`. **`Env`** (`core/config/env.dart`): flavor dev/prod (`--dart-define=FLAVOR=`), Android emülatör `10.0.2.2:5005` / iOS-sim `localhost:5005` / prod `https://api.kadirli.app`, **`--dart-define=API_BASE_URL=` override** (gerçek cihaz LAN testi). ⭐PLANDIŞI: **`/gelistirici/tasarim` yaşayan stil kılavuzu** (debug-only; palet+tipografi+tüm buton/kart/durum varyantları tek ekranda), **11 test** (`flutter test`: açılış smoke + font/renk + tema kalıcılığı + Env + AppButton), Türkçe locale sabitleme (`tr_TR`+`initializeDateFormatting`), dikey kilit, yazı ölçeği 0.9–1.4 clamp, `SegmentedButton` yeşil tint (Material varsayılanı turuncu `secondaryContainer` kullanıyordu), `mobile/README.md`. **Doğrulama:** Android emülatörü (Pixel_9, API 37) — uygulama açılıyor, açık↔koyu anında değişiyor, tercih yeniden açılışta korunuyor; `flutter analyze` 0 sorun, `flutter test` 11/11. ⚠️ **Bu makinede Xcode kurulu değil → iOS derlenemedi/denenmedi** (CocoaPods da yok); iOS 11.15'ten önce bir kez denenmeli. **⏭️ SIRADAKİ: Faz 11.2 — Ağ katmanı (dio + zarf açma + hata sözlüğü + auth interceptor + token deposu + PagedResult/Meta modelleri + imageUrl/formatDate helper'ları). `core/network/` ve `core/utils/` şu an BOŞ; build_runner kurulu ama hiç çalıştırılmadı.**
> Önceki: 25 Temmuz 2026 (4. oturum) — **MOBİL (Flutter) TASARIM + PLANLAMA yapıldı; kod YAZILMADI. FAZ 11 açıldı.** Kullanıcıyla tasarım yönü belirlendi: his=**sıcak & topluluk**, ana renk=**doğa yeşili** (#2C7A57), navigasyon=**alt sekme + Ana Sayfa hub + sağ üst Ayarlar/Kontrol** (kullanıcının özel isteği: ayarlar ekranı = uygulama kontrol merkezi). Üretilen dokümanlar: **`Memory_Bank/MOBILE_UX_PLAN.md`** (tasarım sistemi: renk/tip/ikon/hareket + UX/navigasyon + modül ekran deseni + ortak durumlar + backend'e göre davranış), **`docs/mobile-mockup.html`** (6 ekran görsel mockup: Ana Sayfa hub / İlanlar liste / İlan detay / Ayarlar / Bildirimler / Giriş-OTP; HER ekranda "buton→uç" haritası — kullanıcının şartı: işlevsiz buton yok; açık+koyu tema; claude.ai artifact olarak da yayınlandı). **Progress.md'ye FAZ 11 (Mobil) eklendi:** 15 alt-faz (11.1 iskelet+tema → 11.2 ağ katmanı → 11.3 auth → 11.4 hub → 11.5 ayarlar/profil → 11.6 duyuru+kesinti → 11.7 eczane+rehber → 11.8-11.9 ilanlar → 11.10 etkinlik+kampanya → 11.11 vefat+taksi+mekan → 11.12 ulaşım+şikayet → 11.13 bildirim+FCM → 11.14 cila → 11.15 yayın), her biri **1 oturumda tam bitirilebilir** boyutta (yarım iş yok kuralı); teknoloji kararları (Flutter+Riverpod+go_router+dio+freezed+secure_storage), çalışma kuralları ve tasarım token'ları Progress'te. ⚠️ **11.13 FCM oturumunda Firebase config (google-services.json / GoogleService-Info.plist) kullanıcıdan istenecek.** **⏭️ SIRADAKİ OTURUM: Faz 11.1 (Flutter proje iskeleti + tema). Referanslar: API_CONTRACT.md + MOBILE_UX_PLAN.md + docs/openapi.json + docs/mobile-mockup.html.**
> Önceki: 25 Temmuz 2026 (3. oturum) — **Faz 10.13 API kontrat temizliği TAMAMLANDI → FAZ 10 TÜM MADDELERİ BİTTİ.** (1) Meta tutarlılığı: `ApiResponseWrapperFilter` kendi `ApiResponse<T>`'sini dönen ~13 handler'ın boş meta'sını da doldurur (reflection; handler'a dokunulmadı) → TÜM public uçlarda meta.timestamp/path/traceId. (2) CORS: `AddCors`+`UseCors("Default")` ← `Cors:Origins` (native mobil CORS istemez, Flutter WEB için; boşsa etkisiz). (3) Swagger JWT `AddSecurityDefinition` (⚠️ Microsoft.OpenApi 2.7.5: tipler `Microsoft.OpenApi` ns'inde, `.Models` düzleşti, reference API kalktı). (4) **Path kebab-case:** yeni `SlugifyParameterTransformer`+`RouteTokenTransformerConvention` → `[controller]` token'lı 5 controller (Announcements/PowerOutages/Files/Notifications/Users) artık kebab; ⚠️ `power-outages` tireli → eski `/v1/PowerOutages` 404 (test güncellendi). (5) `docs/openapi.json` (3.0.4, 135 uç, Bearer). (6) `Memory_Bank/API_CONTRACT.md` = Flutter el kitabı (zarf/hata kodları/auth akışı/sayfalama/tarih/görsel/rate limit/60 public uç). (7) Ölü kod: AutoMapper paketi + 6 FluentValidation validator + IDapperContext/DapperContext + ölü `Files` appsettings bloğu silindi; FluentValidation→core paket; `FileStorage:BaseUrl` belgelendi. KARARLAR: görsel URL göreli kalır (istemci origin ekler / prod `FileStorage:BaseUrl` mutlak); announcements NOT_FOUND 200+success:false quirk kontrata yazıldı, davranış korundu. ⭐PLANDIŞI: path kebab-case (Flutter codegen tutarlılığı). Build 0 hata, xUnit **65/65**. **⏭️ FAZ 10 BİTTİ. Kalan production/deploy (mobil geliştirmeyi bloklamaz, YAYINDAN önce): 10.14/(2) hangfire dashboard auth, 10.14/(3) uploads docker volume; gerçek SMS sağlayıcısı + Firebase service-account bağlama. Sıradaki büyük faz: MOBİL (Flutter) — API_CONTRACT.md ana referans.**
> Önceki: 25 Temmuz 2026 (2. oturum) — **Faz 10.11 FCM push TAMAMLANDI (no-op default'la; gerçek Firebase İSTENMEDİ — bilinçli).** SMS/e-posta adaptör deseninin aynısı: `FirebaseAdmin` 3.6.0 + `IPushService`(`SendAsync(list)`→`list<PushResult>` + `IsConfigured`) + `NoOpPushService`(varsayılan `Fcm:Provider=None`) / `FcmPushService`(`Fcm:Provider=Firebase`; service-account yoksa yine no-op+uyarı). DI switch + `SendPushNotificationsJob`(Cron.Minutely, 9.4 resiliency): IsConfigured=false→DB'ye dokunmadan döner; aksi `fcm_sent=false && User.FcmToken!=null` bildirimleri ≤500 batch `SendEachAsync`. **İşaretleme:** fcm_sent=true terminal; başarı→fcm_sent_at dolu+error null, başarısız→fcm_sent_at null+error sebep; UNREGISTERED→User.FcmToken temizlenir; token'sız kullanıcı sorguya girmez (mobil öncesi doğal boş koşar). Config `Fcm:{Provider:None,ServiceAccountKeyPath:""}` Api+Web. ⭐ **PLANDIŞI:** push `data` yükü `notificationId`(+type/relatedId/relatedType) → mobil deep-link+okundu işaretleme. xUnit **65/65** (yeni `PushNotificationsJobTests` 2 test + `FakePushService`). Build 0 hata. **⚠️ Kalan tek bağlama işi (mobil bitince):** Firebase service-account JSON al → `Fcm:Provider=Firebase` + `Fcm:ServiceAccountKeyPath` — başka kod değişmez (FCM ücretsiz). **⏭️ SIRADAKİ: 10.13 kontrat temizliği (API_CONTRACT.md + CORS + zarf/meta denetimi + openapi.json; en son). Kalan deploy: 10.14/(2) hangfire auth, 10.14/(3) uploads volume; gerçek SMS bağlama.**
> Önceki: 25 Temmuz 2026 — **Faz 10.14/(1) Ads reddi red gerekçesini kaydetmiyordu — DÜZELTİLDİ.** `RejectAdCommandHandler` yanlışlıkla `ApprovedBy`/`ApprovedAt` dolduruyordu → artık `RejectedReason`/`RejectedAt` yazıyor, onay izlerini null'lıyor ("kim reddetti" zaten IAuditableCommand'da; command'e opsiyonel `Reason`+`AuditDetails` eklendi, RejectCampaign deseni). Web reject action sebep alıyor + Index'te JS'siz `<details>` popover (ban-sebep deseni); Admin API `POST /v1/admin/ads/{id}/reject?reason=`. 🔑 **BAĞLANTILI YAN DÜZELTME:** `ApproveAdCommandHandler` red alanlarını null'lıyor — yoksa reddet→onayla akışında bayat gerekçe kalırdı (red alanları ilk kez dolduğundan artık ulaşılabilir). `ApprovedBy`/`ApprovedAt` DTO/view'da HİÇ okunmuyor (grep doğrulandı; yalnız yazma+audit) → temizleme güvenli. xUnit **63/63** (yeni Reject regresyon testi: reason yazılır + approval trail temiz + MyAds'te görünür + reddet→onayla gerekçe temizlenir). Build 0 hata. **⏭️ SIRADAKİ: 10.11 FCM push (⚠️ oturum başında Firebase service-account JSON'u istenecek) → 10.13 kontrat temizliği (en son).**
> Önceki: 19 Temmuz 2026 (6. oturum) — **Faz 10.10-A (plan dışı vizyon turu, kullanıcı talebi): panel etkileşim istatistikleri TAMAMLANDI.** Kapsam ayrı bir tartışma ajanıyla KES/KALSIN/EKLE + karşı-itiraz turuyla belirlendi. 🔑 Kilit karar: panel Index'leri public query paylaştığından public DTO'lara sayaç EKLENMEDİ — panel-only **`Get{Modül}AdminStatsQuery`** deseni (Taxis: taxi_calls GROUP BY count+MAX(called_at) — denormalize total_calls OKUNMAZ, tek kaynak; Announcements: view/click + announcement_views tekil erişim [timestamp yok → trend imkansız, toplam]; Ads: GetAdAdminStatsQuery(AdId) — AdDetailDto tel/WA sayaçlarını taşımıyordu). Panel: TaxiAdmin "Çağrı/Son Çağrı", AnnouncementsAdmin "Görüntülenme (n tekil)/Tıklama", CampaignsAdmin "Kod Gör.", AdsAdmin Index "Görüntülenme" + Edit'e salt-okunur Etkileşim kartı (10.9g deseni). Dashboard "Etkileşim" satırı: 7g yeni kayıt / 7g taksi çağrısı / 7g yeni ilan / toplam duyuru görüntüleme (GetDashboardStatsQuery +4 alan, 60sn cache bedava). **KVKK çizgisi: "kim gördü/kim favoriledi" listeleri panele GİRMEZ, yalnız sayı.** KES'ler: ayrı istatistik sayfası (veri sıfır — boş vitrin), bildirim okunma metriği (10.11 SONRASI aday), JS tablo sıralaması (JS'siz panel konvansiyonu), admin API stats uçları (tüketici yok; query hazır, uç açmak dakikalık). xUnit **62/62** (PanelStatsTests); canlı panel smoke psql geçici verisiyle doğrulandı, kalıntılar+dashboard cache temizlendi. ⚠️ Öğrenim düzeltmesi: view STATİK Türkçe metni ham UTF-8 (entity-encoding yalnız model-bound çıktıda). **⏭️ SIRADAKİ: 10.11 FCM push (⚠️ oturum başında Firebase service-account JSON'u istenecek) → 10.13 kontrat temizliği (en son).**
> Önceki: 19 Temmuz 2026 (5. oturum) — **Faz 10.12 (etkileşim sayaçları) TAMAMLANDI — 10.11'den ÖNCE yapıldı** (kullanıcı onayıyla öne alındı: FCM'den bağımsız; 10.11 Firebase service-account JSON'u bekliyor). `AnnouncementView`/`CampaignCodeView`/`TaxiCall` ilk kez kullanımda. Uçlar: `POST /v1/announcements/{id}/view|click` (anonim + public-write rate limit; sayaçlar atomik ExecuteUpdate; yalnız active+süresi geçmemiş duyuru 404 aksi; giriş yapmışsa view'da announcement_views'a composite-PK TEK satır — view_count ise her çağrıda artar), `POST /v1/campaigns/{id}/view-code` ([Authorize]; kullanıcı başına tek kayıt, 2. istek AYNI kaydı döner, code_view_count="kaç farklı kullanıcı" semantiği; KARAR: kodsuz kampanya 400 VALIDATION_ERROR iz düşülmez), `POST /v1/taxis/drivers/{id}/call` ([Authorize] — telefon döndüğü için; her çağrı yeni taxi_calls satırı + total_calls; verified+active şart). Cache şartı doğrulandı: announcements/campaigns/taxis sorguları cache'siz — sayaçlar hiçbir cache grubunu invalidate etmiyor. **PLANDIŞI (altyapı):** `IUnitOfWork.SetQuery<T>()/AddToSetAsync<T>()` (where T:class) — AnnouncementView BaseEntity değil, IRepository kısıtına takılıyordu (IDapperContext ölü + Dapper Application'da yok → EF Set<T> tercih edildi). xUnit **61/61** (yeni CountersTests 3 test); canlı curl tam senaryo (⚠️ mock kampanyaların tarihi geçmiş — end_date geçici uzatıldı, TATLI20/orijinal tarihe geri alındı); kalıntılar temiz. **⏭️ SIRADAKİ: 10.11 FCM push (⚠️ oturum başında kullanıcıdan Firebase service-account JSON'u istenecek) → 10.13 kontrat temizliği (en son).**
> Önceki: 19 Temmuz 2026 (4. oturum) — **Faz 10.10 (bildirim modülü) TAMAMLANDI.** `Notification` entity'si ilk kez kullanımda. Uçlar (`NotificationsController`, hepsi [Authorize]): `GET /v1/notifications?unreadOnly=` (paged; **unreadCount `data` içinde** — zarf meta'sı filter'da sabit olduğundan; filtre bağımsız toplam okunmamış), `PATCH {id}/read` (sahiplik: başkasınınki 404, tekrar idempotent), `POST read-all` (atomik ExecuteUpdate, `{markedCount}`). Üretim: yeni `IAnnouncementNotificationGenerator` + `AnnouncementNotificationGenerator` (Application DI scoped) — çağıranlar: CreateAnnouncement (anında yayın), **UpdateAnnouncement (plandışı: scheduled→active geçişi de yayın anı)**, PublishScheduledAnnouncementsJob (set-UPDATE'ten duyuru-başına "önce üret sonra yayınla" akışına geçti; çökme arası güvenli, Faz 9.4 resiliency korundu). KARARLAR: send_push=false → satır DA yazılmaz (10.11 sade kalsın); **plandışı: NotificationPreferences.Announcements=false kullanıcıya satır yok** (EF8 ToJson üzerinde jsonb sorgusu çalışıyor) + **body 500 karaktere kırpılır**; idempotency = duyuru başına related_id işareti (varsa komple atla); hedefleme neighborhood → PrimaryNeighborhoodId VEYA UserNeighborhood, diğer targetType'lar "all". xUnit **58/58** (yeni NotificationsTests 4 test); canlı: all/mahalle hedefleme, 401/404/idempotent read, read-all, tercih-kapalı üretim yok, zamanlanmış duyuru job'la 8 kullanıcıya mükerrersiz üretim. Kalıntılar temiz. **⏭️ SIRADAKİ: 10.11 FCM push (⚠️ oturum başında kullanıcıdan Firebase service-account JSON'u istenecek) → 10.12 sayaçlar → 10.13 kontrat.**
> Önceki: 19 Temmuz 2026 (3. oturum) — **10.9 DENETİMİ (kullanıcı talebi): a-i kod düzeyinde yeniden tarandı, 5 bulgu kapatıldı.** (1) 🔴 Web paneli artık **global antiforgery** kullanıyor (`AutoValidateAntiforgeryTokenAttribute` — eski ~30 POST aksiyonu korumasızdı; `Login.cshtml` düz form olduğundan `@Html.AntiForgeryToken()` eklendi; AnnouncementsAdmin fetch'i zaten header gönderiyordu). (2) 🔴 `AccessDeniedPath` var olmayan sayfaya işaret ediyordu → `Account/Denied` action+view eklendi (panele girip yetkisiz sayfa açan moderatör artık 404 değil açıklayıcı sayfa görür; moderatörün modül sayfalarına erişememesi TASARIM GEREĞİ). (3) 🟠 `ResolveComplaintCommand` handler'ına status whitelist (`in_progress|resolved|rejected`) — Admin API ham string geçiriyordu; API doc'taki yanlış "in_review" düzeltildi. (4) 🟠 13 admin komutuna daha `IAuditableCommand`: guide item/category delete, **DeleteIntracityRoute (10.9h'nin kendi komutu — kaçmıştı)**, intercity-schedule/intracity-stop delete, taxi delete/**verify**, place delete, announcement/power-outage delete (eski `ApiResponse<bool>` desen — `AuditBehavior` artık `Success=false`'ta iz bırakmıyor), pharmacy delete + **nöbet assign-duty/delete-duty** (kim atadı izleniyor), **ChangeMyPassword** (staff/change-password, detaysız). Self-servis komutlar (DeleteMyAd/MyAccount/MyFile) bilinçli kapsam dışı. (5) 🟡 StaffAdmin Create hata sonrası girilen değerleri (şifre hariç, matris dahil) koruyor. Dokunulmayanlar (rapor): Logout GET, CreateStaff telefon format validasyonu (Faz 8), DeathsAdmin inline lookup okumaları (10.13 notu). xUnit **54/54**; canlı: token'sız POST 400/token'lı 302, Denied 200, taxi verify + nöbet assign-duty audit izleri, API'den "banana" statüsü 400. Kalıntılar temiz.
> Önceki: 19 Temmuz 2026 (2. oturum) — **Faz 10.9 BÖLÜM 5: (e) Staff UI + (g-panel) + (i-kapsam) tamamlandı → 10.9 BAŞLIĞI TAMAMEN KAPANDI (a-i hepsi).** (e): yeni Web `StaffAdminController` + Index/Create/Edit + paylaşımlı `_PermissionMatrix.cshtml` (16 modül × 5 eylem checkbox matrisi; modül listesi `StaffAdminController.Modules` — API `[RequirePermission]` adlarıyla birebir, "lookups" dahil; indexed binding `permissions[i].X`; tüm bayrakları boş satır kaydedilmez — `CleanPermissions`); Faz 8 command/query'leri olduğu gibi kullanıldı, yeni command yok; Index'te izinsiz moderatöre "İzin yok!" uyarısı, super_admin "Korumalı" (UI'da gizli + command reddi); Edit'te bilgiler/izinler AYRI form (SetPermissions komple-değiştir semantiği), şifre sıfırlama bölümü; sidebar "Personel". (g-panel): `AdsAdmin.Edit` GET+invalid-POST `ViewBag.PropertyValues` (GetAdPropertyValuesQuery) → Edit.cshtml'de salt-okunur "Kategori Özellikleri" kartı (Boolean→Evet/Hayır; düzenleme bilinçli yok). (i-kapsam): 9 komuta marker — deaths approve/delete, events approve/reject/delete, campaigns approve/reject(reason→details)/delete, complaints resolve(details {status}; AdminNotes bilinçli dışarıda). xUnit **54/54**; canlı: moderatör oluştur→izin matrisi DB'de doğru bayraklarla→komple izin değişimi→şifre reset (hash değişti)→soft delete; ilan Edit'te psql'le eklenen "Yakıt Tipi: Dizel" göründü; panel reject/approve/resolve akışları audit_logs'a affected_id'li düştü. ⚠️ Öğrenim: bazı mock kayıtlar (örn. "Kadirli Yaz Konseri" etkinliği) 3 Tem'den beri soft-silinmiş — canlı test hedefi `deleted_at IS NULL` ile seçilmeli. Kalıntılar temizlendi (smokemod, özellik değerleri, statüler geri, audit 0). **⏭️ SIRADAKİ: 10.10 bildirim modülü (uçlar + duyuru yayınında bildirim üretimi) → 10.11 FCM → 10.12 sayaçlar → 10.13 kontrat.**
> Önceki: 19 Temmuz 2026 — **Faz 10.9 BÖLÜM 4: (d-panel) LookupsAdmin panel UI tamamlandı → (d) alt maddesi TAMAMEN kapandı.** Yeni Web `LookupsAdminController` (`admin,super_admin`; tek `Index(open?)` GET + 10 POST: 5 lookup × Create/Update, hepsi antiforgery'li, silme bilinçli YOK) + `Models/LookupsIndexViewModel` + `Views/LookupsAdmin/Index.cshtml` — plandaki gibi tek sayfa, 5 `<details>` akordiyon bölüm (Mahalleler/Mezarlıklar/Camiler/Etkinlik Kategorileri/Mekan Kategorileri; sayı rozetleri, satır içi ekleme formu + `<details>` düzenleme popover'ı — JS'siz). **Desen notu: POST-redirect `?open=<bölüm>` parametresiyle döner, view yalnız o bölüme `open` basar (akordiyon kapanmaz).** Mahalle satırında sakin sayısı + Aktif/Pasif rozeti + pasife alma etki uyarısı; tip select'i boş seçenekli (boş → NULL). Index sorguları: admin query'ler (mahalle/mekan kat.) + public query'ler (mezarlık/cami/etkinlik kat. — command'ler lookups grubunu invalidate ettiğinden bayat cache riski yok). Sidebar'a "Tanımlar" (fa-tags, en altta Şikayetler'in altı). xUnit **54/54** (yeni test yok — salt panel işi; Application/API testleri 18 Tem'den); canlı: panelden mahalle ekle → public `/v1/neighborhoods` 10→11 TAZE → pasife al → 10'a düştü; mükerrer ad banner'ı; mezarlık/etkinlik kat./mekan kat. create+update public uçlarda taze; audit_logs 6 satır panel aktörüyle doğrulandı. Test kalıntıları + audit satırları + lookups cache'i temizlendi. **⏭️ SIRADAKİ OTURUM (10.9'un kalanı, tek paket): e-Staff UI (izin matrisi, modüllere "lookups" dahil), g-panel (AdsAdmin Edit'te salt-okunur özellikler), i-kapsam (deaths/events/campaigns/complaints onaylarına marker) — ayrıntı Progress.md 10.9 "KALAN İŞLER"; sonra 10.10 bildirim.**
> Önceki: 18 Temmuz 2026 (3. oturum) — **Faz 10.9 BÖLÜM 3: (c) İlan kategori & özellik yönetimi tamamlandı; oturum kullanıcı talebiyle (c) sonrası bilinçli KESİLDİ.** (c): `Features/Ads/Commands/AdCategoryCommands.cs` (AdCategory + CategoryProperty + PropertyOption CRUD — hepsi `ICacheInvalidator(ads-lookup)` [10.5 ŞARTI KAPANDI] + audit; KARARLAR: ParentId taşıma yok, rename slug'ı yeniler [unique 409], PropertyType değiştirilemez, option update yok [değer kopyası — silme ilanları bozmaz], kategori silme 409: alt kategori ∥ ilan [IgnoreQueryFilters] ∥ özellik; özellik silme değer varsa 409; Select seçeneksiz 400) + `AdCategoryAdminQueries` (pasifler dahil + sayaçlar, cache'siz) + API `v1/admin/ads/categories` ("categories" literal'i `{id}`'den öncelikli) + panel `AdCategoriesAdmin` (ağaç Index + Create/Edit + Properties: `<details>` formlar, seçenek chip'leri; AdsAdmin'e "Kategoriler" butonu; PLANDIŞI: AdsAdmin.LoadCategoriesAsync inline→query drift düzeltmesi). Slugify `Common/Utils/SlugHelper`'a ortaklaştı. **YAN ÜRÜNLER (panel UI'ları YOK):** (i-ÇEKİRDEK) `AuditBehavior` MediatR pipeline + `IAuditableCommand`/`IAuditContext` (Api: user_id claim, Web: NameIdentifier — İKİSİ DE CANLIDA KANITLANDI; hata isteği düşürmez, bool-false iz bırakmaz, Guid yanıt create'te AffectedId olur, komut ASLA serialize edilmez) — marker: Ads approve/reject/delete, SetUserBan, Staff×5, Business delete/verify + tüm yeni komutlar; (d-API) `LookupCommands` + `LookupAdminQueries` + `v1/admin/lookups/*` (mahalle/mezarlık/cami/etkinlik+mekan kategorisi Create+Update; DELETE YOK — FK'lı sözlük; `ICacheInvalidator(lookups)` [10.4 ŞARTI KAPANDI]; ⚠️ yeni izin modülü "lookups"); (g-API) `GetAdPropertyValuesQuery` + `GET v1/admin/ads/{id}/properties`. xUnit **54/54** (yeni `AdCategoryAdminTests`: invalidation + audit assert'lü); canlı: public kategori 8→9 TAZE (invalidation kanıtı), panel POST akışı antiforgery'yle çalıştı, audit_logs iki aktör tipiyle doğrulandı; test kalıntıları + smoke audit satırları temizlendi. **⏭️ SIRADAKİ OTURUM (tek paket): d-panel (LookupsAdmin UI + sidebar "Tanımlar"), e-Staff UI (izin matrisi, modül listesine "lookups" dahil), g-panel (AdsAdmin Edit'te salt-okunur özellikler), i-kapsam (deaths/events/campaigns onaylarına marker) — ayrıntı Progress.md 10.9 Bölüm 3 "KALAN İŞLER".**
> Önceki: 18 Temmuz 2026 (2. oturum) — **Faz 10.9 BÖLÜM 2 tamamlandı: (b) İşletme/Business yönetimi — kampanya modülü artık gerçek işletmelerle kullanılabilir.** Yeni `Features/Businesses/` (Dtos + Queries + Commands): `GetBusinessesQuery` (arama/kategori/isVerified, Clamp AdminMaxLimit, cache'siz), `GetBusinessByIdQuery`, `GetBusinessCategoriesQuery`, Create/Update (`BusinessRules.ValidateAsync` paylaşımlı; Instagram `@` soyulur), **`DeleteBusinessCommand` — KARAR: campaigns FK DB'de CASCADE + Business soft-delete değil → `IgnoreQueryFilters` ile HERHANGİ bir kampanyası (soft-silinmiş dahil) olan işletme 409**, `SetBusinessVerificationCommand` (verify/unverify, izleri NULL'lar), `CreateBusinessCategoryCommand` (aynı ad 409). Yeni Admin API `v1/admin/businesses` (GET/POST/{id} PUT/DELETE + verify/unverify + categories GET/POST, `RequirePermission("businesses",...)`) + yeni panel `BusinessesAdminController` + Index/Create/Edit view'ları (logo upload "business" modülü; **hızlı kategori ekleme `<details>` formu ana formun DIŞINDA — iç içe form HTML'de geçersiz**) + sidebar "İşletmeler". `CampaignsAdmin.LoadBusinessesAsync` inline sorgudan `GetBusinessesQuery`'ye geçti (Faz 9.4 kuralı; DTO `.Id/.BusinessName` taşıdığından view kırılmadı) + kampanya formuna "yeni işletme ekleyin" linki. xUnit **51/51** (yeni `BusinessesAdminTests` 2 test); canlı panel akışı doğrulandı (kategori→dropdown→işletme→verify→kampanya formunda görünüm→kampanyalı silme 409 banner→temiz silme OK), test kalıntıları temizlendi. ⚠️ curl form testi öğrenimi: `-F "alan=@x"` dosya upload olur — `--form-string` kullan. Detay: Progress.md 10.9 Bölüm 2. **Sıradaki: 10.9 Bölüm 3 — kalan c (ilan kategori/özellik; ads-lookup invalidator ŞART) / d (lookup CRUD) / e (Staff UI) / g (moderasyonda property) / i (AuditLog).**
> Önceki: 18 Temmuz 2026 — **Faz 10.9 BÖLÜM 1 tamamlandı: (a) nöbet takvimi UI + (f) panel şifre değiştirme + (h) inline drift düzeltmeleri.** (a): `PharmaciesAdmin`'e `Schedule/ScheduleCreate/ScheduleDelete` action'ları + `Schedule.cshtml` (EventsAdmin/Calendar deseni birebir — server-rendered aylık grid; gün hücresinde eczane chip'i + × silme, "+" formu doldurur); mevcut 10.4 command/query'leri kullanıldı, `Source:"panel"`; PLANDIŞI: "Bugün nöbetçi" banner'ı (atama yoksa kırmızı uyarı) + atanmamış gelecek günler amber işaretli; canlıda panelden atama → `GET /v1/pharmacies/on-duty` TAZE döndü (cache invalidation kanıtı), mükerrer atama ConflictException mesajıyla banner'da. (f): YENİ `ChangeMyPasswordCommand` (mevcut şifre doğrulanır; `Password==null || Role==User` reddi — OTP kullanıcısının şifresi yok) + `AccountController.ChangePassword` GET/POST + sidebar linki; PLANDIŞI: header'da giriş yapan kullanıcı adı; canlıda admin şifresi değiştirilip test edildi ve **seed şifresi `Admin123!`'e geri döndürüldü**. (h): `UsersAdmin.Ban/Unban` → `SetUserBanCommand` (ban_reason/banned_at/banned_by artık DOLU — psql'le kanıtlandı; PLANDIŞI: JS'siz `<details>` popover'ıyla ban sebebi giriliyor, listede sebep görünüyor); `TransportAdmin.Delete` → YENİ `DeleteIntracityRouteCommand` (bilinçli hard delete — IntracityRoute soft-delete değil, duraklar cascade; transport cache'siz → invalidator gerekmedi). Yeni panel POST'larında `[ValidateAntiForgeryToken]`. xUnit **49/49** (yeni test eklenmedi — panel işi, canlı smoke ile doğrulandı; iki yeni command panel-only). Test kalıntıları temizlendi. Detay: Progress.md 10.9 Bölüm 1. **Sıradaki madde: 10.9 Bölüm 2 — önerilen (b) İşletme/Business CRUD (kampanya modülünün ön koşulu); sonra c/d/e/g/i.**
> Önceki: 17 Temmuz 2026 (7. oturum) — **Faz 10.8 (eksik public uçlar ve kontrat boşlukları) tamamlandı.** Yeni uçlar: `GET /v1/complaints/my` ([Authorize], paged — anonim şikayetler user_id NULL olduğundan listelenmez); `DELETE /v1/files/{id}` (sahiplik 403, 13 referans alanı taranır — referanslıysa 409; KARAR: soft delete + fiziksel dosya best-effort silinir; yetim dosya Hangfire job'ı ERTELENDİ; 🐛 plandışı fix: `LocalFileStorageService.DeleteFileAsync` göreli URL'de fırlatıyordu); `DELETE /v1/users/me` (STORE ZORUNLULUĞU KAPANDI — KARAR: soft delete + anonimleştirme: phone→"del"+12hex yeniden kayda açılır, kişisel alanlar NULL, ilanlar soft-silinir, favoriler silinir, refresh jti iptal + kalan refresh'ler global filtre sayesinde 401; yalnız Role=User silebilir, admin 403); **Transport saat/durak** (`IntercitySchedule`/`IntracityStop` ilk kullanım: public DTO'larda `schedules:[{id,departureTime:"HH:mm"}]` + `stops:[{stopName,stopOrder,timeFromStart}]`, admin CRUD `v1/admin/transport/intercity/{routeId}/schedules` + `intracity/{routeId}/stops` POST/DELETE — 400/404/409 kuralları; MockDataSeeder'a idempotent seed; panel formu 10.9'da); **Ads** `?sort=` whitelist (newest/oldest/price_asc/price_desc, bilinmeyen 400) + KARAR: arama artık başlık+AÇIKLAMADA (description indeksi yok — büyürse eklenir, 10.13 notu); **Announcements** sayfalama+`?typeId=` — `GetAnnouncementsQuery` `PagedResult`'a geçti (⚠️ kontrat: data artık `{items,totalCount,...}`; Web paneli Limit=200 ile uyarlandı; mahalle hedefleme kararı 10.10'A ERTELENDİ — "istemci filtreler" geçici kural). ⚠️ Yeni öğrenim: entity config'lerinde `HasQueryFilter(DeletedAt==null)` global filtreleri VAR — soft-silinmiş satır okumak `IgnoreQueryFilters()` ister. xUnit **49/49** (yeni `MissingEndpointsTests` 6 test); tüm uçlar canlı curl ile doğrulandı, test kalıntıları temizlendi. Detay: Progress.md 10.8. **Sıradaki madde: 10.9 (panel tamamlama — a: nöbet takvimi UI EN ACİL; muhtemelen 2-3 oturuma bölünür).**
> Önceki: 17 Temmuz 2026 (6. oturum) — **Faz 10.7 (Public API görünürlük/güvenlik Bölüm 2 + istek sertleştirme) tamamlandı.** Denetimdeki tüm görünürlük bug'ları kapatıldı — desen: her sorguya varsayılanı `false` opsiyonel bayrak (admin/panel davranışı birebir korunur, yalnız public controller `true` geçer): **Deaths** liste yalnız approved (`?status=` public'te yok sayılır; **KARAR: archived public'e dahil değil**), detay approved-değilse yalnız ekleyen (RequesterId claim'den, diğerine 404); **Events/Campaigns/Announcements detayları** id ile artık pending/scheduled/süresi-geçmiş dönmüyor (campaign approved+tarih-geçerli şartı; announcements NOT_FOUND'u mevcut kontrat gereği hâlâ HTTP 200+success:false — 10.13 konusu); **Taxis** public'te IsVerified+IsActive zorunlu (istemci parametreleri etkisiz), **Places/Pharmacies/Transport** IsActive zorunlu — ⚠️ `GetPharmaciesQuery` cache'li olduğundan `OnlyActive` bayrağı CacheKey'e eklendi (`:pub{...}`). **Sayfalama:** yeni `Common/Models/Pagination.Clamp` TÜM 16 sayfalı handler'da — public 50 / admin 200 (bayraksız paylaşımlı listelerde tek sınır 200; paneldeki en büyük sabit limit 50, kırılmadı). **Rate limit:** yeni `public-write` policy (15/dk/IP, `RateLimiting:PublicWrite`) → deaths/complaints/ads POST + track-phone/whatsapp + **files/upload (ekstra)**; favorite/extend gibi hafif+authorize uçlar bilinçli global limitte. xUnit **43/43** (yeni `PublicVisibilityTests` 6 test + RateLimitingTests'e 429 senaryosu); canlı curl: pending vefat sızıntısı kapalı, psql ile pasifleştirilen kayıtlar public'te 404 (sonra geri alındı), 16. anonim complaint 429. Detay: Progress.md 10.7. **Sıradaki madde: 10.8 (eksik uçlar — complaints GET my, files DELETE, hesap silme [store zorunluluğu], transport saat/durak, ads sıralama, announcements sayfalama).**
> Önceki: 17 Temmuz 2026 (5. oturum) — **PROJE GENELİ DENETİM yapıldı (geliştirme YOK, yalnız tespit; kullanıcı talebi).** Tüm public API + Admin API + Web panel controller/view'ları ve entity kullanımı tarandı; bulgular Progress.md Faz 10'a **3 yeni madde** olarak eklendi ve eski 10.7-10.10 maddeleri **10.10-10.13'e ötelendi** (eski kayıtlardaki 10.7/10.10 referansları eski numaradır — Progress'te numaralandırma notu var). Yeni maddeler: **10.7 🔴 görünürlük bug'ları Bölüm 2** (Deaths public liste/detay status filtresiz — kullanıcı gönderimi pending ilanlar YAYINDA; Events/Campaigns/Announcements detayları id ile her statüde okunuyor; Taxis doğrulanmamış sürücüleri telefonuyla dönüyor; Places/Pharmacies/Transport pasif kayıtları dönüyor; hiçbir listede Page/Limit clamp'i yok — ?limit=1000000 tüm tabloyu çeker; anonim/user yazma uçlarında hedefli rate limit yok), **10.8 eksik uçlar** (complaints GET my Faz 6 kaydında var kodda YOK; files DELETE yok; hesap silme DELETE /users/me yok — App Store/Play Store ZORUNLU, mobil yayın engeli; IntercitySchedule/IntracityStop hiç kullanılmıyor — ulaşım yanıtlarında saat/durak yok; Ads sıralama parametresi yok; Announcements listesinde sayfalama yok), **10.9 ⭐ ÖZELLİK EKLEMELERİ — panel tamamlama** (a: nöbet takvimi UI'ı YOK — mobilin 1 no'lu özelliği panelden girilemiyor, EN ACİL; b: Business CRUD hiçbir katmanda yok — kampanya modülü fiilen kullanılamaz; c: AdCategory/CategoryProperty/PropertyOption yönetimi yok — yalnız seed; d: mahalle/mezarlık/cami/kategori lookup CRUD'ları yok; e: Staff paneli yok — API var UI yok; f: admin panelden şifre değiştiremiyor; g: ilan moderasyonunda property değerleri görünmüyor; h: UsersAdmin Ban/Unban inline — BanReason/BannedAt/BannedBy yazılmıyor + TransportAdmin inline hard delete; i: AuditLog tamamen ölü). **Sıradaki madde: 10.7 (görünürlük bug'ları — güvenlik önce).**
> Önceki: 17 Temmuz 2026 (4. oturum) — **Faz 10.6 (Ads mobil uçları Bölüm 2) tamamlandı.** Yeni uçlar: `PUT /v1/ads/{id}` (kendi ilanını güncelleme — sahiplik 403, kategori değiştirilemez, **her düzenleme yeniden moderasyona düşürür** status=pending + onay/red izleri temizlenir; masterclass PATCH derdi, plan gereği PUT seçildi), `DELETE /v1/ads/{id}` (soft delete, sahiplik 403), `POST/DELETE /v1/ads/{id}/favorite` (**idempotent karar** — 2. istek 200+data:false; yalnız approved favorilenebilir, aksi 404), `GET /v1/users/me/ads?status=` (`MyAdDto`: rejectedReason + favoriteCount + phone/whatsappClickCount + extension bilgileri — sahibe özel alanlar) + `GET /v1/users/me/favorites` (silinen ilan listeden düşer, yayından düşen `isAvailable:false`), `POST /v1/ads/{id}/extend` (sabit 30 gün, hak MaxExtensions=3 dolunca 409; **expired ilan uzatmayla yeniden approved olur**, yeni bitiş max(now, eski)+30g; opsiyonel `{adsWatched}` gövdesi `ad_extensions`'a yazılır), `POST /v1/ads/{id}/track-phone` + `/track-whatsapp` (anonim, atomik ExecuteUpdate — cache invalidation tetiklemez). 10.5'in handler validasyonları `Features/Ads/AdSubmissionRules.cs`'e çıkarıldı (CreateAd + UpdateMyAd paylaşır). `AdFavorite`/`AdExtension` entity'leri ilk kez kullanımda. xUnit 36/36 (yeni `AdsMobilePart2Tests` 5 test — ExpireAdsJob etkileşimi dahil); tam canlı curl senaryosu (iki kullanıcı + admin). Detay: Progress.md 10.6. **Sıradaki madde: 10.7 (bildirim modülü: uçlar + duyuru yayınında bildirim üretimi).**
> Önceki: 17 Temmuz 2026 (3. oturum) — **Faz 10.5 (Ads mobil uçları Bölüm 1) tamamlandı.** Yeni public uçlar: `GET /v1/ads/categories?parentId=` + `GET /v1/ads/categories/{id}/properties` (yeni `CacheGroups.AdsLookup` "ads-lookup" grubunda 15 dk TTL; `AdCategoryQueries.cs`), `GET /v1/ads/{id}` (detay: kategori adı + görseller + property değerleri; approved olmayanı YALNIZ sahibi görür — diğerlerine 404; view_count ayrı atomik `ExecuteUpdateAsync` ile artar — bunun için Application'a `Microsoft.EntityFrameworkCore.Relational` paketi eklendi), `POST /v1/ads` `[Authorize]` (UserId claim'den, status=pending, imageFileIds + propertyValues; `CreateAdCommand.IsUserSubmission` bayrağı: cep telefonu regex + görsel sahipliği (`files.uploaded_by==UserId`) + zorunlu property denetimi yalnız user akışında — panel etkilenmez). **🐛 Güvenlik düzeltmesi:** public `GET /v1/ads` status filtrelemiyordu — pending/rejected ilanlar telefonlarıyla sızıyordu; `GetAdsQuery.OnlyPublished` eklendi (public=true → approved + süresi geçmemiş; panel/admin davranışı değişmedi). **Seed:** DbSeeder'a alt kategoriler (Araçlar→Otomobil/Motosiklet/Ticari Araç, Emlak→Satılık/Kiralık Konut/Arsa) + kategori özellikleri (Otomobil 5, Satılık Konut 4; Select'ler options'lı) idempotent eklendi — dev DB'de oluştuğu doğrulandı. **⚠️ 10.10 notu:** `Features/Ads/Validators/*` FluentValidation sınıfları hiçbir pipeline'a kayıtlı değil (ölü kod) — 10.5 validasyonu handler'a yazıldı, genel karar 10.10'da. xUnit 31/31; tam canlı curl senaryosu (cache 36ms→3.7ms, onay akışı, viewCount, 5 validasyon reddi). Detay: Progress.md 10.5. **Sıradaki madde: 10.6 (Ads Bölüm 2: benim ilanlarım, favoriler, uzatma, iletişim sayaçları).**
> Önceki: 17 Temmuz 2026 (2. oturum) — **Faz 10.4 (nöbetçi eczane + public lookup uçları) tamamlandı.** `PharmacySchedule` ilk kez kullanıma açıldı (önceden yalnız seeder'daydı): `GET /v1/pharmacies/on-duty?date=` (varsayılan gün **Türkiye saati** — `TurkeyClock`, Europe/Istanbul + UTC+3 fallback; UTC olsaydı 00:00-03:00 arası yanlış gün dönerdi) ve `GET /v1/pharmacies/schedule?year&month`; admin nöbet CRUD'u `v1/admin/pharmacies/schedule` (GET/POST/DELETE, aynı eczane+gün 409 — aynı güne farklı eczaneler serbest). Lookup'lar: `GET /v1/neighborhoods` + `/v1/deaths/cemeteries` + `/v1/deaths/mosques` + `/v1/events/categories` (`Features/Lookups/LookupQueries.cs`). Cache: on-duty/schedule `pharmacies` grubunda (nöbet command'leri ICacheInvalidator — invalidation canlıda redis-cli ile kanıtlandı), lookup'lar yeni `CacheGroups.Lookups` grubunda 15 dk TTL (CRUD'ları yok — eklenirse invalidator şart). xUnit 27/27; canlı curl tam senaryo. Detay: Progress.md 10.4. **Sıradaki madde: 10.5 (Ads mobil uçları Bölüm 1: kategoriler, detay, kullanıcı ilan verme).**
> Önceki: 17 Temmuz 2026 — **Faz 10.3 (users/me + fcm-token) tamamlandı + 10.2'nin bekleyen dev DB temizliği kullanıcı onayıyla yapıldı.** Yeni uçlar: `GET /v1/users/me` (profil + mahalle adı + bildirim tercihleri), `PATCH /v1/users/me` (username/age/primaryNeighborhoodId/profilePhotoFileId/removeProfilePhoto; username+mahalle **30 günde bir** değişebilir — ihlal 400 `USERNAME_CHANGE_LIMIT`/`NEIGHBORHOOD_CHANGE_LIMIT`, ilk değişiklik serbest; username çakışması 409; profilePhotoFileId yalnız kullanıcının kendi yüklediği dosya), `PATCH /v1/users/me/notifications` (kısmi güncelleme), `POST /v1/notifications/fcm-token` (yeni `NotificationsController` — aynı token başka kullanıcıdaysa ondan temizlenir, cihaz hesap değiştirme senaryosu). KARAR: eski `GET/PUT /v1/users/{id}/profile` uçları ve ölü profile CQRS'leri SİLİNDİ (IDOR yüzeyi tamamen kalktı). EKSTRA: `xunit.runner.json` ile test koleksiyonları artık sıralı koşuyor (5 sınıfın paralel Testcontainers çakışması flaky hata veriyordu). xUnit 22/22; tüm senaryolar canlı curl ile doğrulandı. Detay: Progress.md 10.3. **Sıradaki madde: 10.4 (nöbetçi eczane + public lookup uçları).**
> Önceki: 16 Temmuz 2026 (3. oturum) — **Faz 10.2 (auth: refresh/logout/register) tamamlandı.** KARAR: masterclass §12.3 temp_token+register akışına geçildi (verify-otp artık kullanıcı OLUŞTURMAZ; yeni kullanıcıya `{isNewUser:true, tempToken}`, kayıtlıya `{isNewUser:false, accessToken, refreshToken, expiresIn}` döner — eski `{token}` alanı kaldırıldı). Yeni uçlar: `POST /v1/auth/register` (username+mahalle+yaş, 409/400 validasyonları), `POST /v1/auth/refresh` (jti rotasyonu — eski refresh Redis `revoked_jti:*` iptal listesine, kullanıcı DB'den taze okunur), `POST /v1/auth/logout` ([Authorize], refresh iptali + FcmToken temizliği). Temp/refresh token'lar RefreshSecret+`token_type` ile imzalı — [Authorize] uçlarından geçemez. Access süresi 1 güne indirildi (`Jwt:AccessExpiresDays`, artık config'ten okunuyor). **EKSTRA (kritik):** entegrasyon testlerinin aslında Testcontainers'a değil DEV veritabanına yazdığı keşfedildi ve düzeltildi (AddInfrastructure bağlantıları artık kullanım anında IConfiguration'dan okur); Program.cs'teki güvensiz JWT fallback secret kaldırıldı. xUnit 17/17; tüm senaryolar canlı curl ile doğrulandı. ~~Eski oturum test kalıntılarının dev DB temizliği kullanıcı onayı bekliyor~~ → 17 Tem'de onayla yapıldı. Detay: Progress.md 10.2.
> Önceki: 16 Temmuz 2026 (2. oturum) — **Faz 10.1 (KRİTİK GÜVENLİK: public yazma uçları) tamamlandı.** Admin-işi public yazma kopyaları SİLİNDİ (announcements/pharmacies/guide-categories/events/places/taxis-drivers/transport POST-PUT-DELETE + deaths PUT/DELETE + PowerOutages'ın korumalı kopyaları) — karşılıkları `v1/admin/*`'ta, Web paneli ISender kullandığından etkilenmedi. Deaths POST `[Authorize]` + `AddedBy` claim'den (pending'e düşer). Files upload `[Authorize]` + 10 MB + magic-byte (yalnız jpeg/png/webp, uzantı/Content-Type'a güvenilmez; dosya adı sanitize + uzantı tespit edilen türden). Users `{id}/profile` IDOR kapatıldı (`[Authorize]` + id==user_id claim, kalıcısı 10.3'te /users/me). `ApiControllerBase.CurrentUserId` eklendi. Yeni `PublicEndpointAuthorizationTests` ile xUnit 12/12; tüm senaryolar canlı curl ile doğrulandı. Detay: Progress.md 10.1.
> Önceki: 16 Temmuz 2026 — **Faz 9.2 (Güvenlik & İletişim) altyapısı tamamlandı** (sağlayıcı anlaşması olmadığı için gerçek SMS/mail BAĞLANMADI, bağlamaya hazır): `ISmsService`/`IEmailService` + Dev/log adaptörleri (`Sms:Provider`/`Email:Provider` config switch), OTP login yanıtından kaldırıldı (yalnız `Otp:DevMode=true` iken `otp` alanı döner; DevMode=false'ta kod `ISmsService`'e gider), masterclass §12.2 sertleştirmesi (MaxAttempts→5 dk blok, timing-safe karşılaştırma, saatlik limit artık 500 değil 429), .NET 8 Rate Limiting (Api: auth 5/dk/IP + global 300/dk/IP, zarf'lı 429 + Retry-After; Web: panel-login 5/dk/IP). Sağlayıcı bağlama talimatı Progress.md Faz 9.2'de. xUnit 7/7; tüm senaryolar canlı doğrulandı.
> Önceki: 15 Temmuz 2026 — Faz 9.3 (Gözlemlenebilirlik) ve Faz 9.4 (Performans & Mimari) tamamlandı. 9.3: Serilog (Console + günlük rolling JSON dosya + Seq) Api+Web'de, yanıt zarfı `meta`'sına `traceId`, `/health` + `/health/live` + `/health/ready` (Postgres/Redis/Hangfire). 9.4: Redis distributed cache (MediatR `CachingBehavior`/`CacheInvalidationBehavior`; guide+pharmacies 15 dk grup-invalidation'lı, dashboard 60 sn TTL), Hangfire job'ları dirençli (`ExecuteUpdateAsync` idempotent + `AutomaticRetry{60,300,900}→Fail` dead-letter + `DisableConcurrentExecution`), BFF geçişi DEĞERLENDİRİLDİ ve bilinçli ertelendi (tetikleyici koşullar Progress.md 9.4'te). Ekstra: Web Dashboard ve GuideAdmin CategoryDelete inline kod yerine Application command/query'lerine geçirildi. Hepsi canlı doğrulandı; detay Progress.md Faz 9.

## Mevcut Teknik Durum
- **Çalışma Alanı**: `/Users/atahanblcr/Desktop/dotnet/KadirliApp`
- **Mimari**: Clean Architecture (Domain, Application, Infrastructure, Api, Web).
- **Build**: `dotnet build KadirliApp.sln` → 0 hata (yalnızca Tests projesinde Testcontainers deprecation uyarıları; Hangfire storage uyarısı 15 Tem'de giderildi).
- **Çalışma ortamı**: Docker (`kadirliapp_db` Postgres :5432, `kadirliapp_redis` :6379, `kadirliapp_seq` Seq UI :8081 / ingestion :5341) — `docker compose up -d` KadirliApp/ dizininden. launchSettings portları: Api :5005, Web :5203.
- **Loglar**: Console + `KadirliApp.Api/logs/api-*.ndjson` ve `KadirliApp.Web/logs/web-*.ndjson` (JSON, 14 gün) + Seq (`http://localhost:8081`, Application property'siyle Api/Web ayrışır, TraceId ile arama). Hata yanıtlarındaki `meta.traceId` Seq'teki olayla eşleşir.
- **Cache**: Redis'te `cache:*` anahtarları + `cache-group:*` set'leri; `redis-cli --scan --pattern 'cache*'` ile gözlenebilir. Redis çökerse istekler cache'siz devam eder (fail-open).

## Ne Gerçekten Çalışıyor (canlı doğrulandı, 4 Temmuz 2026)
- **Panel girişi gerçek**: `admin / Admin123!` → DB'den kullanıcı bulunur, BCrypt doğrulanır, claim'lere GERÇEK user.Id yazılır. Approve/Verify aksiyonları `approved_by`/`verified_by` kolonlarına gerçek admin id'sini yazıyor (DB'den doğrulandı).
- **Seed**: `DbSeeder` açılışta migration + admin + tüm lookup tablolarını (mahalle, kategoriler, mezarlık, cami vb.) idempotent doldurur. Dashboard'daki "Paneli Test Verileriyle Doldur" butonu `MockDataSeeder`'ı çağırır (idempotent — dolu tablolara dokunmaz): 4 kullanıcı, 5 ilan, 3 duyuru, 2 vefat, 3 eczane+nöbet, 2 kesinti, 2+2 ulaşım hattı, 2 işletme+2 kampanya, 3 etkinlik, 3 mekan, 3 taksici, 2 şikayet.
- **Dashboard gerçek veri**: Toplam kullanıcı / aktif ilan / bekleyen onaylar (5 modülün pending toplamı) / toplam duyuru + "Son Aktiviteler" (son ilan+duyurular) DB'den geliyor (`DashboardViewModel`). **15 Tem:** panel artık inline sorgular yerine `GetDashboardStatsQuery`/`GetRecentActivitiesQuery` (ISender) kullanıyor — 60 sn Redis cache'li.
- **Tüm 13 admin modülü Index'i 200 dönüyor** ve sidebar'da linkli: İlanlar, Duyurular, Etkinlikler, Kampanyalar, Kullanıcılar, Vefat, Eczaneler, Kesintiler, Ulaşım, Taksiciler, Mekanlar, Şehir Rehberi, Şikayetler. Faz 7 ile birlikte bu modüllerin mantıksal eksikleri giderildi (Örn: Duyurular için yeni tür ekleme, mahalle seçimi düzeltmeleri, etkinlik/kampanya ve mekanlar için fotoğraf ve konum eklemeleri yapıldı).
- **API OTP akışı gerçek**: POST `/v1/auth/login` → Redis'e OTP yazar ve döner; `/v1/auth/verify-otp` → kullanıcıyı bulur/oluşturur, gerçek JWT döner (canlı test edildi; route 8 Tem'de `api/auth`'tan `v1/auth`'a taşındı, eski yol 404).

## Ne Gerçekten Çalışıyor — Faz 8 eklemeleri (canlı doğrulandı, 7 Temmuz 2026)
- **Response zarfı kontratı artık uygulanıyor**: `ApiResponseWrapperFilter` tüm başarılı API yanıtlarını `{success:true, data, meta}` zarfına sarıyor (zaten `ApiResponse<T>` dönen eski handler'larda çift sarma yok); `ExceptionMiddleware` hataları `{success:false, error:{code,message}, meta}` + doğru HTTP koduna çeviriyor (NotFound→404 canlı doğrulandı).
- **Admin API (Faz 4 karşılığı) yazıldı**: `Api/Controllers/Admin/` altında 13 controller, `v1/admin/*`, hepsi `AdminPanel` policy korumalı. 15 liste endpoint'i 200 + zarf; approve/ban/unban mutasyonları çalışıyor; yetkisiz istek 401. JWT'deki `user_id` claim'i `CurrentAdminId` ile okunuyor.
- **Application'a yeni CQRS'ler**: `GetDashboardStatsQuery`, `GetRecentActivitiesQuery`, `GetUsersQuery`, `SetUserBanCommand`, `DeleteGuideCategoryCommand` (8 Tem — item/alt kategori varsa 409 CONFLICT; guide FK'ları `ON DELETE RESTRICT`). Web hâlâ eski inline kodunu kullanıyor — Admin API bunları kullanıyor.
- **Güvenlik**: Public `PowerOutagesController` POST/PUT/DELETE artık `AdminPanel` yetkisi istiyor (öncesinde tamamen açıktı). **GÜNCELLEME (16 Tem, Faz 10.1):** bu korumalı kopyalar dahil TÜM admin-işi public yazma uçları silindi — public yüzey artık salt-okunur + 3 bilinçli yazma ucu: deaths POST (`[Authorize]`, pending), files upload (`[Authorize]`, 10 MB, magic-byte), complaints POST (bilinçli anonim).

## Bilinen Sınırlar / Dikkat
- ✅ **ÇÖZÜLDÜ (8 Tem):** Auth route'u `v1/auth` yapıldı; yanlış OTP artık 400 `INVALID_OTP`, banlı/pasif hesap 403 `FORBIDDEN` dönüyor (VerifyOtp handler tipli exception'lara geçirildi — eski `UnauthorizedAccessException`'lar middleware'de 500'e düşüyordu).
- ✅ **ÇÖZÜLDÜ (8 Tem):** Staff CRUD / admin profile / change-password / `[RequirePermission]` ince yetkilendirme (Application katmanında ve Admin API'de tamamlandı ve test edildi).
- Mekan `amenities` alanı jsonb — API'ye JSON-string gönderilmeli (`"{\"WC\":true,...}"`, panelin formatı); düz metin 500 verir (ileride 400'e çevrilebilir).
- ✅ **ÇÖZÜLDÜ (8 Tem):** `Otp:DevMode` artık gerçekten çalışıyor — `RedisOtpService` config'i okumuyordu, düzeltildi; DevMode'da OTP sabit `123456`. **16 Tem:** OTP artık response'ta YALNIZ DevMode'da dönüyor; DevMode=false'ta `ISmsService`'e gidiyor (bugün Dev/log adaptörü). Prod öncesi kalan tek iş: gerçek SMS sağlayıcısı + `Otp:DevMode=false`.
- ✅ **ÇÖZÜLDÜ (13 Tem):** Dosya yükleme tüm ilgili admin formlarına bağlı — Duyuru/Etkinlik/Kampanya/Mekan/Vefat (Create+Edit) ve artık İlanlar (çoklu görsel, kapak yönetimi). Mock verilerde görsel hâlâ yok (görseller elle yüklenmeli).
- Ziyaretçi telemetrisi yok (Dashboard'dan sahte "Günlük Ziyaretçi" kartı kaldırıldı, yerine "Toplam Duyuru" geldi).
- Npgsql `EF.Functions.ILike` Application katmanında kullanılamıyor → aramalar `ToLower().Contains()` deseniyle.
- Timestamptz kolonlara yazarken `DateTime.SpecifyKind(..., DateTimeKind.Utc)` şart.
- ✅ **ÇÖZÜLDÜ (8 Tem):** Birim/entegrasyon testleri + CI/CD (Masterclass Faz 7) tamamlandı. `KadirliApp.Tests` xUnit projesi kuruldu, Testcontainers ile entegrasyon testleri yazıldı ve `.github/workflows/dotnet.yml`

## Bir Sonraki Olası Adımlar (Faz 9 - Sürdürülebilirlik & Production)
- ✅ **TAMAMLANDI (13 Tem):** Panel & UI Geliştirmeleri — dosya yükleme UI entegrasyonu + mobil istemci filtreleri.
- ✅ **TAMAMLANDI (16 Tem — altyapı):** Güvenlik & İletişim (Faz 9.2) — OTP yanıttan kaldırıldı (DevMode hariç), ISmsService/IEmailService soyutlaması (Dev/log adaptörleri, config switch), OTP deneme bloğu, Rate Limiting (Api auth+global, Web panel-login). **KALAN (mobil yayın öncesi):** gerçek SMS sağlayıcısı satın alınıp bağlanması (`Infrastructure/Notifications/`'a implementasyon + DI switch'e case + `Sms:Provider` + `Otp:DevMode=false`) ve o zaman health check'e SMS kontrolü.
- ✅ **TAMAMLANDI (15 Tem):** Gözlemlenebilirlik — Serilog (Console/dosya-JSON/Seq) + traceId + `/health` uçları.
- ✅ **TAMAMLANDI (15 Tem):** Performans & Mimari — Redis distributed cache (MediatR behavior'ları ile invalidation) + Hangfire retry/dead-letter/idempotency.
- ✅ **DEĞERLENDİRİLDİ (15 Tem):** BFF geçişi bilinçli ertelendi — karar, gerekçeler ve geçişi tetikleyecek koşullar Progress.md Faz 9.4'te. Kural: yeni panel özellikleri inline kod yerine HER ZAMAN Application command/query üzerinden yazılmalı (hem BFF hazırlığı hem cache invalidation için).

## 🎯 AKTİF PLAN: Faz 10 — Mobil Öncesi Hazırlık (15 Tem 2026'da açıldı)
- Mobil (Flutter) tarafına geçmeden önce public API'nin hazırlanması için **10 oturum-boyutunda madde** Progress.md Faz 10'da: 10.1 KRİTİK güvenlik taraması (public yazma uçları tamamen anonim!, Files upload anonim 100MB, Users {id}/profile IDOR) → 10.2 auth (refresh/logout/register YOK) → 10.3 users/me + fcm-token → 10.4 nöbetçi eczane + lookup'lar → 10.5-10.6 Ads mobil uçları (kategori/detay/create; me-scoped/favori/extend) → 10.7 bildirim modülü (Notification entity hiç kullanılmıyor) → 10.8 FCM push (Firebase service-account kullanıcıdan istenecek) → 10.9 etkileşim sayaçları → 10.10 kontrat temizliği + API_CONTRACT.md + CORS (AddCors/UseCors hiç yok, ölü config).
- Her oturumda BİR madde; ayrıntılar ve doğrulama kriterleri Progress.md'de. **Referans kuralı:** DOTNET_MASTERCLASS.md ilk referans ama tek kaynak DEĞİL (Faz 7/8/9 geliştirmeleri onu kısmen eskitti) — her maddede mevcut kod + Memory_Bank kontrol edilmeli, çelişkide mevcut kod kazanır.
- Faz 9.2 (SMS + rate limiting) ayrı oturumda; mobil YAYIN öncesi zorunlu ama geliştirme başlangıcına engel değil.
