 # KadirliApp — Code Review Checklist

> Bu liste jenerik bir kurumsal şablon değil; `ARCHITECTURE.md`, `Memory_Bank/API_CONTRACT.md`,
> `Memory_Bank/Active_Context.md` ve `Memory_Bank/Progress.md`'den (Faz 10–12.12) çıkarılmıştır.
> Özellikle **"Görünmez Sözleşmeler" (§7)** ve **tekrarlayan hata sınıfları** buraya birebir
> taşınmıştır — bu proje aynı hatayı birden fazla kez üretmiş ve her seferinde not düşülmüş,
> bu checklist o dersleri PR aşamasına çekmek için var.
>
> Kullanım: yeni bir PR'da ilgili bölümü (Backend/Panel/Mobil/DB) süz, uygulanabilir maddeleri
> işaretle. Her satırdaki kod referansı "nerede kontrol edilir"i gösterir.

---

## 1. Genel / Mimari

| Kural | Açıklama | Referans |
|---|---|---|
| Katman yönü ihlal edilmemiş mi? | `Domain ← Application ← Infrastructure ← Api/Web`. `Application` içinden `DbContext`'e dokunulmamalı, yalnız `IUnitOfWork`/`IRepository<T>`. Zaten build'de kırılır ama PR'da neden kırıldığını anlamak için bilinçli kontrol et. | ARCHITECTURE.md §1 |
| Yeni modül eklendiyse **18 adımlı reçete** eksiksiz mi? | Entity → Configuration → DbContext → Migration → Feature (Dtos/Queries/Commands) → Public controller → Admin controller → İzin adı → Panel controller+view → `API_CONTRACT.md` → testler → `ARCHITECTURE.md` modül tablosu satırı. Eksik adım genelde §7'ye yeni bir görünmez sözleşme ekler. | ARCHITECTURE.md §4 |
| `ARCHITECTURE.md` modül tablosu güncellendi mi? | Yeni/değişen/silinen modül varsa tabloya satır eklenmeli/çıkarılmalı — yoksa `ArchitectureDocTests` kırmızı olur (bilerek). PR'da bunu manuel de doğrula. | ARCHITECTURE.md §10.8, `Integration/Architecture/ArchitectureDocTests.cs` |
| DTO alanı **silindi/yeniden adlandırıldı mı** (kırıcı değişiklik)? | Additive serbest; silme/rename için 3 adımlı geçiş planı (ikisini birlikte doldur → istemci geçsin → sonra sil) zorunlu, tek PR'da yapılmamalı. | ARCHITECTURE.md §5 |
| Uç davranışı değişikliği (sıralama varsayılanı, sayfalama şekli, doğrulama sıkılığı) eski istemcileri kırıyor mu? | Varsayılan sıralamayı değiştirmek listeyi sessizce ters çevirir; sayfalamayı ekleyip/kaldırmak `data` şeklini değiştirir (`[...]` ↔ `{items,...}`) ve istemci ayrıştırıcısını patlatır. | ARCHITECTURE.md §5 |

---

## 2. Backend — Domain / Application (CQRS)

| Kural | Açıklama | Referans |
|---|---|---|
| Feature klasörü doğru yapıda mı? | `Features/<Modül>/{Commands,Queries,Dtos}`; handler `IUnitOfWork` üzerinden gider, `DbContext`'e doğrudan dokunmaz. | ARCHITECTURE.md §2 |
| Public listede görünürlük filtresi **controller'da zorlanıyor mu**? | `dto.IsActive = true;` gibi — DTO'dan gelen değere güvenilmez. Onaylı+aktif+silinmemiş+süresi geçmemiş kaydı yalnız public uç döndürür. | ARCHITECTURE.md §4, Değişmez Kural #3 |
| Sayısal alan ayrıştırması **binlik ayracı kabul ediyor mu** (etmemeli)? | `NumberStyles.Number` yerine `AllowLeadingSign | AllowDecimalPoint` kullan — geçmişte `"2020,5"` sessizce `20205` olarak okunmuştu (10 kat sapma). | Progress.md Faz 11.14 gerçek hatası |
| Tarih/saat alanı **"TR günü, 00:00 UTC"** konvansiyonuna mı uyuyor? | `dutyDate`/`eventDate`/`funeralDate` gibi alanlar saat dilimine göre kaydırılmamalı; saat ayrı bir alanda (`TimeSpan`) taşınır. Bu hata sınıfı mobil+backend'de **4 kez** tekrarlamış (11.7/11.10/11.11/11.13). | ARCHITECTURE.md §7 madde 6 |
| Yeni cache'lenen sorgu: **grup sabiti + invalidator var mı**? | Cache grup adı yalnızca `CacheGroups` sabitlerinden olmalı; her grup için en az bir invalidate eden komut şart (tek bilinçli istisna: `dashboard`, 60sn TTL'e dayanır). Yoksa panelde güncellenen veri mobilde sessizce eski kalır — ne log ne istisna. | ARCHITECTURE.md §7 madde 22, `CacheContractTests` |
| `IAuditableCommand` gerekiyor mu? | Yazma/silme/onay/red gibi durum değiştiren komutlarda audit izi (`AuditBehavior`) bekleniyor mu — "kim ne zaman yaptı" panelde/log'da izlenebilir olmalı. | Progress.md Faz 10.9 denetimi |
| Onay/red durum makinesinde eski gerekçe temizleniyor mu? | Örn. `ApproveXCommandHandler` önceki `RejectedReason`'ı sıfırlamalı — unutulursa "Onaylandı" rozeti ile eski red gerekçesi yan yana görünür (ilanlarda düzeltilmiş, kampanyada bir süre unutulmuştu). | Progress.md Faz 11.15b |
| Slug/normalizasyon **tek bir yardımcıdan mı** üretiliyor? | İkinci bir gerçekleme yazma — `SlugHelper` tek sahip olmalı. Türkçe büyük `İ` (U+0130) `ToLowerInvariant()` ile küçülmez, ayrı ele alınmalı; aksi halde aynı ada iki farklı slug (mükerrer kayıt) üretilir. | ARCHITECTURE.md §7 madde 21 |
| ⚠️ **TEKRARLAYAN SINIF** Bir kuralı koruyan **taramanın kapsamı** elle mi tutuluyor? | Tutuluyorsa kural **listeye girmeyende delik**. 12.9'da beşinci CDN, 12.11'de `ExtendMyAdCommand` tam bu yüzden korumasız kaldı: 12.10'un yapısal testi modül listesini *türetiyordu* ama taradığı **dosya adı desenini** (`Update*`/`Approve*`/…) elle tutuyordu ve `ExtendMyAd*` hiçbirine uymuyordu → dosya ham `ad.Status = "approved"` yazarken test yeşildi. 🔑 Doğru soru "testi genişletsem yeter mi?" değil, **"korumayı taramanın erişemeyeceği yere taşıyabilir miyim?"** | ARCHITECTURE.md §7 madde 53 |
| Moderasyon durumu yazan yeni bir yol mu açıyorsun (`CS8852` aldın)? | Alanı `set`'e **açma** — geçişi varlığın metoduna taşı (`Ad.Approve/Reject/Resubmit/Extend` deseni). Alanı açmak derlemeyi düzeltir, testleri yeşil bırakır ve korumayı **sessizce siler**; `ModerationSingleOwnerTests` bunu yakalar ama PR'da bilinçli kontrol et. Yeni moderasyonlu modül eklediysen varlığına geçiş metotları **ve** `init` alanları şart. | ARCHITECTURE.md §7 madde 52–53, `ModerationSingleOwnerTests` |
| Yeni bayrakla kapatılabilen kod yolu (`Fcm:Provider=None` gibi) test ediliyor mu? | Bayrakla kapalı yol = hiç çalıştırılmamış yol; anahtar bağlanır bağlanmaz ilk kez patlayabilir (`FirebaseApp.GetInstance` null döner, fırlatmaz). Her bayrakla kapalı yola en az bir birim testi. | ARCHITECTURE.md §7 kod-dışı sözleşmeler |

---

## 3. Backend — API / Controllers

| Kural | Açıklama | Referans |
|---|---|---|
| Yol **kebab-case** mi? | `SlugifyParameterTransformer` çok kelimeli controller adını çevirir ama elle `[Route]` yazarken de kebab kullan (`/v1/lost-items`, PascalCase 404 verir). | ARCHITECTURE.md §7 madde 12 |
| Admin controller `AdminApiControllerBase`'den mi türüyor + `[RequirePermission]` her aksiyonda var mı? | Yetki politikası tabanda tanımlı; eksik `[RequirePermission]` sessizce daha geniş erişim açabilir. | ARCHITECTURE.md §4 adım 7, Değişmez Kural #4 |
| Yeni izin adı `permissions` tablosuna eklendi mi + panel rollerine dağıtıldı mı? | İzin yoksa moderator 403 alır — "karşılığı olmayan yetki" tuzağının API tarafı. | ARCHITECTURE.md §4 adım 8 |
| Yanıt zarfı (`{success,data,meta}`) korunuyor mu, `meta.traceId` her yanıtta var mı? | İstisna yaratma (announcements'ın 200+success:false quirk'i gibi) — bilinçli değilse zarftan sapma istemciyi kırar. | API_CONTRACT.md §2 |
| Yeni hata durumunda uygun `code` kullanıldı mı (özel string değil)? | `error.code` sözlüğe (`VALIDATION_ERROR`, `CONFLICT`, `NOT_FOUND`...) uymalı; istemci `code`'a göre dallanıyor, mesaj metnine değil. | API_CONTRACT.md §3 |
| Search parametre adı tutarlı mı? | Çoğu modül `search` kullanır, taksi + ulaşım `searchTerm` kullanır — yeni endpoint eklerken hangisi olduğunu bilinçli seç ve dokümante et, yanlış ad **sessizce yok sayılır** (400 gelmez). | ARCHITECTURE.md §7 madde 4 |

---

## 4. Panel (KadirliApp.Web — Razor/MVC)

| Kural | Açıklama | Referans |
|---|---|---|
| Yeni panel controller'ına **hem** `[Authorize(Roles="admin,super_admin,moderator")]` **hem** `[PanelPermission("<modül>")]` eklendi mi? | Rol listesine `moderator` yazıp özniteliği unutmak, moderatöre o modülde **sınırsız** yetki verir — bu proje bunu canlıda yaşadı (11.15b'nin en büyük bulgusu). `PanelModeratorPermissionTests` yakalar ama PR'da elle de kontrol et. | ARCHITECTURE.md §3, Active_Context.md |
| Ekran **yalnız admin'e** açıksa desen farklı — modül anahtarı verilmedi mi? | `[Authorize(Roles="admin,super_admin")]` + `[PanelPermission]` **YOK** + `PanelMenu.Items` satırının `Module`'ü **`null`** + `AdminOnlyControllers`'a controller adı. Modül anahtarı verirsen izin matrisinde moderatöre dağıtılabilen ama rol kapısı yüzünden **asla çalışmayacak** bir yetki belirir — 11.15b'nin "karşılığı olmayan yetki" hatasının tekrarı. Örnek: `StaffAdmin`, `AuditLogsAdmin`, `TrashAdmin`. | ARCHITECTURE.md §3, `PanelAuditLogTests`, `PanelTrashTests` |
| Panelde bir zaman/durum ayrımı **istemcide de** yapılıyor mu? | Yapılıyorsa iki tanım **birebir** aynı olmalı ve testle kilitlenmeli: `PowerOutagePhaseRules` ↔ mobil `PowerOutage.isActive/isUpcoming/isPast` (başlangıç dâhil, bitiş hariç). Ayrışırsa panel "sürüyor" derken vatandaş "planlı" görür ve **kimse hata almaz** — §7 madde 27. | `PanelPowerOutageFilterTests` |
| Soft-delete edilmiş kaydı listeleyen yeni sorguya `IgnoreQueryFilters()` konuldu mu? | Global süzgeç tam olarak o satırları gizler; unutulursa ekran **her zaman boş** görünür ve hata da vermez (çöp kutusu). Geri getirme ayrıca `status`'e **dokunmamalı** — yoksa sil+geri getir ikilisi moderasyonu atlar (§7 madde 28). | `PanelTrashTests` |
| Yeni `IAuditableCommand` eklendiyse `PanelDisplay.AuditAction` sözlüğüne satır atıldı mı? | Denetim izi ekranı ham `AuditAction` değerini basmaz. Sözlük **kaynak taranarak** kilitli (`AuditAction => "…"`), eksik satır testi kırar — ama neyin Türkçesi olacağı insan kararı. | `PanelAuditLogTests.AuditAction_HasTurkishLabel…` |
| `PanelMenu.Items`'a satır eklendi mi? | Menü tek liste; eklenmezse ekran erişilebilir ama menüden görünmez (ölü buton kuralının tersi — "gizli buton"). `StaffAdminController.Modules` ve `PanelDisplay.ModuleLabel()` **buradan türer** — ikinci bir liste yazma. | ARCHITECTURE.md §3 |
| ⚠️ **TEKRARLAYAN** Durum/rol ekrana **ham** mı basılıyor? | `@item.Status` / `@user.Role` doğrudan yazılmaz → `<partial name="_StatusBadge" model="PanelDisplay.Status(...)" />`. 11.15c'de **yedi listede** `expired`/`archived`/`SuperAdmin` ham basıyordu; sebep her görünümün kendi if/else zincirini yazması ve **son `else` dalının** ham değeri geçirmesiydi. | `PanelDisplayTests`, `PanelUsabilityTests` |
| Para `PanelDisplay.TL()`'den mi geçiyor? | `ToString("C2")` **kullanılmaz**: panel `Program.cs`'te bilinçli olarak `InvariantCulture`'a sabit, bu yüzden jenerik **`¤`** basar (canlıda `¤750,000.00` görüldü). | `PanelDisplayTests.TL_UsesTurkishFormat…` |
| Yeni gezinme bağlantısı **dar ekranda** da var mı? | Menü iki yerde çizilir (kenar çubuğu + `<details>` açılır menü) ama **tek listeden** gelir (`_MenuLinks.cshtml`). Kendi `<a>` bloğunu yazarsan dar ekranda görünmez. | `PanelUsabilityTests.NarrowScreen_…` |
| Panelde gösterilen **"aktif/yayında" sayacı** public uçla aynı tanımı mı kullanıyor? | Ayrışırsa panel ile vatandaş **farklı gerçeklik görür ve kimse hata almaz**: 11.15c'de panel "Aktif İlanlar 1" derken `GET /v1/ads` 0 döndürüyordu (`ExpiresAt` yok sayılıyordu). | ARCHITECTURE.md §7 madde 23 |
| Bir kaydı **görünmez kılan/geri getiren** aksiyon, ona bağlı türetilmiş veriyi de düşünüyor mu? | Silinen duyurunun bildirimleri ayakta kalıyordu → kullanıcı bildirime dokunup boş sayfaya düşüyordu. Kaynakta temizle **ve** sorguda "hedefi yaşayan" süzgeci koru; sayaç (`unreadCount`) listeyle **aynı** sorgudan türemeli. | ARCHITECTURE.md §7 madde 24 |
| Yeni Index view'de **sayfalama UI'ı** var mı (yalnız `PagedResult` dönmek yetmez)? | Geçmişte 8 Index view `PagedResult`'a bağlıyken sayfalama arayüzü hiç yoktu → 20. kayıttan sonrasına erişilemiyordu. Ortak `_Pagination.cshtml` kullan, mevcut query string'i koru. | Progress.md Faz 11 denetim oturumu |
| Başarı/hata mesajı **yalnız `_Layout`'ta mı** basılıyor? | `TempData` key'leri harf duyarsız; view içinde ayrıca `TempData["Success"]/["Error"]` basmak çift balona yol açar. Controller'da tek standart key (`Success`/`Error`) kullan. | Progress.md Faz 11 denetim oturumu |
| Yeni yazma aksiyonu **audit izi** bırakıyor mu? | `IAuditableCommand` deseni panelde de geçerli; onay/red/silme gibi aksiyonlarda "kim yaptı" izlenebilir olmalı. | Active_Context.md güvenlik notları |
| Silme/onay gibi geri alınamaz aksiyonlarda onay **neyi sildiğini yazıyor mu**? | `onsubmit="return confirm(...)"` **yazılmaz** → `data-confirm="…"` özniteliği (dinleyici `_Layout`'ta tek yerde). Kaydın adını yaz. ⚠️ Adı inline JS dizesine gömmek kırılgandır: Razor öznitelikleri HTML-encode ettiği için tırnaklı bir başlık (`Ali'nin arabası`) dizeyi bozar; öznitelikte taşınırsa bu sorun yok. | 11.15c |
| Dar ekranda (< 1024px) bu sayfaya ulaşmanın bir yolu var mı? | Panelin kenar çubuğu `hidden lg:flex`; dar ekranda menü tamamen kayboluyor — yeni ekran eklerken bu boşluğu büyütme. | Active_Context.md |
| Varsayılan admin şifresi / hassas bilgi `IsDevelopment()` koşulu **olmadan** ekrana basılıyor mu? | Login sayfasındaki satır 11.15c'de koşula bağlandı (`@inject IWebHostEnvironment Env`); yeni debug/yardım metni eklerken tekrarlama. | 11.15c |
| ⚠️ **TEKRARLAYAN** Yeni **toplu işlem** aksiyonunun adı `…Selected` ile mi bitiyor? | İzin eylemi aksiyon adının **önekinden** türetilir (§7 madde 19). `BulkApprove` hiçbir moderasyon önekiyle eşleşmez ve sessizce **`update`**'e düşer — yalnız düzenleme yetkisi olan moderatör toplu ONAY yapabilir hâle gelir. Bu, 11.15b'nin "karşılığı olmayan yetki" hatasının üçüncü biçimi. | ARCHITECTURE.md §7 madde 29, `PanelBulkActionTests` |
| Toplu işlem, modülün **tek-kayıt komutunu** mu çağırıyor? | Toplu SQL `UPDATE` yazılırsa denetim izi (komut başına düşer), önbellek geçersizleştirmesi ve iş kuralları (ör. onayın süresi dolmuş ilana taze pencere vermesi, §7 madde 25) **hiç çalışmaz**: panel "42 ilan onaylandı" der, mobil hiçbirini göstermez. Kayıt başına hata partiyi durdurmamalı, sayılıp mesajda söylenmeli. | ARCHITECTURE.md §7 madde 29, `PanelBulkActionTests` |
| Yeni **sıralama anahtarı** benzersiz bir ayraçla (`ThenBy(Id)`) bitiyor mu? | Eşit değerli satırlarda Postgres sırayı garanti etmez → sayfalı listede aynı kayıt iki sayfada görünüp bir başkası hiç görünmez (**sessiz veri kaybı**). "Bir ikincil anahtar koymak" yetmez, ayracın benzersiz olması gerekir. Varsayılan anahtar da modülün **eski sırasıyla birebir aynı** kalmalı — değişirse mobil liste sessizce ters döner. | ARCHITECTURE.md §7 madde 30, `PanelSortingTests` |
| Yeni parola girişi/denetimi `PanelPasswordPolicy`'den mi geçiyor? | Kural 11.18 öncesi **üç ayrı handler'da** elle `Length < 6` olarak kopyalanmıştı; politikayı sıkılaştıran biri birini atlarsa o kapıdan zayıf parola girmeye devam eder. Parolayı **sahibi değil yönetici** belirlediyse (`CreateStaff`, `ResetStaffPassword`, seed) `MustChangePassword` işaretlenmeli. | `PanelPasswordSecurityTests` |
| Yetki-hassas bir alan (rol, aktiflik, ban, parola) değiştiren yeni bir yol açtın mı? | Açık oturumlar `OnValidatePrincipal` (`PanelPrincipalValidator`) ile her istekte DB'den tazeleniyor; yeni alan oraya da yansımalı. ⚠️ Parola damgası karşılaştırması **saniyeye yuvarlanır** — çerezin `IssuedUtc`'si RFC1123 ile saklandığı için saniye altını taşımaz; ham karşılaştırma, parolasını değiştiren kişiyi kendi oturumundan atar. | `PanelPasswordSecurityTests` |
| Dar ekranda menü hâlâ açılıyor mu? | Kenar çubuğu `hidden lg:flex`; dar ekranın tek gezinme yolu üstteki `<details>` menüsü. 11.15c öncesinde o buton **bir kabuktu** (`id`/`onclick`/JS yoktu) ve <1024 px'de panelde hiç menü yoktu. | `PanelUsabilityTests` |

---

## 5. Mobil (Flutter)

| Kural | Açıklama | Referans |
|---|---|---|
| Dar sütunda (360dp) veya 1.4 yazı ölçeğinde `Row` **taşması** test edildi mi? | Bu proje aynı `RenderFlex overflow` hatasını **7+ kez** farklı ekranlarda üretti (Pharmacy/Guide/Ad/Favorite/Lookup/Event/Complaint kartları). Metin `Text` yerine `Flexible`/`Expanded`+ellipsis kullan; golden test'e 1.4 ölçek senaryosu ekle. | ARCHITECTURE.md §7 kod-dışı, birden çok Progress.md girdisi |
| Yeni liste/uç provider'ına **`retry: apiRetry`** verildi mi? | Riverpod 3 hatalı provider'ları sınırsız yeniden dener; `apiRetry` yalnız bağlantı/timeout/5xx/429'da ≤2 tekrar yapar. | ARCHITECTURE.md §7 kod-dışı |
| Yeni sayfalı liste **kendi altbilgisini yazıyor mu** (yazmamalı)? | `PagedListFooter` ortak bileşeni kullan — 11 kopya bundan tekilleştirildi. | Progress.md Faz 11.15 |
| Boş/hata durumu (`EmptyView`/`ErrorView`) **kaydırılabilir mi**? | Sarmalamadan direkt kullanma; `ScrollableStateBody` olmadan pull-to-refresh boş/hata durumunda sessizce ölür. | Progress.md Faz 11.15 |
| Form/detay rotası bir listenin **kardeşi mi, alt rotası mı**? | `go_router` iç içe rotada üst ekranı da kurar — form/detay rotaları her zaman **kardeş** olmalı, yoksa arka planda gereksiz istek+diyalog açılır (11.9'da taslak diyaloğu düzenleme ekranının üstüne fırlamıştı). | ARCHITECTURE.md §7 kod-dışı |
| `context.push` ile açılan ekran, durum değiştikten sonra **`addPostFrameCallback` içinde** kapatılıyor mu? | Aksi halde ekran router redirect'inin üstünde asılı kalır (kayıt sonrası kapanmama, hesap silme sonrası sonsuz spinner gibi 3 gerçek hata buradan çıktı). | Progress.md Faz 11.5 |
| ⚠️ **TEKRARLAYAN (4 kez)** Tarih gösteren yeni bir karta (`"3 saat önce"` gibi) **`now` enjekte edilebiliyor mu**? | Enjekte edilemeyen göreli tarih golden testi her gün kırar → insan `--update-goldens`'ı refleks yapar ve testin değeri sıfırlanır. 11.15b'de `AnnouncementTile`+`ComplaintCard`, 11.15c'de `NotificationTile` bu yüzden kırıldı. 🔑 **Referansı yenilemeden önce PNG'nin NE gösterdiğine bak:** `NotificationTile`'ın referansı "20 dakika önce" değil tam tarih basıyordu — yani referansın kendisi **hatalı davranışın çıktısıydı**. | ARCHITECTURE.md §8 |
| Yeni ortak bileşen/liste kartı `test/golden/`'a eklendi mi (uzun Türkçe metinle)? | Kısa örnek hiçbir düzen hatasını göstermez; golden senaryosu **uzun** Türkçe metinle olmalı. | ARCHITECTURE.md §8 |
| Arayüz metni **Türkçe**, hata mesajı kullanıcıya **teknik/İngilizce sızmıyor** mu? | `turkish_ui_test.dart` sözlük eksiksizliğini kaynağı tarayarak kontrol eder — yeni hata koduna karşılıksız metin eklenirse kırılır. | Değişmez Kural #6, ARCHITECTURE.md §8 |
| Yeni buton bir uca/ekrana gidiyor mu (işlevsiz buton yok)? | `app_modules_test.dart` bunu mekanik doğruluyor ama yeni ekran eklerken elle de düşün. | Değişmez Kural #5 |
| ⚠️ **TEKRARLAYAN SINIF** Sunucudan gelen bir alan **eksik ya da boş** olduğunda istemcinin varsayımı kaydı **gizliyor** mu? | Gizlememeli. 12.6'da `schedules[].days` boş/eksikse anlamı **"her gün"**dür (`OperatingDays.fromCodes`): "hiçbir gün" saymak, 12.5 **öncesinden kalan** — yani alanı hiç olmayan — her seferi ekrandan **sessizce silerdi**. Panel saatleri gösterir, vatandaş hiçbir şey görmez, log temizdir. Kural: additive bir alanın **yokluğu**, o alan eklenmeden önceki davranışı vermeli; şüphede kalınca **göstermek** doğru yön. ⚠️ Aynı şey süzmede de geçerli — sunucu elemiyorsa (§7 madde 46) istemci de elememeli, yalnız **bildirmeli**: elense hafta içi çalışan bir hattın kartı Pazar günü **boş** görünürdü. | ARCHITECTURE.md §7 madde 49, `operating_days_test.dart` |
| ⚠️ **TEKRARLAYAN SINIF** Yeni alan **haftanın günü / takvim** ile mi ilgili — ve bu kez **mobilde** mi? | Dönüşümün mobildeki de **tek sahibi** olmalı (`operating_days.dart`). ⚠️ Dart'ın `DateTime.weekday`'i **Pazartesi=1 … Pazar=7** olduğu için projedeki maskeyle *tesadüfen* hizalı — tam bu yüzden tehlikeli: `1 << weekday` ya da `weekday % 7` yazan ikinci bir eşleme **derlenir, çalışır ve günü bir kaydırır**, ekran açılır, hata vermez. Sunucudaki Pazar=0 kaymasının (§7 madde 46) aynası. Testin her yedi günü **tek tek** denetlemesi şart: yalnız Pazartesi'ye bakan bir test kaymayı görmez. | ARCHITECTURE.md §7 madde 49, `operating_days_test.dart` |
| 🐛 Bir öğeyi "geçmiş/bitmiş" gösteren yeni bir stil mi ekliyorsun (üstü çizili, soluk, "kalktı")? | Koşulu **eksiksiz** yaz: 12.6'da "saati geçti" tek başına yetmiyordu, sefer **o gün çalışmıyorsa hiç kalkmamıştır** — üstünü çizmek "bugünkü sefer gitti" demektir ve yalandır. İki *ayrı ayrı doğru* kural (saat geçmişliği + gün maskesi) çarpıştığında hasar sessizdir. Aynı ayrım metinde de var: **"Bugünkü seferler bitti" ≠ "Bugün sefer yok"** (ikincisi canlı emülatör denetiminde bulundu). ⚠️ **Golden bu sınıfı TUTAMAZ:** karşılaştırıcının %0.5 piksel toleransı (anti-aliasing için bilinçli) tek bir üstü çizili hapı yutar — kural `Text.style.decoration`'a bakan **davranış** testiyle ve **iki yönlü** kilitlenmeli (çizilmeyen kadar çizilen de denetlenmeli, yoksa "hiç çizme" gerçeklemesi de yeşil kalır). | ARCHITECTURE.md §7 madde 50, `transport_screen_test.dart` |
| Sayfalı bir listeye yeni bir süzgeç mi ekliyorsun? | Süzme **sunucuda** olmalı, istemcide değil: 20'lik sayfadan 3 kayıt eleyip "17 hat" demek `totalCount`'u ve sonsuz kaydırmayı **yalancı** yapar. Süzgeç ile arama **tek filtre nesnesinde** taşınmalı (`IntercityFilter`/`GuideFilter` deseni) — ayrı tutulurlarsa şeride dokunmak aramayı sessizce düşürür (panelde 12.5'te aynı sebeple tek forma alınmışlardı). ⚠️ İkili bir süzgeç şeridine **"Tümü" ekle**: yalnız `bus`/`minibus` sunulsaydı, sunucuya yarın eklenecek üçüncü bir tip mağazadaki eski sürümlerde **hiçbir süzgeçte görünmezdi**. Boşalan liste **sebebini söylemeli** ("hiç hat yok" ≠ "bu tipte hat yok"). | `transport_screen_test.dart`, `paged_feed.dart` |
| 🔴 **YENİ SINIF (12.14b)** Ertelediğin borç **zamanla kötüleşiyor** mu? | Ayrı bir sınıf: "sonra yaparız" ile "sonra yapamayız" farkı. 12.12 metin arası görselleri hotlink bıraktı — makul bir borçtu; ama görsellerin **%9'u imzalı/süreli** adres (ölçüldü) ve süresi dolduğunda istemci onları *zarifçe gizlediği* için hasarın **hiçbir belirtisi olmayacaktı**: haberler sessizce görselsizleşir, uçlar 200 döner, log temiz kalır. 🔑 Borcu ertelerken sor: *"bu, bekledikçe daha mı pahalı oluyor?"* Öyleyse borç değil **zamanlanmış arıza**dır. | ARCHITECTURE.md §7 madde 63 |
| ⚠️ Dış içeriği **yeniden yazıyor** musun (adres değiştirme, aynalama)? | Sağlama/idempotentlik ölçütü **yeniden yazmadan ÖNCEKİ, kaynağın hâliyle** hesaplanmalı. Aynalanmış gövde kaynağınkine **hiçbir zaman** eşitlenemez → sonrasıyla hesaplansaydı her koşu kaydı "değişmiş" sayar ve **sonsuza kadar** yeniden yazardı: ne hata, ne belirti, yalnız durmadan artan bir sayaç. ⚠️ Yeniden yazma **idempotent** olmalı (kendi çıktısını tekrar işlemeye çalışmamalı) ve başarısız parça **olduğu gibi bırakılmalı** — silmek, "aynalayamadım" durumunda eski davranışa düşmek yerine **bilgi kaybı** olur. | ARCHITECTURE.md §7 madde 63, `NewsSyncTests` |
| ⚠️ Başarısız bir dış çağrıyı **yeniden deniyor** musun — kalıcı mı geçici mi? | Ayrımı **ölç**. İmzalı/süreli bir adresin hatası **kalıcıdır**: "eksik kaldı" bilgisini sağlamaya yazmak, 15 dakikada bir koşan bir senkronda **günde 96 boşuna istek** demekti. Kalıcı hatalarda yeniden deneme kaynağı da bizi de yorar ve hiçbir zaman başarmaz; telafi yolu **elle tetiklenebilen turlu bir iş** olmalı. | ARCHITECTURE.md §7 madde 63, `MirrorNewsBodyImagesJob` |
| ⚠️ Var olan kayıtları da düzeltmen gerekiyor mu (yeni davranış **geriye dönük** mü)? | Senkron/alım yalnız *kaynakta değişeni* yeniden yazıyorsa eski kayıtlar **hiç düzelmez** — ve genelde en eski kayıtlar en çok çürümüş olanlardır. Ayrı, **idempotent ve tur tavanlı** bir geri doldurma şart; ölçüt kaydın **kendi hâli** olmalı (bir bayrak değil), yoksa iş ne zaman biteceğini bilemez. ⚠️ Geri doldurma **sağlamaya dokunmamalı**: dokunursa bir sonraki senkron kaydı "değişmiş" sayıp az önce yazılanı **geri ezer**. | ARCHITECTURE.md §7 madde 63, `NewsSyncTests.Backfill_*` |
| 🐛 Paylaşılan veritabanlı testlerde yeni bir **tekilleştirme** mi ekledin? | Testler artık o anahtarı **paylaşamaz**. 12.14'te gövde görseli tekilleştirmesi `files.metadata` üzerinden koşular arası çalışıyor, `ResetAsync` ise yalnız modül tablolarını boşaltıyordu → iki test aynı görsel adresini kullanınca ikincisi "zaten aynalanmış" yoluna düştü ve indiriciye hiç uğramadı. 🔑 Kırılma **doğruydu**: tekilleştirmenin çalıştığını kanıtlıyordu. Her testin kendi anahtarını üretmesi, temizliği genişletmekten ucuz. | `NewsSyncTests` |
| ⚠️ Mobilde **yazı ölçeğini çarpan** bir ayar mı ekliyorsun? | Çarpım **tavanlanmalı**. Sistem ölçeği 1.4'e (uygulamanın üst sınırı) çıkabiliyor; üstüne 1.3'lük bir tercih 1.82 eder ve ekranın en dar yerleri **hiç denenmemiş** bir ölçekte çizilir — bu projenin yedi kez tekrarlamış taşma sınıfına yeni bir kapı. ⚠️ Ölçeği **her şeye** uygulama: rozetler ve meta satırları düzenin taşıyıcısı; yalnız okunan metne uygula ve bunu kullanıcıya **söyle** (yoksa "neden bütün uygulama büyümedi?" sorusu doğar). | `news_text_scale_test.dart` |
| ⚠️ **YENİ SINIF (12.14)** İstemcide **dış kaynaklı HTML** mi çiziyorsun? | Temizliğin sahibi **sunucudur** (alım anında, `NewsHtmlPolicy`); istemcide **ikinci bir beyaz liste yazma**. İki sahipli bir güvenlik kuralı, ayrıştıkları anda gazetenin yarın kullanacağı bir etiketi **sessizce yutar** — sunucu "gönderdim" der, ekran boş kalır, log temizdir. İstemcinin işi yalnız **stil**. ⚠️ Üç bağ unutulmamalı: `onLinkTap` bağlanmazsa gövdedeki bağlantı **çizilir, tıklanır, hiçbir şey olmaz** ("işlevsiz buton yok"un gövde içindeki karşılığı); `<img>` paketin varsayılanına bırakılırsa **önbelleklenmez** ve hata durumunda **kırık kutu** basar; yükleme sırasında yer tutulursa görsel gelince metin **zıplar**. | ARCHITECTURE.md §7 madde 61, `news_body_test.dart` |
| ⚠️ Aynalanmayan (hotlink) bir dış görsel mi gösteriyorsun? | O görsel **zamanla kırılacak** — ölçüldü: haber gövdelerindeki görsellerin %9'u **süreli** `fbcdn` linki (12.12). Açılmadığında **yer tutucu bile gösterme, tamamen gizle**: paragrafların arasında duran kırık bir kutu, hiç olmayan bir görselden **daha çok** dikkat çeker ve sıfır bilgi taşır. (Kapak görselinde tersi doğru: orası aynalanıyor ve kartın düzeni ona göre kuruluyor.) | ARCHITECTURE.md §7 madde 61 |
| ⚠️ İstemcide **yerel bir liste** mi tutuyorsun (yer imi, "sonra oku", taslak)? | Kaydın **anlık görüntüsünü** sakla, yalnız kimliğini değil: id saklansaydı ekran her açılışta N istek atardı **ve** kaynakta yayından kalkan kayıt listede *"bulunamadı"* satırına dönüşürdü — kullanıcı **neyi kaydettiğini bile göremezdi**. ⚠️ Büyük alanları (gövde) saklama: `SharedPreferences` **bütün dosyayı belleğe alır**. ⚠️ Listeye **tavan** koy (sınırsız büyüyen deponun sorunu yıllar sonra fark edilir) ve **bozuk tek bir satır listeyi düşürmesin** — sürüm geçişinde bozulan bir JSON, kullanıcının 40 kaydını birden götürür. | ARCHITECTURE.md §7 madde 62, `news_screen_test.dart` |
| 🐛 Bir kartta **taşma testi yazdın** ama gerçekten kilitliyor mu? | 12.14'te bozma turunda ölçüldü: `NewsCard`'ın meta satırından `Flexible` kaldırıldı ve test **yeşil kaldı** — gerçek veride o metinler hiçbir zaman satırı taşıracak kadar uzun olmuyor. Kartın asıl regresyon riski taşma değil **sınırsız büyüme**ydi (`maxLines` düşerse gazeteden gelen uzun bir başlık kartı ekran boyuna çıkarır). Kural: taşma testi yazmak yetmez, **bozup kırmızıya döndüğünü gör**; dönmüyorsa test yanlış şeyi kilitliyordur — o zaman ya senaryoyu gerçekten zorlayıcı yap ya da **doğru değişmezi** (ör. `maxLines`/`overflow`) doğrudan iddia et. | ARCHITECTURE.md §8, `news_card_test.dart` |
| ⚠️ Sunucuda **en az uzunluk / eşik** olan bir süzgeci istemciye mi bağlıyorsun? | Eşiği **istemci de bilmeli**. Sunucu 2 karakterin altındaki `search`'ü süzgeç uygulamadan yok sayıyor (400 değil — §5: bir yazım hatası listeyi boşaltmaz); istemci yine de gönderirse kullanıcı **tüm listeyi** görüp süzülmüş sanır. Boşalan/boşalmayan liste **sebebini söylemeli**: *"Arama için en az 2 harf yazın"* ≠ *"sonuç yok"*. | ARCHITECTURE.md §5, `news_screen_test.dart` |
| 🐛 Hedefi bir **sekme (kabuk) rotası** olabilen bir gezinme mi yazıyorsun? | `context.push` **kullanma** — `AppNav.of(context, …)` kullan. `go_router` imperative sayfalara rastgele, `StatefulShellRoute` sayfalarına **`route.hashCode`** anahtarı verir; kabuk en üstte değilken bir kabuk rotası `push` edilirse listeye **aynı anahtarla ikinci bir `ShellRouteMatch`** girer ve `Navigator._debugCheckDuplicatedPageKeys` uygulamayı **çökertir**. 12.2'de canlıda yaşandı, kök nedeni 12.3'te bulundu. ⚠️ En sinsi hâli **push bildiriminin deep-link'i**: hedef `/ilanlar/:id` gibi bir sekme **alt** rotasıysa ve kullanıcı o an bir modül ekranındaysa (kabuk dışı) uygulama dokunur dokunmaz çöker. Kural router'a sorulur, elle rota listesi tutulmaz — liste çürür, çökme sessizce geri gelir. | ARCHITECTURE.md §7 kod-dışı, `shell_page_key_test.dart` |

| 🔴 **YENİ SINIF (12.16)** Bir hata **her koşuda mı** çıkar, yoksa **bir olasılıkla** mı? | Olasılıksa **tek koşuluk bir test onu kilitleyemez**. 12.16'da "eskiyi yürürlükten kaldır + yeniyi yayınla" tek `SaveChanges`'teydi; testler üst üste **üç kez yeşil** koştu ve ölçüldüğünde **8 koşudan 5'i** düşüyordu. Sebep: EF, aynı tablonun UPDATE'lerini **birincil anahtar sırasına** göre gönderir ve anahtarlar `gen_random_uuid()` — yani **rastgele**. 🔑 Böyle bir kilit **tekrarlı** olmalı (`LegalPublishTests`: 10 tur) ve *"bende çalışıyor"* diyen geliştiricinin **haklı** olabileceği unutulmamalı. | ARCHITECTURE.md §7 madde 72, `LegalPublishTests` |
| ⚠️ **Kısmi unique indeks** altında **iki satırı birden** mi güncelliyorsun? | Tek `SaveChanges`'e **sığmaz**. Postgres kısıtı **deyim başına** denetler ve **ertelenemez**: `DEFERRABLE` yalnız *kısıtlarda* var, kısmi unique indeks kısıt olamaz (UNIQUE constraint `WHERE` kabul etmez). İşi ikiye böl **ama tek işlemde tut** — `IUnitOfWork.ExecuteInTransactionAsync`. ⚠️ `BeginTransactionAsync`'i **doğrudan çağırma**: `EnableRetryOnFailure` elle açılan işlemleri reddediyor (bu metot 12.16'ya kadar projede hiç çağrılmamıştı ve çağrıldığı an patlıyordu). | ARCHITECTURE.md §7 madde 72 |
| ⚠️ Geri alınamaz bir aksiyona onay kutusu mu koyuyorsun — `data-confirm` **nerede**? | **Formun** üzerinde, butonun değil. `panel.js`'in dinleyicisi **submit olayında formun** özniteliğine bakıyor; butonda duran bir `data-confirm` onay penceresini **sessizce hiç açmaz** ve aksiyon uyarısız koşar (12.16'da yaşandı, `PanelConfirmDialogTests` yakaladı). ⚠️ Ayrıca **iç içe `<form>` yazma**: tarayıcı içtekini sessizce atar → buton çizilir, tıklanır, hiçbir şey olmaz. Butonu formun dışına alıp `form="<id>"` ile bağla. | `PanelConfirmDialogTests` |
| ⚠️ Bir görünüm **yorumunda** açı parantezli etiket örneği mi veriyorsun? | Verme. `PanelExternalOriginTests` görünümün **ham metnini** tarıyor; bir yorumdaki `<script>` bile "nonce'suz blok" sayılıp testi kırıyor (12.16'da yaşandı). Korumayı gevşetmek yerine **yorumu** düz metne çevir ("betik etiketi"). | `PanelExternalOriginTests` |
| 🔴 **YENİ (12.16)** Bir kaydı **zorunlu** kılan yeni bir kural mı yazıyorsun? | Zorunluluğun **ön koşulunu** da say: 12.16'da *"yayında sürümü olmayan belge zorunlu OLAMAZ"* — sayılmasaydı `IsMandatory` işaretli ama metni yayınlanmamış bir belge **kaydı tamamen kilitlerdi** (istemci gösterecek metin bulamaz, sunucu onaysız kaydı reddeder, uygulama **hiç yeni kullanıcı alamaz**) ve sebep hiçbir ekranda yazmazdı. ⚠️ Tutarsız ayarı kural **yutmamalı**, panel **uyarı olarak göstermeli**. | ARCHITECTURE.md §7 madde 71, `LegalConsentRulesTests` |
| ⚠️ **Kanıt** niteliğinde bir tablo mu açıyorsun (rıza, denetim, hukuki dayanak)? | Refleksle **saklama süresi işi yazma** (§11) — kanıtı süreyle silmek, kanıtı hiç tutmamakla aynı kapıya çıkar. Ve **hesap silme** ile ilişkisini bilinçli seç: kişisel veri **silinir** (12.7 `user_identities`), kanıt **kalır** (12.16 `user_consents`, kullanıcı satırı anonimleşir). FK'lar `Restrict` olmalı ki bir gün yazılacak gerçek bir `DELETE` sessiz veri kaybı değil **hata** üretsin. | ARCHITECTURE.md §7 madde 74 |

---

## 6. Database / Migration

| Kural | Açıklama | Referans |
|---|---|---|
| Migration üretilen SQL **okundu mu** (`dotnet ef migrations add` sonrası)? | Kör `database update` yapılmamalı; kolon tipi/index/ilişki beklenen mi kontrol et. | ARCHITECTURE.md §4 adım 4 |
| Tablo/kolon adları **snake_case** mi? | `Configurations/` altındaki EF yapılandırmasında proje konvansiyonu snake_case. | ARCHITECTURE.md §2 |
| Modül kaldırılıyorsa **tablo düşürülüyor mu** (düşürülmemeli, genelde)? | Soft-delete'li veri varsa tabloyu düşürmek geri dönüşsüz silme demektir. Önerilen: `DbSet`+entity kaldır, tabloyu bırak; gerçekten silinecekse önce `pg_dump` yedeği. | ARCHITECTURE.md §6 |
| Yeni jsonb kolon DTO'da doğru tipte mi temsil ediliyor? | `places.amenities` gibi `jsonb` bir DTO'da `string` olarak modellenmişse yanıt "JSON içeren metin" döner, nesne değil — bilinçli olmalı ve dokümante edilmeli. | ARCHITECTURE.md §7 madde 5 |
| Index gerekliliği analiz edildi mi? | Yeni sık sorgulanan kolon (özellikle filtre/arama alanı) için index düşünüldü mü. | Genel DB pratiği |
| Foreign key / normalizasyon kuralları uygulanmış mı? | Yeni tablo ilişkileri FK ile kurulmalı, gereksiz veri tekrarından kaçınılmalı. | Genel DB pratiği |

---

## 7. Security

| Kural | Açıklama | Referans |
|---|---|---|
| Yeni admin/panel aksiyonu **rol + izin** ikisini birden mi kontrol ediyor? | Bkz. §4 — tek başına rol kontrolü moderatöre sınırsız yetki verebilir. | Active_Context.md |
| ⚠️ **Gerçek parola/anahtar `Memory_Bank/*.md` dosyalarına yazılmış mı?** | Progress.md ve Active_Context.md oturum hafızası gibi kullanılıyor ama **repoda yaşıyorlar ve depo herkese açık.** 11.18'de canlı doğrulamada değiştirilen panel parolası bu dosyalara düz metin yazılıp push edildi. Yazılacak tek şey "değiştirildi" bilgisidir; parolanın kendisi **hiçbir dokümana** girmez. Sızan bir parola geçmişte kalır → **yanmış sayılır, yeniden kullanılmaz.** | 11.18 |
| Hassas veri (telefon, adres, TC vb.) loglanmıyor mu? | `AuditBehavior`/Serilog çıktısına PII sızdırmamaya dikkat. | Orijinal enterprise checklist maddesi, hâlâ geçerli |
| Yeni public yazma ucu rate-limit'e tabi mi? | Anonim POST uçları (complaints, announcements/view gibi) public-write rate limit kullanıyor — yeni anonim uç eklerken aynısını uygula. | Progress.md Faz 10.12 |
| Oturum/cookie iptali düşünüldü mü? | Panel cookie'si 8 saat sabit, `OnValidatePrincipal` yok — silinen/banlanan personelin oturumu hâlâ çalışabilir. Yeni yetki-hassas aksiyon eklerken bu boşluğu büyütme, mümkünse kapatmaya katkı ver. | Active_Context.md güvenlik notları |
| Yeni endpoint **`EndpointAuthorizationSweepTests`** kapsamına otomatik giriyor mu? | Yapısal test `EndpointDataSource`'tan tüm uçları tarar; anonim yazma ucu ekliyorsan beklenen listeyi bilinçli güncelle (kaçak varsa test kırmızı kalmalı, listeye sessizce eklenmemeli). | ARCHITECTURE.md §8 |

---

## 8. Performans / Cache

| Kural | Açıklama | Referans |
|---|---|---|
| Yeni cache'lenen query `ICacheableQuery` mi uyguluyor, anahtar her filtreyle değişiyor mu? | İki farklı sorgu aynı anahtarı paylaşırsa yanlış veri döner. | `CacheContractTests` |
| Redis erişilemezse **fail-open** mı çalışıyor? | `CachingBehavior` cache okunamazsa/yazılamazsa isteği düşürmemeli, handler'a düşmeli — yeni cache kullanan koda bu davranış korunmalı. | KadirliApp.Application/Common/Behaviors/CachingBehavior.cs |
| Bellek/memory'ye BULK data basan bir kurgu var mı? | Varsa sayfalama/streaming'e çevrilmeli; aksi halde out-of-memory riski (bkz. eski `UsersAdmin` filtresiz `ToListAsync()` hatası — panelin en büyük büyüyecek tablosunu filtresiz çekiyordu). | Progress.md Faz 11 denetim oturumu |

---

## 9. Test

| Kural | Açıklama | Referans |
|---|---|---|
| Yeni iş kuralı için birim testi var mı **ve kuralı geçici bozup kırmızı olduğu görüldü mü**? | Testi yazmak yetmez; kuralı bilerek boz, testin gerçekten kilitlediğini doğrula (proje ölçütü bu). | ARCHITECTURE.md §8 |
| Yeni uç görünürlük + "mutlu yol" testiyle kapsandı mı? | Yetki testi yapısal taramayla kendiliğinden gelir; görünürlük (`ModuleVisibilitySweepTests` deseni) ayrı eklenmeli. | ARCHITECTURE.md §8 |
| Mobil yeni ekran: boş/yükleniyor/hata durumu + ana etkileşim test edildi mi? | Minimum kapsam budur. | ARCHITECTURE.md §8 |
| Panel testi `[Collection("panel")]` kullanıyor mu? | Kendi `IClassFixture`'ını açan sınıf süiti dakikalarca uzatır; tüm panel testleri tek container çiftini paylaşmalı. | ARCHITECTURE.md §8 |
| Test sabit `Future.delayed`/gerçek saat kullanıyor mu (kullanmamalı)? | `waitUntil(condition)` kullan; sabit gecikme flaky olur, sabit `DateTime.now()` gece saatlerinde testleri kırar (bu proje bunu en az 3 kez yaşadı). | ARCHITECTURE.md §8 |

---

## 10. Dokümantasyon

| Kural | Açıklama | Referans |
|---|---|---|
| `API_CONTRACT.md` yeni/değişen uç ve DTO alanlarını yansıtıyor mu? | Flutter tarafının tek referansı; `docs/openapi.json` da yenilenmeli. | ARCHITECTURE.md §4 adım 10 |
| Yeni bir "görünmez sözleşme" (koda bakarak anlaşılmayan, bozulunca sessiz hasar veren bağımlılık) doğdu mu? | Doğduysa `ARCHITECTURE.md` §7 tablosuna satır + `InvisibleContractsTests`'e karşılık gelen test eklenmeli. | ARCHITECTURE.md §7 |
| 🔴 **YENİ (Faz A denetimi) — KAPSAM SORUSU** Yazdığın kilit bir **tarama** ya da **elle tutulan liste** mi? | Öyleyse sor: *kapsam **dizinden mi, tipten mi, elden mi** geliyor?* Faz A'da bulunan beş deliğin **dördü** kapsamdı — kilit doğru şeye bakıyordu ama **dar bir kümede**: sıralama süpürmesi 8 haritadan **1'ini** geziyordu; panel dış-origin taraması yalnız `Views/**`'ı okuyordu (aynı bağımlılık `panel.css`'e bir `@import` olarak yazılabiliyordu); moderasyon taraması modül kümesini *türetip* dosyaları `Update*.cs` **deseniyle** buluyordu — yani **12.11'in dersi aynı testin içinde hâlâ açıktı**; kesintinin sınır kuralı panelde kilitliyken **mobil aynada** hiç iddia edilmiyordu. 🔑 Çözüm listeyi büyütmek **değil**: kapsamı `Directory.GetFiles` (dizin), yansıma (tip) ya da var olan bir kayıttan **türet**. | ARCHITECTURE.md §7, `Memory_Bank/Contract_Audit.md` |
| 🔍 **YENİ (Faz 0 denetimi)** Yeni sözleşmenin kilidini `Memory_Bank/Contract_Audit.md`'ye **cinsiyle** yazdın mı? | *"Testi var"* ≠ *"kilitli"*. Tabloya madde numarası, **kilit cinsi** (derleyici/DB kısıtı 🟢🟢 · davranış 🟢 · istemci/elle kapsam 🟠 · kaynak taraması/golden/kuruluma bağlı 🔴) ve **iddianın sözleşmenin hangi yüzünü** tuttuğu yazılır. 🔑 Faz 0'ın bulduğu altıncı hata biçimi tam burada doğuyor: *sözleşme bir modülün adını taşıyor, kilit başka bir modülde duruyor* (madde 26 vefatta ölçülü, **ilanda değil**) ya da *sözleşme dört anahtar sayıyor, test birinin varlığına bakıyor* (madde 16). Kilidin **kapsamını yaz**, adını değil. | ARCHITECTURE.md §7, `Memory_Bank/Contract_Audit.md` |
| `Memory_Bank/Progress.md`'ye oturum özeti düşüldü mü (kararlar + gerçek hatalar + doğrulama)? | Proje bu formatı kronolojik hafıza olarak kullanıyor; büyük değişikliklerde atlanmamalı. | ARCHITECTURE.md giriş tablosu |

---

## 11. Yayın / Platform yapılandırması (Faz 11.16)

> Bu bölümün ortak özelliği: **hataları `flutter run` ile görünmez.** Debug
> build'in kuralları farklı olduğu için buradaki her madde ilk kez *mağazadan inen*
> uygulamada ortaya çıkar. `ARCHITECTURE.md` §7'nin "bayrakla kapalı yol = hiç test
> edilmemiş yol" maddesinin yapılandırma dosyalarındaki karşılığı.

| Kural | Açıklama | Referans |
|---|---|---|
| Yeni bir platform izni gerektiren API kullandın mı? | `image_picker`, konum, mikrofon… her biri **iki yere** dokunur: Android `AndroidManifest.xml`, iOS `Info.plist` kullanım açıklaması. ⚠️ iOS'ta açıklama **yoksa** izin diyaloğu bile görünmeden uygulama **anında çöker** — 11.16'da `NSCameraUsageDescription` tam olarak böyle eksikti (kamera iki ekranda kullanılıyordu). Metin Türkçe ve "neden" sorusunu cevaplamalı; Apple gerekçesiz metni reddediyor. | `release_config_test.dart` |
| Bir izne **dolaylı** mı güveniyorsun? | `INTERNET` ana manifestte yoktu, release'e `firebase_messaging`in manifestinden birleşerek giriyordu. Uygulamanın can damarı bir eklentinin iç detayına bağlıysa, eklenti değişince release **ağa hiç çıkamaz** ve debug'da bu asla fark edilmez. Gereken izni **açıkça** bildir. | `release_config_test.dart` |
| Yeni bir geliştirici/tanılama ekranı eklediysen **rotası** da koşullu mu? | Menü girişini `Env.showDevTools` ile gizlemek **yetmez** — rota tablosunda kayıtlı kaldığı sürece yayın yapısında da açılabilir (deep-link ya da elle adres). 11.16'da `/gelistirici/ag` böyleydi: yedi gerçek uca istek atıp `traceId` basan bir ekran. `GoRoute` kaydının kendisi `if (Env.showDevTools)` bloğunda olmalı. | `release_config_test.dart` |
| Yeni bir **production ayarı** eklediysen kapıya yazıldı mı? | Yayın ayarları bir *kontrol listesi* değil **açılış kapısı**: `ProductionReadinessGuard` Production'da güvensiz ayar bulursa uygulamayı açmaz. Yanlış ayarların ortak özelliği **sessiz** olmaları — uygulama açılır, loglar temiz, uçlar 200 döner. En tehlikelisi `Otp:DevMode`: açık kalırsa login ucu OTP'yi **yanıtta** döndürür ve herkes istediği numarayla girer. | `ProductionReadinessGuardTests` |
| ⚠️ Görsel URL'i **mutlaklaştırma** — `FileStorage:BaseUrl` boş kalmalı | Sezgiye ters olduğu için ayrı satır: görünmez sözleşme #9 görsel URL'lerinin **göreli** dönmesini şart koşuyor, origin'i istemci ekliyor. "Prod domain'i yaz" refleksi mobilde `http://…http://…` üretir ve **hiçbir görsel açılmaz**. Kapı BaseUrl *dolu* ise engelliyor. | `ProductionReadinessGuardTests`, ARCHITECTURE.md §7 madde 9 |
| İmzalama anahtarı olmadan **mağaza artefaktı** üretilebiliyor mu? | Üretilememeli. `bundleRelease` anahtarsız **derlenmez**; `assembleRelease` (APK) debug imzasıyla çalışmaya devam eder (yerel deneme + CI). ⚠️ Gradle'ın `logger.warn`'ı **`flutter build` tarafından yutuluyor** — uyarıya güvenme, kapı koy. | `mobile/android/app/build.gradle.kts`, `secrets/README.md` |
| Marka görseli elle mi düzenlendi? | Düzenlenmemeli — ikon/splash `tool/generate_branding.py` ile **türetiliyor** (renkler tasarım token'larından, harf uygulamanın yazı tipinden). ⚠️ Açılış ekranı ikonla **aynı görsel olamaz**: ikonun zemini marka yeşili, açılış ekranınınki tema rengi → beyaz mark açık zeminde görünmez oldu. Ayrıca Android 12+ logoyu **dairesel maskeye** oturtur, kenardaki öge kesilir. | `tool/generate_branding.py` |

| Yeni **dışa aktarma** ekledin mi? | Dört ayrıntının hepsi `PanelCsv`'de: **UTF-8 BOM** (yoksa Excel Türkçe karakteri bozar), **noktalı virgül ayraç** (yoksa Türkçe yerelde her satır tek sütuna düşer), **formül enjeksiyonu kaçışı** (`=`/`+`/`-`/`@` ile başlayan hücreyi Excel çalıştırır — başlıkları *vatandaş* yazıyor), **tavan aşımında sessiz kırpma değil ret**. Kendi CSV'ni yazma, `PanelCsv`'yi kullan. | `PanelCsvExportTests` |
| ⚠️ Dışa aktarma **tek istekle** mi veri çekiyor? | Çekmemeli. `Pagination.Clamp` panel sorgularını **200 satıra kırpıyor** ve bunu sessizce yapıyor: `Limit = 5000` demek 200 satırlık bir dosyayı "tüm liste" sanmaktır. `PanelCsv.CollectAsync` sayfaları dolaşır. Clamp'i gevşetmek **çözüm değil** (10.7 DoS koruması). | `PanelCsvExportTests.Export_IncludesRowsBeyondTheSinglePageLimit` |
| Dışa aktarma **ekrandaki filtreyi** mi kullanıyor? | Aynı sorgu nesnesi gönderilmeli ve buton mevcut sorgu dizesini **aynen taşımalı** (`_ExportCsvButton` bunu `RouteData`+`Query`'den kendi kurar; controller adını model olarak geçmeyin — kopyala-yapıştırla yanlış modülün listesi indirilir). Ayrı bir sorgu yazılırsa "ekranda 12 satır var, dosyada 400" ayrışması doğar. | `PanelCsvExportTests.Export_RespectsTheCurrentFilter` |
| Yeni bir **hata/olay kaydı** yazan yol mu açtın? | Yazma yolu isteği **düşürmemeli**: `IErrorLogSink.TryWrite` asla fırlatmaz, asla beklemez, kuyruk doluysa olayı düşürür. ⚠️ `catch` bloğunda **senkron DB yazmak** en tehlikeli tasarım — veritabanı çöktüğünde hata yazma denemesi de patlar, istisna `catch`'in İÇİNDE doğar, yanıt zarfı hiç yazılmaz ve istemci **zarfsız ham 500** alır (§7 madde 10 tam da her şeyin kötü gittiği anda kırılır). Ayrıca yazıcının kendi hatası **asla** tabloya gitmez, yoksa DB hatası → kayıt denemesi → DB hatası döngüsü doğar. | ARCHITECTURE.md §7 madde 31, `PanelErrorLogTests` |
| Tekrar eden kayıt **tekilleştiriliyor mu**? | `ErrorFingerprint` GUID/sayı/tarihi maskeler, benzersiz indeks veritabanındadır. Normalize edilmezse `"Ad {guid} bulunamadı"` her istekte ayrı parmak izi üretir ve tekilleştirme **hiç** çalışmaz; tekilleştirme olmazsa tek bir 500 döngüsü tabloyu dakikada on binlerce satırla doldurur. İkisi de **hiçbir hata vermeden** olur. ⚠️ Benzersiz indeks aynı zamanda Api/Web yarışını yakalar — yazıcı `DbUpdateException`'ı güncellemeye çevirmezse o parti sessizce kaybolur. | ARCHITECTURE.md §7 madde 32, `ErrorFingerprintTests` |
| Kayda **istemciden gelen metin** giriyor mu? | O zaman iki kapı birden şart: **kaynak sunucuda sabitlenir** (istemci kendi çökmesini sunucu hatası gibi gösteremesin) ve görünüm `@Html.Raw` **kullanmaz** — Razor kaçışı kalkarsa panelde **depolanmış XSS** olur, yöneticinin tarayıcısında saldırganın betiği koşar. | ARCHITECTURE.md §7 madde 33, `PanelErrorLogTests.ClientSuppliedMessage_IsEscaped…` |
| Yeni kayıt tablosuna **PII sızıyor mu**? | İstek yolu/sorgu dizesi saklanacaksa `SensitiveDataMasker`'dan geçmeli (`phone`, `otp`, `token`, `password`, `email`). OTP akışında telefon sorgu dizesine düşebiliyor; kayıtlar panelde görülüyor, **CSV olarak dışa aktarılıyor** ve 90 gün saklanıyor — "sonra temizleriz" diye bir şey yok. | §7 checklist maddesi, `SensitiveDataMaskerTests` |
| Sınırsız büyüyen yeni bir tablo mu açtın? | Saklama süresi işi (`PurgeErrorLogsJob` deseni) olmadan tablo tek yönlü büyür ve sorun ancak yıllar sonra fark edilir. ⚠️ Ölçüt **son görülme** olmalı, oluşturulma değil: aylar önce açılıp bugün hâlâ tekrar eden kayıt, `CreatedAt`'e bakılsaydı **hâlâ patlarken** silinirdi. | `PurgeErrorLogsJob` |
| Yalnız-admin ekranın komutu bir **`AuditModule`** yazıyor mu? | Yazmak zorunda — ama o anahtar menüde `Module = null` olduğu için `PanelDisplay.ModuleLabel` onu bulamaz ve denetim izi ekranı **ham İngilizce** basar (Değişmez Kural #6). Karşılığı `PanelDisplay.NonMatrixModules`'a eklenmeli. | `PanelErrorLogTests.AuditActionsAndModule_HaveTurkishLabels` |
| Tek modüle ait **olmayan** bir ekran mı ekledin? | O zaman `[PanelPermission]` takılamaz; izin **sorgunun içinde** uygulanır ve controller adı `PanelMenu.PermissionFilteredControllers`'a **bildirilir**. ⚠️ Listeye ad yazmak yapısal testi susturmaya yetmez: her ad için süzmenin gerçekten çalıştığını kanıtlayan bir davranış testi şart, yoksa "içeride süzüyorum" çürüyen bir yorum satırı olur. | `GlobalSearchTests`, `PanelModeratorPermissionTests` |
| Yeni bir kayıt tablosuna **hesaptan türetilen** bir alan mı yazıyorsun (ad, telefon, e-posta)? | ⚠️ Panelin alışılmış deseni `Username ?? Phone`'dur — ve **maskeleme yapan bir tabloda o desen maskelemeyi deler**: kullanıcı adı olmayan bir vatandaş hesabında ham telefon numarası listeye ve **CSV'ye** düşer. 12.2'de tam bu yaşandı: `Identifier` özenle maskeleniyordu, hemen yanındaki "Kullanıcı" sütunu ham numarayı basıyordu. Yedek değer de aynı maskeleyiciden geçmeli. | `PanelLoginAttemptTests.ExportCsv_…_NeverLeaksARawPhoneNumber` |
| Yeni bir güvenlik eşiği, var olan bir eşiğin **ikizi** mi? | Öyleyse ikisi **tek kaynaktan** türemeli ve test bunu kilitlemeli. 12.2'de şüphe kuralı R1'in eşiği `PanelLockoutPolicy.MaxFailedAttempts`'ten geliyor: ayrışsalardı hesap kilitlenir ama **kimseye haber gitmezdi** (kilit yüzünden eşiğe ulaşacak deneme zaten gelemez) ve 11.18'in kilidi çalışmaya devam ettiği için kimse fark etmezdi. | ARCHITECTURE.md §7 madde 35, `SuspiciousLoginRulesTests` |
| Yeni bir **bildirim/uyarı** kanalı mı açtın? | O zaman **kısma (throttle)** zorunlu ve fail-open/fail-closed kararı bilinçli olmalı. Kısmasız bir uyarı, saldırı anında yöneticinin posta kutusuna **kendi kendimize yaptığımız DoS**'a döner: uyarılar filtreye atılır ve gerçek uyarı da o filtreye düşer — sistem çalışır, hata vermez, işe yaramaz. ⚠️ Kanalın kendisi "bayrakla kapalı yol"dur: ilk kez **gerçek bir olay sırasında** koşacaksa yanlış yapılandırmayı en kötü anda öğrenirsin — panele elle tetiklenen bir "kanalı dene" yolu koy. | ARCHITECTURE.md §7 madde 36, `SecurityAlertJob`, `LoginAttemptsAdmin/SendTestAlert` |
| İstemci IP'sine bakan yeni bir kod mu yazdın? | IP'yi **tek bir yer** okumalı (12.2'de `LoginAttemptRecorder`) ve `ForwardedHeaders` ara katmanı pipeline'ın **en başında** kurulmalı. Ters vekil arkasında bu yoksa `RemoteIpAddress` her istekte proxy'nin IP'sidir: IP bazlı kurallar/limitler **herkeste aynı** partisyona düşer ve hata vermeden yanlış çalışır. ⚠️ `KnownProxies`/`KnownNetworks` boş bırakılamaz — istemci kendi `X-Forwarded-For`'unu uydurup kaydı **zehirler**. | `ForwardedHeadersSetup`, `ProductionReadinessGuardTests` |
| Yeni bir çapraz-modül sorgusu **soft-delete**'i düşündü mü? | Global arama silinen kaydı **göstermez** (`IgnoreQueryFilters` yok) — silinen kaydın yeri Çöp Kutusu. Gösterseydi hem "silmiştim ama çıkıyor" karmaşası doğardı hem de sonuçtan düzenleme ekranına giden bağlantı boş sayfaya götürürdü. | `GlobalSearchTests.DeletedRecords_DoNotAppearInSearch` |
| Aynı hedef kitleyi seçen **ikinci bir kod yolu** mu yazıyorsun? | Yazma — hedefleme tek sahipli olmalı (`INotificationDispatcher`). 12.2b'de duyuru üreticisi ile panelin manuel gönderimi aynı metottan geçiriliyor; ayrı yazılsalardı **aynı mahalleye farklı kişi kümesi** giderdi ve iki taraf da hiç hata vermezdi (§7 madde 23'ün sınıfı). ⚠️ Panelin "tahmini alıcı" önizlemesi de **aynı sorguyu** çağırmalı: ayrı sayım "342 kişiye gidecek" der, gönderim 280 satır yazar ve fark hiçbir yerde görünmez. ⚠️ Kullanıcı tercihi (`NotificationPreferences`) elle gönderimde de geçerlidir — "yönetici yolladıysa herkese gitsin" istisnası 10.3'ün tercih ekranını yalancı yapar. | ARCHITECTURE.md §7 madde 38, `PanelPushCampaignTests` |
| Bir alan **terminal** mi (bir kez yazıldı mı geri alınamaz)? | Öyleyse panel onu geri almayı **teklif etmemeli**. `Notification.FcmSent=true` terminaldir: "yeniden gönder" butonu konsaydı **hiçbir şey yapmaz ve kimse hata almazdı** — panelin en sinsi yalan biçimi. Yeniden gönderim yeni kayıt açar; iptal yalnız **henüz yazılmamış/iletilmemiş** satıra dokunur. ⚠️ Butonun görünürlük koşulu **sunucudan** gelmeli (`CanCancel`), görünüm kendi koşulunu yazarsa komutun reddedeceği bir buton çizilir. 🐛 12.2b'de o koşul ilk yazımda "tamamlanmamış" idi ve yanlıştı: *"tamamlandı" ile "geri çekilecek bir şey kalmadı" aynı şey değil.* | ARCHITECTURE.md §7 madde 37, `PanelPushCampaignTests` |
| Yeni bir **sayaç kolonu** mu açtın (sorgu anında `COUNT` yerine)? | O zaman yazımı **artımlıdır ve tek sahiplidir**; "bir kez daha say, düzelir" yolu yoktur. Yazım atlanırsa pano sonsuza kadar eski değeri gösterir: iş yapılır, hiçbir hata oluşmaz ve **yalnız pano yalan söyler**. ⚠️ "Tamamlandı" ölçütünü *işlenen = toplam* diye kurma: 12.2b'de gönderim işi yalnız token'ı olan satırları alıyor, token'sız alıcılar sonsuza kadar bekliyor görünürdü ve **hiçbir kampanya kapanmazdı**. Ölçüt "işlenebilir bekleyen kalmadı" olmalı. 🐛 Ve o kontrol **aynı işlemdeki henüz kaydedilmemiş satırları dışlamalı** — veritabanı onları hâlâ eski hâlleriyle görür. | ARCHITECTURE.md §7 madde 39, `PanelPushCampaignTests` |
| ⚠️ **TEKRARLAYAN SINIF** Bir alanı **türetilmiş** hâle mi getiriyorsun (artık elle değil bir sözlükten/kaynaktan yazılacak)? | O zaman **türetmenin tek sahibi** olmalı ve normalleştirme var olan yardımcıdan geçmeli. 12.3'te `power_outages.neighborhood` serbest metinden sözlüğe bağlandı: kolon **kaldırılmadı ve adı değişmedi** (mağazadaki eski sürümler eşleşmeyi hâlâ ad üzerinden yapıyor), yalnız değeri artık `neighborhoods.name`'den geliyor. İki komutta ayrı ayrı yazılsaydı biri güncellenip diğeri unutulduğunda kayıt **FK'sı dolu ama adı eski** kalır ve panel ile mobil farklı mahalle görürdü. ⚠️ Serbest metni kaynağa bağlayan eşleştirme **`SlugHelper`'dan** geçmeli (§7 madde 21): ikinci bir küçültme yazılırsa Türkçe `İ` yüzünden kayıt **yanlış kaynağa** bağlanır — ve yanlış bağlamak hiç bağlamamaktan kötüdür, çünkü bildirim **başka bir mahalleye** gider ve kimse hata almaz. | ARCHITECTURE.md §7 madde 40, `PowerOutageNeighborhoodMatcherTests` |
| Geri doldurma (backfill) yazıyor musun? | **Migration içinde kör SQL ile yapma.** Üç sebep: (a) eşleştirme kuralı uygulama kodundadır, SQL'e kopyalanırsa ikinci bir gerçekleme doğar; (b) migration bir kez koşar — sözlüğe *sonradan* eklenen bir satır eski kayıtları asla kurtaramaz; (c) 🔑 **eşleşmeyenleri raporlamalı**, yoksa "kaç kayıt hâlâ hedeflenemiyor?" sorusu cevapsız kalır. Adım idempotent olmalı ve **yalnız boş alana** dokunmalı: yöneticinin elle kurduğu bağ, açılışta bir tahminle ezilemez. | `PowerOutageNeighborhoodBackfill`, `PanelPowerOutageNeighborhoodTests` |
| Bir modül **ilk kez otomatik bildirim** göndermeye mi başlıyor? | Var olan bildirim zincirini (`Announcement` + `AnnouncementNotificationGenerator` + `SendPushNotificationsJob` + deep-link) **yeniden kullan**, yeni bir `relatedType` uydurma: §7 madde 18 gereği mağazadaki eski sürümler tanımadıkları türde bildirime dokununca **sessizce hiçbir yere gitmez**. Kaynak kaydın silinmesi duyuruyu **ve bildirimlerini** de götürmeli (madde 24/41), güncelleme **ikinci duyuru üretmemeli** (bir yazım düzeltmesi şehre ikinci push atmamalı) ve gönderim yalnız hedeflenebilir kayıtta mümkün olmalı — panel bunu **buton kapalı + sebep** ile söylemeli, sessizce "gönderildi" dememeli. | ARCHITECTURE.md §7 madde 41–42, `PanelPowerOutageNeighborhoodTests` |
| ⚠️ Bir varlığı `Repository.Query()` ile alıp **değiştiriyor** musun? | `Query()` varsayılan olarak **`AsNoTracking()`** döner — bağlantısız nesnede yapılan değişiklik `SaveChanges`'e **hiç ulaşmaz**. `Update()`/`Remove()` çağırıyorsan EF nesneyi yeniden iliştirir ve sorun görünmez; ama `SoftRemove` gibi **yalnız alan yazan** bir işlem sessizce kaybolur: kayıt "silinmiş görünür", `deleted_at` boş kalır, hiçbir hata oluşmaz. Değiştireceksen `Query(tracking: true)` kullan. | 12.3 canlı bulgusu, `PanelPowerOutageNeighborhoodTests.Delete_…` |
| ⚠️ **TEKRARLAYAN SINIF** Aynı entity'yi DTO'ya çeviren **ikinci bir `Select` bloğu** mu yazıyorsun (liste + detay)? | Yazma — projeksiyon **tek sahipli** olmalı (`EventProjection`). 12.4'te etkinliğin liste ve detay sorguları iki ayrı `Select` bloğuydu ve dört yeni konum alanı yalnız birine eklenseydi **detay ekranı sessizce konumsuz kalırdı**: ne derleyici, ne mevcut test, ne de gözle bakan insan bunu yakalar — çünkü ekran açılır, hata vermez, yalnız bir satır eksiktir. ⚠️ Projeksiyonun EF'e çevrilemeyen bir parçası varsa (etiket üreten saf metot) ifade ağacı **ham alanları** döndürsün, hesap bellekte tek bir `Finish` adımında yapılsın. | ARCHITECTURE.md §7 madde 43, `PanelEventDistrictTests.DetailAndList_…` |
| Bir alanı **türetilmiş** hâle getirirken çıpası bir **veritabanı bayrağı** mı, kod sabiti mi? | Sabit olmalı. 12.4'te `Event.IsLocal`'ın çıpası `DistrictDefaults.HomeSlug` **sabiti**: sözlükteki bir `IsHome` bayrağı olsaydı panelden yanlışlıkla başka bir ilçeye taşınabilir ve o andan sonra yazılan **her** etkinlik sessizce "yerel değil" olurdu. Sabit olduğu için de tersi gerekir: kaynağın kendisi (ev ilçesi satırı) panelden **yeniden adlandırılamaz ve pasifleştirilemez** — komut reddeder, form da sebebini yazar. | ARCHITECTURE.md §7 madde 44, `PanelEventDistrictTests.HomeDistrict_CannotBeRenamed…` |
| Geri doldurman **bir eşleştirme değil, düz bir varsayım** mı ("hepsi X'tir")? | O zaman varsayımın **gelecekte de doğru kalmasını** iki kapı garanti etmeli, yoksa idempotent adım her açılışta yeni kayıtları da ezer. 12.4'te `district_id IS NULL` = "12.4 öncesinden kalma" varsayımı yalnız şu ikisiyle ayakta: alan **zorunlu** (komut reddeder) ve sözlükte **silme yok** (FK'nin `SetNull`'ı tetiklenmez). Biri kalkarsa yöneticinin bilerek boş bıraktığı kayıt sessizce varsayılana çekilir. ⚠️ Tarama `IgnoreQueryFilters` ile yapılmalı: çöp kutusundan geri gelen kayıt aksi hâlde alansız döner. | ARCHITECTURE.md §7 madde 45, `EventDistrictBackfill` |
| İstemcinin **bölge/kapsam** gibi bir kümeyi kendisi mi hesaplaması gerekiyor? | Gerekmemeli — kapsamın tanımı **sunucuda** yaşamalı ve istemci yalnız adını söylemeli (`?locationScope=nearby`). Mobilde "Osmaniye dışı" diye hesaplansaydı, sözlüğe yarın eklenen bir Osmaniye ilçesini **mağazadaki eski sürümler çevre il sayardı** ve liste hata vermeden yanlış olurdu (§7 madde 23'ün sınıfı). ⚠️ Aynı soruyu soran iki parametre yazma: `onlyLocal` ayrı bir süzgeç değil, tek enum'a **çevrilen** bir kısayoldur. Ve bilinmeyen değer 400 değil **varsayılan** olmalı (§5) — bir yazım hatası listeyi boşaltmamalı. | ARCHITECTURE.md §5, `EventLocationScopeTests` |
| ⚠️ Bir sözlük değeri **pasifleştirilebiliyor** mu ve bir kayıt ona FK ile mi bağlı? | O zaman **üç** kural birlikte yazılmalı, ikisi yetmez: (a) pasif değer **yeni olarak** seçilemez, (b) var olan kayıtta **seçili kalır** (düşerse form kaydedildiğinde alan sessizce boşalır/değişir), (c) 🐛 **kaydın düzenlenmesini engellemez.** (a)+(b) tek başına yazıldığında **düzenlenemeyen bir kayıt** doğar: yönetici yalnız başlığı/fiyatı düzeltmek istese bile **hiç dokunmadığı bir alan** için hata alır ve kaydedebilmesinin tek yolu kaydı *başka bir değere taşımak*tır — yani başlığı düzeltmek için konumu değiştirmek. 12.5 canlı denetiminde `EventDistrictResolver`'da (12.4 kodu) yaşandı; aynı şekilli `TransportDeparturePointResolver` aynı anda düzeltildi. Kapıya kaydın **şu anki** değerini geçir. | ARCHITECTURE.md §7 madde 45 ve 48, `PanelEventDistrictTests.EventInADeactivatedDistrict_CanStillBeEdited` |
| 🐛 Tailwind **`peer-checked:`** ile stillenen birden çok kutu **yan yana** mı duruyor? | Her biri **kendi sarmalayıcısında** olmalı. `peer-checked:` **genel kardeş seçicisi** (`~`) üretir: kutular düz kardeşse ilk kutu işaretlendiği anda kendisinden **sonraki bütün** etiketler seçili stilini alır. 12.5'in gün seçicisinde tam bu oldu — "Hafta içi" seçildiğinde Cmt ve Paz da **seçili görünüyordu** ama veride yoktu: panel bir şey söyler, kayıt başka şey der, **kimse hata almaz**. ⚠️ CSS testlenemez; testlenebilen **yapısal kural** "her kutu kendi sarmalayıcısında"dır. | `PanelTransportFieldModelTests.DayPicker_WrapsEachCheckboxSoPeerStylingCannotLeak` |
| ⚠️ **TEKRARLAYAN SINIF** Yeni bir alan **haftanın günü / takvim** ile mi ilgili? | Dönüşümün **tek sahibi** olmalı: .NET `DayOfWeek` **Pazar=0**'dan başlar, projedeki gün maskesi **Pazartesi=1**'den (`OperatingDays`). İki yerde ayrı yazılan bir eşleme **"Salı seferi Pazartesi görünür"** üretir ve ne derleyici, ne test, ne de gözle bakan insan yakalar — ekran açılır, hata vermez, yalnız gün yanlıştır. Bu, §7 madde 6'daki "TR günü, 00:00 UTC" kaymasının (4 kez tekrarlamış) kardeşi. ⚠️ Kodlar/etiketler DTO'ya çıkıyorsa **kontrattır**: yeniden adlandırmak mağazadaki eski sürümlerde günü tanınmaz yapar. | ARCHITECTURE.md §7 madde 46, `OperatingDaysTests` |
| Bir kaydı **görünmez kılabilecek** yeni bir alan mı ekliyorsun ("hiçbir gün", "hiçbir kategori", boş hedef kümesi)? | O değer **yasaklanmalı** ve komut sebebini **söylemeli**. Kabul edilirse panelde *duran* ama mobilde *hiç görünmeyen* bir kayıt doğar: yönetici girdiğini sanır, vatandaş asla göremez, log temizdir. 12.5'te `OperatingDays = 0`, 12.3'te hedefsiz kesintinin bildirimi (§7 madde 42) aynı sınıf. ⚠️ Reddetme kaydı **ezmemeli**: geçersiz gönderim eski değeri korumalı, yoksa "reddettim" derken veri kaybolur. | ARCHITECTURE.md §7 madde 46, `PanelTransportFieldModelTests` |
| Var olan bir listeye **süzülebilir yeni bir eksen** mi ekliyorsun (tip, kapsam, durum)? | Sunucuda **elemekle bildirmek** arasındaki fark kritik: mağazadaki eski sürümler yeni alanı tanımıyor. 12.5'te uç seferleri günlere göre **elemiyor**, yalnız `days` ile bildiriyor — elense eski sürümler için liste **sebepsiz boşalırdı** ve bu bir regresyon olurdu. Kural: **bugünkü doğruluk seviyesini koru**, yeni bilgiyi ek alan olarak ver. ⚠️ Süzgeçte bilinmeyen değer **süzmemeli** (§5); kayıt yolundaki "varsayılana düş" davranışıyla aynı metodu paylaşırsa `?tip=yanlışYazım` sessizce yanlış listeyi verir — iki dönüşüm ayrı olmalı. | ARCHITECTURE.md §7 madde 46–47, `TransportVehicleTypeTests` |
| Alt kayıt (sefer, durak, seçenek) yalnız **ekle + sil** ile mi yönetiliyor? | Alt kayda **ikinci bir alan** eklendiği anda bu yetmez olur: 12.5 öncesinde bir seferin saatini değiştirmenin tek yolu sil+yeniden ekleydi ve gün maskesiyle birlikte bir *düzenleme* denetim izinde **silme** olarak görünürdü. Update komutu yazarken aksiyon adı `Update…` olmalı (izin öneki, §7 madde 19) ve varlık `Query(tracking: true)` ile alınmalı. | ARCHITECTURE.md §7 madde 19, `PanelTransportFieldModelTests.UpdateSchedule_…` |
| ⚠️ Panele **dış origin'den** bir varlık mı ekliyorsun (CDN script/stylesheet/font)? | Ekleme — `wwwroot/lib`'e al. 12.9 öncesinde panel dört origin'den besleniyordu ve bulgu "kozmetik" değildi: `unpkg`'den gelen Leaflet **10 formda** kullanılıyor ve gelmediğinde yönetici **boş bir kutu** görüyor, koordinat seçemiyor, ekranda **hiçbir hata çıkmıyordu**. ⚠️ Bu sınıfın en sinsi yanı **geliştiricinin makinesinde hiç görünmemesi**: internet hep var, panel hep çalışır; sorun ilk kez belediyenin kısıtlı ağında ya da bir CDN kesintisinde doğar (§11'in var olma sebebi). Elle liste tutulmaz — `PanelExternalOriginTests` `Views/**`'ı tarar. 📌 Tek bilinçli istisna **harita kareleri**: "Leaflet gelmedi" seçiciyi **öldürür**, "kareler gelmedi" haritayı gri bırakır ama **koordinat seçimi çalışır** — bu yüzden istisna yalnız CSP'nin `img-src`'ında. | ARCHITECTURE.md §7 madde 51, `PanelExternalOriginTests` |
| ⚠️ **TEKRARLAYAN SINIF** Panelde yeni bir butona/kutuya satır içi `onclick=` / `onchange=` mi yazıyorsun? | Yazma. CSP'de `script-src` için `'unsafe-inline'` **yok**, nonce ise yalnız `<script>` **bloklarını** kapsar — satır içi bir işleyici tarayıcı tarafından **çalıştırılmaz**: buton tıklanır, hiçbir şey olmaz, sunucuda iz kalmaz, yalnız konsolda bir CSP ihlali görünür (yöneticinin bakmadığı yer). Bu, "işlevsiz buton yok" kuralının panel karşılığı. Davranışı `wwwroot/js/panel.js`'teki **delege dinleyiciye** (`data-*` kancası) ya da görünümün kendi **nonce'lu** bloğuna koy. 🐛 `onsubmit="return f()"`'yi taşırken dikkat: `addEventListener` içinde **`return false` hiçbir şey yapmaz**, iptal **`preventDefault()`** ile olur — atlanırsa doğrulama sessizce devre dışı kalır. 12.9'da **47** işleyici taşındı. | ARCHITECTURE.md §7 madde 51, `PanelExternalOriginTests.NoView_UsesAnInlineEventHandler…` |
| 🐛 Panelde bir Tailwind sınıfını **C# tarafında** mı üretiyorsun (rozet/buton rengi)? | O zaman dosyanın `tailwind.config.js`'in `content` listesinde olması **şart**. CDN sürümü tarayıcı-içi JIT olduğu için sınıfın nerede yazıldığı hiç önemli değildi; derlenmiş Tailwind yalnız **gördüğünü** üretir. Bu projede rozet renkleri `PanelDisplay.cs`, `PowerOutagePhase.cs` ve `BulkToolbarViewModel.cs`'te yaşıyor — `content` yalnız `Views/**` olsaydı panelin **bütün durum rozetleri sessizce renksiz** kalırdı ve ne derleyici, ne test, ne log söylerdi. ⚠️ Testin sınıf listesi **elle tutulmaz, türetilir**: elle seçilen dört sınıftan ikisi meğer görünümlerde de geçiyordu, yani `content` bozulsa bile yeşil kalırlardı. | `PanelExternalOriginTests.CompiledCss_ContainsBadgeClasses…` |
| Türetilmiş ama **commit edilen** bir çıktı mı ürettin (`panel.css` gibi)? | O zaman **sürüklenme kapısı** şart: kaynak değişip çıktı yenilenmediğinde hiçbir şey hata vermez, yalnız o öğe stilsiz kalır. CI çıktıyı yeniden üretip `git diff` ile karşılaştırır (`.github/workflows/dotnet.yml`). ⚠️ Çıktı depoda durmalı — depoyu klonlayan biri `npm install` yapmadan paneli açabilmeli; npm adımı **geliştirici makinesinde zorunlu değil**, CI'da doğrulanır. ⚠️ `asp-append-version="true"` unutulmamalı: içeriği değişen ama adı değişmeyen dosya yöneticinin tarayıcısında günlerce eski kalır. | `.github/workflows/dotnet.yml`, `PanelAssetGuard` |
| Yayın için **çalışma anında** doğrulanacak bir şey mi var — kaynağı mı denetliyorsun? | Kaynak dosyaları çalışma anında taramak **yalan söyleyebilir**: Razor görünümleri derlenip assembly'ye gömülüyor, yayında `.cshtml` bulunması garanti değil — dosya tarayan bir kapı **sıfır dosya bulur ve yeşil geçer**. Denetimi ikiye ayır: kaynak → **yapısal test** (derleme zamanı), çalışma anı → **gözlenebilir** olan (`PanelAssetGuard`: türetilmiş varlıklar yerinde mi). ⚠️ "Var mı" yetmez, "boş mu" da sorulmalı — yarıda kesilen bir derleme 0 baytlık dosya bırakır. | `PanelAssetGuard`, `PanelExternalOriginTests` |
| ⚠️ **TEKRARLAYAN SINIF** Bir kaydın **durumunu** yazan İKİNCİ bir yol mu açıyorsun (form alanı, toplu SQL, admin API gövdesi)? | Açma — geçişin **tek sahibi** olmalı. 12.10'da panelin Düzenle formundaki durum menüsü tam bu ikinci yoldu ve **üç kuralı birden** atlıyordu, üçü de sessizce: (a) modülün onay kuralları (#25 taze pencere, bayat red gerekçesinin temizlenmesi, `ApprovedBy`/`ApprovedAt`) — gerçek Postgres'te süresi dolmuş bir ilan bu yoldan "onaylanıp" mobilde **hiç görünmüyordu**; (b) izin matrisi, çünkü izin eylemi aksiyon adından türer (§7 madde 19) ve `Edit` → `update`'e düşer → **yalnız düzenleme yetkisi olan moderatör moderasyon kararı verir**; (c) denetim izi, kararı ya hiç ya da `update` olarak yazar. ⚠️ Alanı DTO'dan **silme** (§5 kırıcı) ama **sessizce de yutma**: farklı bir değer gelirse komut **reddetmeli ve sebebini söylemeli** — hiçbir şey yapmayan bir buton, işlevsiz butondan kötüdür (§7 madde 37). ⚠️ Guard ilk yazmadan **ÖNCE** çağrılmalı, yoksa reddedilen istek kaydın diğer alanlarını yine de ezer. | ARCHITECTURE.md §7 madde 52, `ModerationSingleOwnerTests`, `PanelModerationOwnershipTests` |
| 🔑 Bir yolu **kapatırken**, o yolun tek taşıyıcısı olduğu bir işlevi de siliyor musun? | Silmemelisin — kapatmadan önce karşılığını **yaz**. 12.10'da vefat modülünde durum menüsü aynı zamanda **reddetmenin ve arşivlemenin TEK yoluydu**; menü karşılıksız kaldırılsaydı bir hatayı düzeltirken iki işlev silinmiş olurdu (`RejectDeathNoticeCommand` + `ArchiveDeathNoticeCommand` bu yüzden doğdu). ⚠️ Yeni aksiyonun **adı** izin eylemini belirler: `Archive` öneki 12.10'da moderasyon listesine eklendi, yoksa POST olduğu için sessizce `update`'e düşer ve yayından kaldırma kararı düzenleme yetkisine açılırdı. | ARCHITECTURE.md §7 madde 19 ve 52, `PanelModeratorPermissionTests` |
| 🐛 Panelde bir `<form>`'un **içine** ikinci bir aksiyon butonu mu koyuyorsun? | HTML'de form **iç içe olamaz** — tarayıcı içteki `<form>`'u sessizce atar ve buton hiçbir şey yapmaz. Hedefi buton üzerinde `formaction` ile değiştir (`_ModerationStatusField` deseni) ve `formenctype="application/x-www-form-urlencoded"` ver: Düzenle formları `multipart/form-data` ve override olmasaydı "Onayla"ya basmak henüz kaydedilmemiş fotoğrafı da yükleyip çöpe atardı. ⚠️ `data-confirm` o butonda **çalışmalı**: dinleyici 12.10'a kadar yalnız formun özniteliğine bakıyordu, buton üzerindekini **sessizce yok sayıyordu** (`e.submitter` bu yüzden eklendi). ⚠️ Kullanıcıya "bu işlem formu kaydetmez" demeyi unutma — yoksa yönetici başlığı düzeltip Onayla'ya basar ve düzeltmesi sessizce kaybolur. | `PanelConfirmDialogTests`, `_ModerationStatusField` |
| 🐛 Panelde **daha önce hiç kullanılmamış** bir Tailwind sınıfı mı yazıyorsun (`bg-gray-600` gibi)? | Derlenmiş CSS'i **yenile** (`npm run build`) ve çıktıyı commit et. 12.10'un canlı denetiminde yeni "Arşivle" butonu `bg-gray-600` kullanıyordu; sınıf `panel.css`'te olmadığı için buton **beyaz üstüne beyaz** çizildi: DOM'da var, tıklanabilir, ölçüleri doğru — **insan gözüyle yok**. 12.9'un "C#'ta üretilen rozet sınıfı" maddesinin kardeşi; ikisi de derlenmiş Tailwind'in yalnız **gördüğünü** üretmesinden doğuyor. CI sürüklenme kapısı bunu yakalar, ama PR'da elle de yenile. | `.github/workflows/dotnet.yml`, `PanelExternalOriginTests` |
| ⚠️ **YENİ SINIF (12.12)** Bir **dış kaynaktan** veri mi alıyorsun (başka birinin API'si)? | O zaman üç hasar sınıfı birden doğar ve üçü de **sessizdir**: (a) **kaynak susabilir** — senkron durursa uygulama eski veriyi göstermeye devam eder, uçlar 200 döner, log temizdir → "son başarılı koşu" damgası + eşik (`NewsSyncHealth`) **zorunlu**; (b) **kaynak panelin yaptığını ezer** → kaynağın alanları ile yöneticinin alanları **ayrı kolon** ve ikisi de `init` (ihlal `CS8852`); "kilit bayrağı" reddedildi çünkü koruma *senkron kodunun kilidi kontrol etmesine güvenmek* olurdu; (c) **kaynakta silinen kayıt bizde sonsuza kadar yaşar** → `modified_after` silmeyi hiç bildirmez, ayrı bir **mutabakat işi** şart ve o iş kaynak **boş liste** döndürdüğünde durmalı (yoksa tek bir kesinti bütün arşivi düşürür). | ARCHITECTURE.md §7 madde 54–57, `NewsSyncTests` |
| ⚠️ **TEKRARLAYAN SINIF (5. kez)** Dış kaynağın tarih parametresi **hangi saat diliminde**? | Ölçmeden yazma. WordPress'in `modified_after`'ı **site-yerel** (UTC+3) karşılaştırıyor: canlıda `10:11:36` (yerel) 0 sonuç, `07:11:36` (UTC) 4 sonuç verdi. 🔑 **Yön kritik**: pencereyi geniş tutmak mükerrer okuma üretir ve upsert idempotentse **zararsızdır**; ters yön her koşuda **3 saatlik veriyi sessizce atlar**. Dönüşümün **tek sahibi** olmalı (`WordPressTimeWindow`) ve gidiş-dönüş testle kilitlenmeli — bu, "TR günü 00:00 UTC" tuzağının (4 kez tekrarladı) beşinci biçimi. ⚠️ Sayfalı bir arşivi **sayfa numarasıyla** taramak da aynı sınıf: koşular arasında eklenen tek bir kayıt bütün sayfaları kaydırır ve sınırdaki kayıt **sonsuza kadar atlanır** → tarih imleci kullan. | ARCHITECTURE.md §7 madde 54, `WordPressTimeWindowTests` |
| Dış kaynaktan gelen **HTML** mi saklıyorsun? | Temizlik **alım anında** yapılır, gösterim anında değil: veritabanında temiz olmayan bir gövde durduğu sürece **her yeni ekran** yeni bir XSS yüzeyidir. Beyaz liste kütüphanenin varsayılanı değil **senin** kararın olmalı (`NewsHtmlPolicy` — Application'da, testle kilitli) ve `script`/`style` **içeriğiyle** atılmalı (yalnız etiketi atmak sayfaya JS/CSS metnini düz yazı olarak basar). ⚠️ Temizleyici **tek kapı değildir**: panelde `@Html.Raw` yine yasak, istemci ikinci bir beyaz liste yazmaz. | `NewsHtmlSanitizerTests`, ARCHITECTURE.md §7 madde 33 |
| Dış görseli **aynalıyor** musun (indirip kendi depona yazmak)? | Aynalamanın üç kapısı var: **boyut tavanı** (hem `Content-Length`'e hem gerçekten okunan bayta bak — başlık yalan söyleyebilir), **`Content-Type` denetimi**, **zaman aşımı**; ve indirici **asla fırlatmamalı** — görselsiz bir kayıt, hiç inmemiş kayıttan iyidir. Aynı URL ikinci kez indirilmemeli (yoksa `uploads/` mükerrer dosyayla şişer ve sorun yıllar sonra fark edilir); **koşu içi** tekilleştirme ayrıca gerekir çünkü aynı partideki ikinci kayıt henüz yazılmamış dosyayı göremez. ⚠️ Uçlar yine **göreli** URL dönmeli (§7 madde 9). | `NewsSyncTests`, `HttpNewsImageDownloader` |
| Toplu yazan bir alım hattı mı yazıyorsun (`SaveChanges` bir partiyi birden yazıyor)? | O zaman "kayıt başına hata partiyi durdurmamalı" (§7 madde 29) **kendiliğinden geçerli değildir**: kolon tavanını aşan tek bir alan, hatayı **kayıt başına değil batch başına** doğurur ve o koşuda **hiçbir kayıt inmez**. Kolon sınırlarını yazmadan önce uygula (`NewsColumnLimits`). ⚠️ Koşu defteri de zehirlenir: aynı `DbContext` üzerinden ikinci `SaveChanges` yine patlar ve koşu satırı sonsuza kadar "çalışıyor" görünür → son çare **`ExecuteUpdate`** (değişiklik izleyicisine dokunmaz). | `NewsSyncTests.OversizedSourceFields_…` |
| Yeni bir satır, aynı `SaveChanges` içinde eklenen **başka bir satıra FK ile** mi bağlanıyor? | Bağ **gezinme özelliğinden** kurulmalı (`Campaign = campaign`), FK skalerinden değil. 🐛 12.2b'de tam bu tuzağa düşüldü: `Id` kolonları `gen_random_uuid()` varsayılanıyla tanımlı olduğu için EF onları **store-generated** sayıyor ve değer ancak INSERT'ten sonra geri dönüyor — yani `parent.Id` o anda hâlâ `Guid.Empty`. Bütün alt satırlar var olmayan bir kayda bağlandı ve FK ihlaliyle patladı. | `NotificationDispatcher`, `PanelPushCampaignTests` |
| ⚠️ **TEKRARLAYAN SINIF (12.13)** Bir kaydı görünmez kılan yeni bir eksen mi ekliyorsun (dışlama, gizleme, kapsam)? | Semantiği **ölçüye** göre seç. 12.13'te kategori görünürlüğü **DIŞLAMA** olarak kuruldu, "en az bir görünür kategori" (OR) olarak değil: ölçüm bir haberin **çoklu kategoride** olduğunu gösterdi (`[49,51,52]`) ve OR semantiğinde "E-Gazete"yi kapatmak **hiçbir işe yaramazdı** — yönetici anahtarı çevirir, haberler görünmeye devam eder (§7 madde 37'nin *"panelin en sinsi yalan biçimi"*). ⚠️ Anahtarın **önizlemesi** olmalı ve sayı **gerçek görünürlük sorgusundan** gelmeli (12.2b'nin `EstimateRecipients` dersi); ters yön de aynı özenle sayılmalı — "kaldırırsam kaç tanesi geri gelir" sorusunun cevabı, **başka bir dışlanmış kategorisi olanları** saymaz. | ARCHITECTURE.md §7 madde 59, `PanelNewsTests` |
| 🔴 **GÜNCELLENDİ (Faz 0 — B6)** Yeni panel yazma aksiyonunun adı **ne olduğunu söylüyor mu**? | Artık bir kapı var: `EveryWriteAction_SaysWhatItIs_InsteadOfSilentlyFallingBackToUpdate` matris controller'larının POST aksiyonlarını **yansımayla türetir** ve adı hiçbir önekle eşleşmeyeni **kırmızıya** çevirir. Üç seçenek, üçü de bilinçli: (a) önekli adlandır, (b) öneki `PanelPermissionFilter.ActionFor`'a ekle, (c) `update` gerçekten doğruysa testteki `deliberateFallbacks` listesine **gerekçesiyle** yaz. 🔑 Tuzak buraya kadar **altı kez** tekrarladı (BulkApprove 11.18 · Archive 12.10 · Unarchive 12.13 · SendNotification 12.15 · ResetOverrides ve Feature — son ikisi bu testin **ilk koşusunda** bulundu). Değişen şey listenin içeriği değil **varsayılanın yönü**: eskiden sessizce `update`, şimdi kırmızı. | ARCHITECTURE.md §7 madde 19, `PanelModeratorPermissionTests` |
| 🔴 Yeni bir aksiyon adı **var olan bir önekin `Un…` hâli** mi (`Unarchive`, `Unpublish`, `Unlock`)? | Öneki **elle ekle**. Eşleşme baştan yapılır: `Archive` öneki `Unarchive`'ı **yakalamaz** → POST olduğu için sessizce `update`'e düşer ve *yayından kaldırmak `approve` isterken yayına döndürmek `update` ile yapılabilir*. Listede `Unverify`/`Unban` çiftleri zaten var — deseni takip et, bozma. §7 madde 19'un üçüncü tekrarı (`BulkApprove` 11.18, `Archive` 12.10, `Unarchive` 12.13). | ARCHITECTURE.md §7 madde 19 ve 58, `PanelNewsTests` |
| 🔴 Uzun sürebilen bir işi panelden mi tetikliyorsun? | **İstek içinde koşturma, kuyruğa at.** Koşu dakikalarca sürerse panelin (ve vekilin) zaman aşımı dolar, yönetici F5'ler ve **ikinci koşu** başlar — yani engellemek istediğin şeyi butonun kendisi üretir. ⚠️ Eşzamanlılık kilidi **veritabanında** olmalı (kısmi unique indeks); Redis bu projede bilinçli olarak fail-open (§7 madde 36), yani tam yarış anında kilidi açar. 🔴 **Kilit ile KURTARMA birlikte yazılır:** süreç öldürülürse (deploy, OOM) satır sonsuza kadar "çalışıyor" kalır ve kilit **bütün gelecek koşuları** engeller — hiçbir hata vermeden. Bir "takılmış koşuyu kapat" adımı olmadan koruma, önlemeye çalıştığı arızanın sebebine dönüşür. ⚠️ Buton kapalıyken **sebebini yazmalı** ve koşul **sunucudan** gelmeli (`CanTrigger`); mesaj "başlatıldı" değil **"kuyruğa alındı"** demeli — söylediğin şey bildiğin şeyden fazla olamaz. | ARCHITECTURE.md §7 madde 60, `PanelNewsTests` |
| ⚠️ **TEKRARLAYAN SINIF (12.13)** Bir performans bulgusunun **sebebini** yazarken ölçtün mü? | 12.12 sonrası denetim *"`Contains` sağlayıcıda `strpos`'a çevrilir, hiçbir indeks karşılayamaz"* diyordu; `ToQueryString()` ile bakıldığında Npgsql'in `Contains`'i de `lower(...) LIKE @p` ürettiği görüldü — **sebep yanlıştı, sonuç doğruydu** (eksik olan trigram indeksiydi, btree `LIKE '%x%'`'i karşılayamaz). 🔑 Bu projede yanlış bir sebep, yanlış bir düzeltmeden **daha pahalıdır**: bir sonraki okuyan onu doğru kabul edip başka bir yerde uygular. Sorgu şekli iddiaları `ToQueryString()`, indeks iddiaları `EXPLAIN` ile **ölçülmeli**. | `PanelNewsTests.TheSearchQuery_ProducesALikeShape…` |
| 🔴 **YENİ SINIF (12.15)** Bir eylem **geri alınamaz bir dış etki** mi üretiyor (push, e-posta, SMS, ödeme)? | O zaman "bir kez" kuralı **üç katmanda** yaşamalı ve üçü de gerekli: **görünüm** (buton yerine bağlantı), **komut** (durumu okuyup reddeder) ve **veritabanı** (kısmi unique indeks). İlk ikisi bir **YARIŞI yakalayamaz**: dış etkiyi tetiklemek ile "yaptım" işaretini yazmak genelde aynı `SaveChanges` içinde değildir — iki eşzamanlı istek ikisi de "yapılmamış" görür. ⚠️ İşaret **terminal** olmalı ve ikinci yazma girişimi kaydı **sessizce ezmemeli**: ezerse ilk etkinin izi (kampanya kimliği) kaybolur ve *iki kez gönderildiği* hiçbir yerde görünmez. ⚠️ İndeksin kapsamını **dar tut**: aynı tabloyu paylaşan başka kaynaklarda ikinci kayıt meşru olabilir ve genel bir unique indeks o yolu sessizce kapatır. 📌 12.13'ün "koruma ile kurtarma birlikte yazılır" kuralı burada **gerekmez** — terminal bir satırın yarıda kalmış hâli yoktur. | ARCHITECTURE.md §7 madde 64, `PanelNewsNotificationTests` |
| ⚠️ **TEKRARLAYAN SINIF** Bir kaydı "gönderilebilir / yayınlanabilir" sayan yeni bir koşul mu yazıyorsun? | Görünmezliğin **kaç ekseni** olduğunu say — plandaki liste eksik olabilir. 12.15'in planı koşulu *"arşivlenmemiş + kaynağı yayında"* diye yazıyordu; haberin **üçüncü** bir görünmezlik ekseni daha var (dışlanmış kategori, §7 madde 59) ve o kayıt panelde **"Yayında"** görünüyor. Bildirimi gönderilseydi vatandaş bildirimi alır, dokunur ve **boş sayfaya** düşerdi (§7 madde 24; 11.15c'de duyurularda yaşandı). 🔑 Koşulu **görünürlük sorgusunun kendisiyle** ölç, bellek kopyasını yeniden yazma — ayrıştıkları an panel bir şey der, vatandaş başkasını görür. | ARCHITECTURE.md §7 madde 65, `NewsNotificationRulesTests` |
| ⚠️ Bir bildirimin **deep-link'i eski sürümlerde çalışmıyor** mu (yeni `relatedType`)? | O zaman gövde **kendi kendine yeterli** olmak zorunda. §7 madde 18 gereği tanınmayan tür mağazadaki sürümlerde **hiçbir yere gitmez** ve hata da vermez; gövdesi *"Detay için dokunun"* diyen bir bildirim o kullanıcılara **yalan söyler** — dokunur, hiçbir şey olmaz, elinde hiçbir bilgi kalmaz. Gövde her koşulda bilgi taşımalı ve **asla boş olamaz** (boş gövdeli mesajı kimi cihaz hiç göstermez → bildirim sessizce buharlaşır). ⚠️ Metnin **tek sahibi** olmalı ve panelin önizlemesi **aynı metni** üretmeli: yönetici okuduğu metni onaylamış olsun. | ARCHITECTURE.md §7 madde 66, `NewsNotificationTextTests` |
| 🐛 EF **aynı kolon kümesine ikinci bir indeks** mi ekliyorsun? | Adlı aşırı yüklemeyi (`HasIndex(new[]{...}, "ModelAdı")` **+** `HasDatabaseName`) kullan. Aksi hâlde EF ikisini **aynı indeks** sayar ve öncekini **sessizce ezer**: 12.15'te üretilen migration, duyuru idempotency'sinin dayandığı lookup indeksini **DROP** ediyordu — ne derleyici ne test söylerdi, yalnız büyüyen bir tablo tam taranmaya başlardı. 🔑 Bu maddeyi yakalayan şey bir test değil, **üretilen SQL'i okuma** kuralıdır (§6). | ARCHITECTURE.md §7 madde 64, `PanelNewsNotificationTests` |
| 🔴 **YENİ SINIF (12.15b)** Yeni bir bildirim **kaynağı** mı ekliyorsun (yeni bir push türü)? | Kullanıcının o kaynağı **ayrı kapatabilmesi** gerekir, yoksa var olan bir eksene bindirirsin ve iki yönlü sessiz hasar doğar: (a) o ekseni kapatan kullanıcı yeni kaynağı da kaybeder ve **ayar ekranı bunu söylemez**; (b) yeni kaynağı istemeyenin tek çıkışı eski ekseni kapatmaktır — 12.15b'de bu, *haber push'u istemeyenin **kesinti bildirimini** kaybetmesi* demekti (§7 madde 41). 🔑 Eşleme **tek sahipli** olmalı (`PushPreferenceTopics`), çünkü aynı cevabı gönderim **ve** panelin alıcı önizlemesi verir; önizleme kaynağı söylemezse "342 kişiye gidecek" der, 280 satır düşer (§7 madde 38). ⚠️ Bilinmeyen kaynak **bugünkü davranışa** düşmeli, "süzme"ye değil — §5'in tersi ve bilinçli: burada bedel, tercihini kapatmış birine bildirim göndermek. | ARCHITECTURE.md §7 madde 67, `NotificationPreferenceAxisTests` |
| 🔴 **YENİ SINIF (12.15b)** JSON kolonda saklanan bir nesneye (`OwnsOne(...).ToJson()`) **yeni alan** mı ekliyorsun? | **Varsayılan başlatıcıya GÜVENME — ölç.** `public bool X { get; set; } = true;` yazmış olmana rağmen EF'in JSON materyalizasyonu başlatıcıyı **çalıştırmaz**: mevcut satırlarda anahtar olmadığı için alan `false` okunur. 12.15b'de canlıda ölçüldü (13 satırın 13'ünde anahtar yoktu) ve bir test bunu yakalamasaydı **bütün kullanıcılar** yeni bildirim ekseninden sessizce çıkmış olacaktı — uçlar 200 döner, panel kampanyayı yine açar, tek belirti "kimse almıyor" olur. 🔑 Çözüm bir **geri doldurma migration'ı** (`'{"X": true}'::jsonb \|\| mevcut` — sağ operand kazanır, yani açık tercih ezilmez; `WHERE NOT (… ? 'X')` ile idempotent). ⚠️ Ve ölçümü **teste çevir**: migration bir kez koştuğu için "geri doldurma çalıştı" iddiası yeniden kullanılan bir test veritabanında **kilitlenemez** (bozma turunda yeşil kaldı) — kilitlenebilen şey EF'in davranışının kendisidir. | ARCHITECTURE.md §7 madde 67, `NotificationPreferenceAxisTests.MissingJsonKey_MaterialisesAsFalse` |
| 🐛 `ExecuteSqlRaw` gövdesine **JSON literali** mi yazıyorsun? | Yazma — parametre olarak geçir (`CAST({0} AS jsonb)`). Gövde `string.Format` gibi okunuyor ve JSON'un `{` karakteri **yer tutucu** sanılıyor: `FormatException`. 12.15b'de aynı tuzağa **iki kez** düşüldü; refleks güçlü. | `NotificationPreferenceAxisTests` |
| 🐛 Yeni testin **kullanıcı/kayıt satırı bırakıyor** mu? | Bırakmasın. Panelin listeleri **sayfalı** ve biriken test satırları ilgisiz testleri kaydırır: 12.15b'de dört yeni test kullanıcısı, seed'deki süper admini kullanıcı listesinin ilk sayfasından düşürdü ve `PanelUsabilityTests` kendisiyle ilgisiz bir sebeple kırmızıya döndü. 🔑 İki taraflı düzelt: test kendi satırlarını **silsin**, ve satır sayısına bağlı iddia **süzgeçle** bağımsızlaştırılsın (iddia "listede X var" değil, "X'in etiketi Türkçe"). ⚠️ Temizlik başka bir testin iddiasını **iddiasız bırakmamalı**: aynı oturumda kurulum SQL'i bütün tabloyu onarınca geri doldurma testi bozma turunda yeşil kaldı — onarım yalnız **kendi** satırlarını kapsamalı. | `PanelUsabilityTests`, `NotificationPreferenceAxisTests` |
| 🔴 **YENİ SINIF (12.7)** Dış bir sağlayıcının **kimlik jetonunu** mu doğruluyorsun (Google/Apple/OIDC)? | Beş kapının **hepsi** gerekli ve biri eksikse hasar sessizdir: imza · `iss` · **`aud`** · süre · **algoritma**. 🔴 **`aud` bir numaralı zafiyettir**: doğrulanmazsa *başka bir uygulamanın* jetonu bizde de geçerli olur — saldırgan kendi uygulamasına giren kurbanın jetonuyla **kurbanın hesabına girer** ve imza/issuer/süre doğru olduğu için hiçbir yerde hata görünmez. Algoritma sabitlenmezse jetonun kendi `alg`'ı belirleyici olur (`alg: none` / HS256 karıştırması). ⚠️ **Fail-CLOSED**: anahtar sunucusuna ulaşılamıyorsa jeton **reddedilir** — Redis'teki fail-open kararının (§7 madde 36) bilinçli tersi, çünkü burada bedel "fazladan e-posta" değil **doğrulanmamış jetonla hesaba girmek**. ⚠️ Client id listesi **boşsa sağlayıcı KAPALI** olmalı; "boş liste = herkesi kabul et" aynı deliğin en geniş hâli. ⚠️ `sub` boşsa reddet: `sub`'sız iki kişi **aynı** kimliğe eşlenir ve benzersiz indeks yüzünden ikincisi **birincinin hesabına** girer. | ARCHITECTURE.md §7 madde 68, `SocialTokenVerifierTests` |
| 🔴 **YENİ SINIF (12.7)** Bir dış kimliği **var olan bir hesapla eşleştiriyor** musun? | **E-posta ile ASLA.** Eşleştirme yalnız sağlayıcının `sub`'ı ile yapılır ve bağlamanın tek meşru yolu **oturum sahibinin kendi ucudur** (orada kullanıcı iki şeyi birden kanıtlar: hesabına ve sosyal hesabına erişim). `User.Email` bu projede **panelden elle giriliyor ve hiç doğrulanmıyor** — e-posta eşleşmesiyle otomatik bağlamak, kurbanın adresiyle bir Google hesabı açan saldırgana **doğrudan hesabı** verirdi. Ayrıca e-posta değişebilir, `sub` değişmez: e-postaya bakan bir bağ kullanıcı adresini değiştirdiğinde **sessizce kopar**. ⚠️ Zaten başkasına bağlı bir kimliği **sessizce taşıma**, çatışmayı **söyle**. | ARCHITECTURE.md §7 madde 69, `SocialLoginTests` |
| ⚠️ **(12.7)** Yeni bir giriş yolu mu açıyorsun? | Var olan **bütün kapıların** o yolda da geçerli olduğunu doğrula: ban · pasiflik · silinmiş hesap. Ayrı bir kapı, moderasyon kararını **sessizce deler** — banlanan kullanıcı "Google ile giriş"e basıp içeri girer. ⚠️ Yeni yol ayrı bir **giriş denemesi kanalı** olmalı (`LoginChannels`): var olan bir kanala bindirilirse panelde *"bu hesaba hangi yoldan girildi?"* sorusunun cevabı sessizce yanlış olur. Ve **başarısız denemesi de kaydedilmeli** — yoksa o yola yapılan saldırı tamamen görünmez kalır. | ARCHITECTURE.md §7 madde 68, `SocialLoginTests.ARejectedSocialAttempt_IsRecordedForThePanel` |
| 🐛 **(12.7)** Yapılandırmayı **DI kaydı sırasında** mı okuyorsun (`AddX(services, cfg)` gövdesinde)? | Okuma, **çözülme anına** ertelenmeli (`AddSingleton(sp => …)`). `AddInfrastructure` `builder.Build()`'den ÖNCE koşar; kayıt anında okunan bir değeri `ConfigureAppConfiguration` ile **ezmek mümkün değildir** (§8 "bilinen test tuzakları" — hız sınırının aynı sınıfı). 12.7'de birebir yaşandı: kod doğruydu ama **kendi entegrasyon testinden erişilemiyordu** ve uç "sağlayıcı kapalı" diye 400 döndü. 🔑 Bayrakla kapalı bir yolun **test edilemez** hâli, hiç test edilmemiş yoldan farksızdır (10.11'in dersi). | ARCHITECTURE.md §8, `SocialLoginWebApplicationFactory` |
| ⚠️ **(12.7)** Yeni bir tabloya **kişisel veri** mi yazıyorsun — ve hesap silinince ne oluyor? | Soruyu **açıkça cevapla ve yaz**, çünkü iki doğru cevap var ve karışırlarsa ikisi de sessiz hasar verir. Kanıt değeri **olmayan** kişisel veri (sosyal `sub`, e-posta) → hesap silinince **fiziksel silinir**: 10.8 anonimleştirme sözü veriyor ve satır kalırsa söz ihlal edilir. ⚠️ Üstelik **benzersiz** bir kolonsa ikinci bir hasar daha var: kalan satır o kişinin aynı hesapla **bir daha asla** kayıt olamaması demektir — telefonu yeniden kayda açan silme akışının tam tersi, ve hata mesajı *"bu hesap başka bir kullanıcıya bağlı"* olur: doğru ama anlaşılmaz. | ARCHITECTURE.md §7 madde 70, `SocialLoginTests.DeletingTheAccount_…` |
| 🔴 **YENİ SINIF (12.7 bozma turu)** Bir kuralı **iki bağımsız sebep** birden koruyor mu? | Öyleyse testin **hangisini** tuttuğunu **ölç** — çünkü biri *tesadüfi* olabilir ve kaybolduğunda kilit de kaybolur. 12.7'de birebir yaşandı: *"sosyal jeton telefonlu kayıt jetonu yerine geçemez"* kuralını hem `token_type` kontrolü hem de *"sosyal jetonda zaten `phone` claim'i yok"* gerçeği koruyordu. Test yeşildi, sözleşme doğruydu — ama `token_type` kontrolü **tamamen silindiğinde test yine yeşil kaldı**: ölçtüğü şey tesadüfi olandı. Jetona yarın bir `phone` claim'i eklense (ör. *"sağlayıcıdan gelen telefonu ön dolduralım"*) ayakta kalan tek koruma `token_type` olurdu ve onu silen değişikliği hiçbir test yakalamazdı → **OTP'siz kayıt**. 🔑 Çözüm: iddiayı, **tesadüfi korumanın devre dışı kaldığı** durumu kurarak yaz (burada: jetonu elle üret — sosyal türde **ama telefon taşıyan**). ⚠️ Bu, *"iddiası zayıf test"* sınıfının **altıncı** tekrarı ve **ilk kez bozma turu tarafından** yakalananı — yani bozma turu bir ritüel değil, gerçekten çalışan bir araç. | ARCHITECTURE.md §7 madde 70, `JwtProviderSocialTokenTests` |
| 🐛 Bir onay penceresi (`data-confirm`) **butona** mı yazılıyor? | Yazma — dinleyici submit olayında **formun** özniteliğine bakar. Butona yazmak yalnız tek formda **birden çok aksiyon** olduğunda ve o görünüm muafiyet listesindeyken güvenlidir. ⚠️ Doğru çözüm **muafiyet listesini büyütmek değil**: liste dosya adına bakıyor, "Edit.cshtml" yazmak projedeki **bütün** Düzenle formlarını muaf kılar. 12.13'te ikinci aksiyon forma **kardeş** yapıldı — iç içe `<form>` riski de, gereksiz multipart gövde de aynı anda yok oldu. | `PanelConfirmDialogTests.EveryConfirmAttribute_SitsOnAFormElement` |

---

## Bakım

Bu dosyanın **referansları** `CodeReviewChecklistDocTests` ile kilitlidir
(`ARCHITECTURE.md` ↔ `ArchitectureDocTests` ilişkisinin aynısı). Denetlenen şey
**maddelerin doğruluğu değil, atıflarının gerçekliği**:

- atıf yapılan her test sınıfı (`…Tests`) ve mobil test dosyası (`…_test.dart` ) **var mı**,
- atıf yapılan ortak yardımcılar (`PanelDisplay`, `PanelMenu`, `PagedListFooter`, `_StatusBadge`…)
  **hâlâ duruyor mu**,
- katman bölümlerinden biri **kaybolmuş mu**,
- satır numarasına **çivilenmiş atıf** var mı.

Bir kuralın hâlâ *iyi bir kural* olup olmadığı insan kararıdır — o kısım kilitlenemez.
Test kırıldığında yapılacak şey testi gevşetmek değil, **checklist'i güncellemektir.**

İki yazım kuralı:

1. Satırlara **dosya:satır** yazma, **sınıf/yardımcı adı** yaz — `PanelDisplay.TL()`
   taşınsa bile aranabilir; bir görünümün "129. satırı" ise ilk düzenlemede yanlış olur.
   (`Checklist_DoesNotPinLineNumbers` bunu zorluyor.)
2. Bir madde **ortak bir bileşene** dönüştüğünde (yani artık unutulamaz hâle geldiğinde)
   satırı silme — bileşenin adını yazarak bırak; yeni gelen "neden böyle" sorusunun
   cevabı burada.

Son gözden geçirme: **14 Ağustos 2026 (Faz 12.7 bozma turu)** — §4'e bir satır daha:
bir kuralı **iki bağımsız sebep** koruyorsa testin **hangisini tuttuğunu ölçmek**
(bozma turunun bulduğu gerçek delik; ayrıntı `ARCHITECTURE.md` §7 madde 70).
Bir önceki: **13 Ağustos 2026 (Faz 12.7)** — §4'e beş satır: dış bir **kimlik
jetonunu** doğrulamanın beş kapısı (ve `aud`'un neden bir numaralı zafiyet olduğu), bir dış
kimliği var olan hesapla **e-posta üzerinden eşleştirme yasağı**, yeni bir giriş yolunun
var olan **bütün kapıları** (ban/pasiflik/silinmiş) ve kendi **kanalını** taşıması,
yapılandırmayı **DI kaydında okumanın** kodu kendi testinden erişilemez kılması, ve yeni bir
tabloya yazılan kişisel verinin **hesap silinince ne olacağının** açıkça yazılması.
Bir önceki: **13 Ağustos 2026 (Faz 12.15b)** — §4'e dört satır daha: yeni bir
bildirim **kaynağının** kendi tercih eksenini gerektirmesi, **JSON kolona eklenen alanın**
varsayılan başlatıcıya güvenememesi (ölç + geri doldur), `ExecuteSqlRaw`'a JSON literali
yazma tuzağı ve **test satırı bırakmanın** ilgisiz testleri kaydırması.
Aynı gün (Faz 12.15) — §4'e dört satır: **geri alınamaz dış
etkinin** üç katmanlı korunması, "gönderilebilir mi" koşulunun görünmezliğin **bütün eksenlerini**
sayması, yeni bir `relatedType`'ın eski sürümlerde ölmesine karşı gövdenin **kendi kendine
yeterliliği** ve EF'in **aynı kolon kümesindeki ikinci indeksi sessizce ezmesi**.
Bir önceki: **12 Ağustos 2026 (Faz 12.14b)** — §5'e altı satır: **zamanla kötüleşen
borcu** tanımak, dış içeriği yeniden yazarken sağlamanın **öncesiyle** hesaplanması, kalıcı ve
geçici dış hataların ayrılması, geriye dönük **geri doldurmanın** üç kuralı, paylaşılan
veritabanında yeni bir tekilleştirmenin **testleri kırması**, mobilde yazı ölçeği çarpanının
**tavanlanması**.
Önceki: **12 Ağustos 2026 (Faz 12.14)** — §5'e beş satır: istemcide **dış
kaynaklı HTML** çizmenin tek sahibi (ve `onLinkTap`/`<img>`/yer tutucu üçlüsü), aynalanmayan
hotlink görselin **gizlenmesi**, istemcide tutulan yerel listenin kaydın **anlık görüntüsünü**
saklaması (+ tavan + bozuk satır toleransı), bir taşma testinin **gerçekten kilitleyip
kilitlemediğinin ölçülmesi**, sunucudaki **en az uzunluk eşiğinin** istemcide de bilinmesi.
Önceki: **12 Ağustos 2026 (Faz 12.13)** — §11'e beş satır: bir kaydı görünmez
kılan yeni eksenin **semantiğini ölçüye göre seçmek** (+ önizlemenin gerçek sorgudan gelmesi,
iki yönde de), `Un…` önekinin izin türetmesinde **elle eklenmesi**, panelden tetiklenen uzun
işin **kuyruğa atılması + DB kilidi + takılmış koşu kurtarması**, bir performans bulgusunun
**sebebinin ölçülmesi** (yanlış sebep yanlış düzeltmeden pahalıdır) ve `data-confirm`'ün
**forma** yazılması.
Önceki: **11 Ağustos 2026 (Faz 12.12)** — §11'e beş satır: **dış kaynaktan veri
almanın üç hasar sınıfı** (kaynak susar · kaynak paneli ezer · silme hiç bildirilmez), dış
kaynağın tarih parametresinin saat dilimi (+ sayfa numarasıyla arşiv taramanın sessiz atlaması),
dış HTML'in **alım anında** temizlenmesi, görsel aynalamanın üç kapısı + tekilleştirme, toplu
yazan bir hatta kolon tavanının **partiyi** düşürmesi.
Önceki: **10 Ağustos 2026 (Faz 12.10)** — §11'e dört satır: bir kaydın durumunu
yazan **ikinci yolun** üç kuralı birden atlaması (iş kuralı · izin · denetim izi) ve alanın
silinmeden **reddedilmesi**, bir yolu kapatırken o yolun tek taşıyıcısı olduğu işlevin
karşılığının **önce yazılması** (+ aksiyon adının izin eylemini belirlemesi), form içine
konan ikinci aksiyon butonunun `formaction`/`formenctype`/`e.submitter` üçlüsü, hiç
kullanılmamış bir Tailwind sınıfının **görünmez buton** üretmesi.
Önceki: **10 Ağustos 2026 (Faz 12.9)** — §11'e beş satır: panele dış origin
eklememek (ve bu sınıfın **geliştirici makinesinde hiç görünmemesi**), satır içi `on*=`
işleyicisinin CSP altında **sessizce ölmesi** + `onsubmit="return f()"` → `preventDefault()`
tuzağı, C#'ta yaşayan Tailwind sınıflarının `content` listesine girmesi (ve testin sınıf
listesini **türetmesi**), commit edilen türetilmiş çıktının **sürüklenme kapısı**, çalışma
anında **kaynak dosya taramanın yalan söylemesi** (derlenmiş Razor).
Önceki: **10 Ağustos 2026 (Faz 12.6)** — §5'e dört satır: additive bir alanın
**yokluğunun** kaydı gizlememesi (ve istemcinin de sunucu gibi "elemek değil bildirmek"i),
haftanın günü dönüşümünün **mobildeki** tek sahibi + Dart'ın Pazartesi=1 hizasının *tesadüfî*
oluşu, "geçmiş/bitmiş" stilinin koşulunun eksiksiz yazılması (**golden'ın %0.5 toleransı bu
sınıfı tutamaz** → davranış testi, iki yönlü), sayfalı listede süzgecin **sunucuda** olması +
filtrenin aramayla tek nesnede taşınması + şeride "Tümü" eklenmesi.
Önceki: **9 Ağustos 2026 (Faz 12.5)** — §11'e altı satır: canlı denetimde
bulunan iki hata (pasifleştirilebilen sözlük değerinin kaydı **düzenlenemez** kılması,
Tailwind `peer-checked:`in genel kardeş seçicisiyle sızması) + dört satır (haftanın günü
dönüşümünün tek sahibi ve Pazar=0 kayması, bir kaydı görünmez kılabilen değerin yasaklanması,
"elemek mi bildirmek mi" kararı + süzgeçte bilinmeyen değer, alt kaydın ekle+sil'e mahkûm
kalmaması).
Önceki: **9 Ağustos 2026 (Faz 12.4)** — §11'e dört satır (liste/detay
projeksiyonunun tek sahibi, türetilmiş alanın çıpasının DB bayrağı değil kod sabiti olması,
"düz varsayım" geri doldurmasının gelecekte de doğru kalmasını sağlayan iki kapı, bölge/kapsam
tanımının sunucuda kalması + bilinmeyen değerin varsayılana düşmesi).
Önceki: **7 Ağustos 2026 (Faz 12.3)** — §11'e dört satır (türetilmiş alanın tek
sahibi + normalleştirmenin `SlugHelper`'dan geçmesi, geri doldurmanın migration'da kör SQL
olmaması ve eşleşmeyenleri raporlaması, bir modülün ilk kez otomatik bildirim göndermesi,
`Repository.Query()`'nin **AsNoTracking** olması) ve §5'e bir satır
(kabuk rotasına `push` → `Navigator._debugCheckDuplicatedPageKeys` çökmesi, `AppNav`).
Önceki: **6 Ağustos 2026 (Faz 12.2b)** — §11'e dört satır (hedeflemenin tek
sahibi + önizleme/gerçek paritesi, terminal alanın panelde geri alınmayı teklif etmemesi,
artımlı sayaç kolonlarının tamamlanma ölçütü, aynı `SaveChanges` içindeki FK'nın gezinme
özelliğinden kurulması).
Önceki: **6 Ağustos 2026 (Faz 12.2)** — §11'e dört satır (hesaptan türetilen
alanın maskelemeyi delmesi, ikiz güvenlik eşiklerinin tek kaynaktan gelmesi, uyarı kanalında
kısma + "kanalı elle dene" yolu, istemci IP'sinin tek yerden okunması).
Önceki: **5 Ağustos 2026 (Faz 12.1)** — §11'e altı satır (hata kaydı yazma
yolunun isteği düşürmemesi, tekilleştirme, istemciden gelen metin + XSS, PII maskeleme,
saklama süresi işi, yalnız-admin ekranların `AuditModule` karşılığı).
Önceki: **4 Ağustos 2026 (Faz 11.16b)** — §11'e beş satır (CSV'nin dört
sessiz tuzağı, tek-istek kırpması, dışa aktarmanın ekran filtresiyle aynı olması,
"sonucu süzen" controller deseni, çapraz-modül sorgusunda soft-delete).
Önceki: **4 Ağustos 2026 (Faz 11.16)** — **§11 (Yayın / Platform
yapılandırması)** eklendi: platform izinleri, dolaylı izne güvenmek, dev rotalarının
sızması, production ayar kapısı, `FileStorage:BaseUrl`'ün boş kalması, imzasız mağaza
artefaktı ve türetilen marka görselleri.
Önceki: **4 Ağustos 2026 (Faz 11.18)** — §4'e beş satır daha eklendi (toplu
işlem ad kuralı ↔ izin türetmesi, toplu işlemin tek-kayıt komutunu çağırması, sıralama
ayracının benzersizliği, parola politikasının tek sahibi, oturum tazeleme).
Önceki: **4 Ağustos 2026 (Faz 11.17)** — panelin dört yeni ekranı bu listeyle
yazıldı; §4'e dört satır eklendi (yalnız-admin ekran deseni, panel↔istemci zaman tanımı
paritesi, `IgnoreQueryFilters` + geri getirmenin `status`'e dokunmaması, denetim eylemi sözlüğü).

## Notlar

- Bu checklist, orijinal kurumsal şablondaki genel maddeleri (isimlendirme, null-check,
  okunabilirlik, gereksiz complexity) **elemedi** — onlar hâlâ geçerli, sadece burada
  tekrar edilmedi çünkü proje-spesifik değiller. PR review'da ikisini birlikte kullan.
- "⚠️ TEKRARLAYAN HATA" diye işaretlemediğim ama tablo içinde vurguladığım maddeler
  (RenderFlex taşması, timezone kayması, cache invalidator eksikliği, PanelPermission
  unutulması) bu projede **birden fazla oturumda** aynı sınıf hata olarak çıkmış —
  review'da bunlara normalden fazla ağırlık ver.
